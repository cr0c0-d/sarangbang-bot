# CLAUDE.md

이 파일은 Claude Code(또는 다른 코딩 에이전트)가 이 저장소에서 작업할 때 읽는 안내서입니다.

## 이 프로젝트가 뭔가

**망고 · 노래하는 망고 (sarangbang-bot)** — Node.js(ESM) 디스코드 봇. 한 저장소에서 **봇 두 개**를 돌립니다.
소유자의 개인 서버 2~3개에서 쓰는 용도.

| 봇 | `BOT_ROLE` | 하는 일 | 실행 |
|---|---|---|---|
| **망고** | `mango` (기본) | TTS 낭독 · 타이머 · 투표 · 이미지 정리 + 웹 갤러리 | `npm start` |
| **노래하는 망고** | `music` | 유튜브 음악 스트리밍 | `npm run start:music` |

**겸하는 모드는 없습니다.** 디스코드 애플리케이션도 둘입니다. (ARCHITECTURE 2.1)

소유자는 프로그래밍 경험이 없는 상태에서 "바이브코딩"으로 이 봇을 만들고 있습니다.
따라서:

- **코드 주석과 사용자용 메시지는 한국어로** 씁니다. 기존 파일들의 톤을 따라가세요.
- 에러 메시지는 원인만 말하지 말고 **다음에 뭘 하면 되는지**까지 적으세요.
  (예: "yt-dlp 실행 실패 → `npm run update-ytdlp` 로 다시 받아보세요")
- 소유자가 혼자 디버깅하기 어렵습니다. 큰 변경을 한 번에 넣기보다 **단계마다 동작하는 상태**를 유지하세요.

## 작업 전에 반드시 읽을 것

**[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** — 설계 근거, 불변조건, 함정이 전부 여기 있습니다.
특히 다음을 건드리기 전에는 해당 절을 먼저 읽으세요.

| 건드리려는 것 | 먼저 읽을 절 |
|---|---|
| 오디오 재생 경로, `StreamType`, opus 인코딩 | 3.2 |
| 음악/TTS 동시성, `connection.subscribe` | 3.3, 4번 불변조건 1 |
| 이미지 저장 경로, 폴더 이름 처리 | 3.6, 4번 불변조건 2, 7절 |
| 갤러리 다운로드 방식 (ZIP으로 바꾸려는 경우) | 3.5 — **ZIP은 소유자가 명시적으로 거부한 방식입니다** |
| 유튜브 추출 라이브러리 교체 | 3.1 |
| 재생목록 처리 | 3.1-1 — **`list=RD…` 는 목록이 아니다. 상한을 없애지 말 것** |
| 채널 설정 읽기/쓰기, `.env` 와의 우선순위 | 3.8 — **`config.*` 를 직접 읽지 말 것** |
| TTS 목소리 추가·변경 | 3.4 — 실재하지 않는 목소리를 넣기 쉽다 |
| 읽어주기 글자 정제 (`cleanText`) | 3.4 — **낱자만 있으면 무음이 된다. `speakJamo` 3단계를 지우지 말 것** |
| 채널 종류 검사 (`ChannelType`) | 3.4-1 — **음성채널도 텍스트 채널이다** |
| 이미지 폴더 경로에 서버 구분 추가 | 3.6 — 위험 대비 이득이 낮다. 단독 작업으로만 |
| 음악 제어판(버튼), 곡 넘기기 로직 | 3.6-1, 3.6-2 — **의도를 추측하지 말 것** |
| 갤러리 인증 범위 | 3.6-3 — **보기는 공개, 삭제·이동만 잠금. 이 경계를 없애지 말 것** |
| 타이머 (저장·복구·자동완성) | 3.4-2 — **재시작을 견뎌야 한다** |
| 기능 on/off, 명령어 차단 | 3.7-1 — **차단은 3곳에서만. 명령어마다 검사 금지** |
| 사진 자동 정리 | 3.6-5 — **데이터를 영구 삭제한다. 안전장치를 없애지 말 것** |
| 명령어 추가 | 3.6-6 — **버튼으로 되는 일을 명령어로 또 만들지 말 것** |
| 음량 조절 방식 | 3.2-1 — **`inlineVolume` 금지. 순수 JS opus 인코더뿐이라 끊긴다** |
| 제어판 메시지 수명, 재시작 정리 | 3.6-1a — **제어판은 메모리에만 두면 안 된다** |
| 지난 재생 목록 | 3.6-1b — **기록은 재생이 시작될 때만. 명령어를 새로 만들지 말 것** |
| 봇 두 개 (`BOT_ROLE`) | 2.1 — **겸하는 모드 없음. `/나가기` 는 양쪽에 있어야 한다** |
| 투표 | 3.6-7 — **만들기는 모달로. 모달에 파일 업로드 칸이 된다. 첨부는 다시 올려야 한다** |

서버 배포 관련 작업이면 **[docs/ORACLE-CLOUD.md](docs/ORACLE-CLOUD.md)** 도 보세요.
(Oracle Always Free 배포 절차, ARM 호환성 확인 결과, 유휴 인스턴스 회수 정책)

## 자주 쓰는 명령

```bash
npm install              # 의존성 설치
npm run update-ytdlp     # yt-dlp 바이너리 갱신 (음악이 안 나올 때 1순위 조치)
npm run deploy           # 망고 명령어 등록 (명령어 추가/수정 후 필수)
npm run deploy:music     # 노래하는 망고 명령어 등록
npm start                # 망고 실행
npm run start:music      # 노래하는 망고 실행
npm run verify           # 토큰 없이 가능한 자체 검증 (코드 수정 후 반드시 실행)
```

## 코드 규칙

- **ESM입니다. 상대 import에 `.js` 확장자를 반드시 붙이세요.** (`'./config.js'`)
- 파일 경로는 항상 `node:path`로 조립합니다. 문자열 이어붙이기 금지 (Windows/Linux 양쪽에서 돌아야 함).
- 디스크에 쓰기 전 경로는 반드시 `src/images/store.js`의 `folderPath()` / `filePath()`를 통과시킵니다.
- 새 슬래시 명령어는 각 기능 모듈의 `commands` 배열에 넣고, `src/commands.js`에서 모읍니다.
  추가한 뒤 `npm run deploy`를 실행해야 디스코드에 보입니다.
- 나만 보이는 응답은 **`flags: MessageFlags.Ephemeral`** 로 쓰세요.
  `ephemeral: true` 는 discord.js 14.27에서 deprecated 되어 경고를 찍습니다.
- **명령어 이름은 한국어로** 짓습니다 (`/재생`, `/채널설정`). 영문 이름은 쓰지 않습니다.
- **비개발자 친구들이 함께 씁니다.** 자주 쓰는 조작은 인자 있는 명령어보다 **버튼·드롭다운**으로
  만드세요 (`src/music/panel.js` 참고). 인자가 필요한 정밀 조작만 슬래시 명령어로 둡니다.
- **같은 일을 명령어와 버튼 양쪽으로 만들지 마세요.** 명령어가 30개까지 불어나 한 번 걷어냈습니다(3.6-6).
  상태를 보여주는 명령어는 별도로 만들지 말고 **인자 없이 실행했을 때** 보여주게 하세요.
- 채널 설정은 `settings.js` 의 `get` / `getWithSource` 로 읽습니다.
  `config.music.textChannelId` 같은 값을 직접 읽으면 `/채널설정` 이 무시됩니다.
- 설정 항목을 추가하려면 `settings.js` 의 `KEYS` 에만 넣으면 명령어 선택지가 따라옵니다.
- 새 기능 모듈을 만들면 `commands.js` 에서 `tag('키', 명령어들)` 로 감싸세요.
  그러면 `/기능` on/off 가 자동으로 적용됩니다. **명령어 안에서 직접 검사하지 마세요.**
- 채널 종류를 검사할 때 `ChannelType` 목록을 하드코딩하지 말고
  `channel.isTextBased()` / `channel.isVoiceBased()` 를 쓰세요. 음성채널은 둘 다 참입니다.
- 설정값은 하드코딩하지 말고 `src/config.js`를 거쳐 `.env`로 뺍니다.
  새 항목을 추가하면 `.env.example`에도 **한국어 설명과 함께** 추가하세요.
- 비밀값(`.env`, 토큰)은 절대 커밋하거나 로그에 찍지 마세요.

## 검증

**코드를 고쳤으면 `npm run verify` 를 돌리세요.** 디스코드 토큰 없이 50개 넘는 항목을 검사합니다
(명령어 스키마·이름 규칙, import 정합성, 경로 안전장치, 폴더 결정 순서, 설정 우선순위,
TTS 목소리 실재 여부, 링크 감지, 웹 인증·바인딩).
동작과 한계는 ARCHITECTURE.md 8절에 있습니다.

기능을 추가했다면 `verify.mjs` 에 검사도 같이 추가하세요.

**단, 토큰이 필요한 것은 자동 검증이 불가능합니다** — 인텐트 적용 여부, 슬래시 명령어 등록,
음성채널 접속, 반응 권한 등. 이건 봇을 실제로 띄워서 README의 확인 순서 1~8단계로만 알 수 있습니다.

## 하면 안 되는 것

- `bin/`, `data/`, `.env*`, `node_modules/`를 커밋하지 마세요 (`.gitignore`에 있습니다).
  `.env.music` 에도 토큰이 들어갑니다. 예시 파일(`.env*.example`)만 추적합니다.
- `libsodium-wrappers` 제거 금지 (음성 암호화에 필요).
- `GuildAudio.subscribeTo()`를 우회해 `connection.subscribe()` 직접 호출 금지.
- 이미지 일괄 다운로드를 ZIP으로 되돌리지 마세요. 요구사항 위반입니다.
