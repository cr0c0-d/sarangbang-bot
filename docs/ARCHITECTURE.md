# 망고 · 노래하는 망고 (sarangbang-bot) 설계 문서

이 문서는 **다른 개발자 또는 LLM 에이전트가 이 프로젝트를 처음 열었을 때
코드를 다 읽지 않고도 안전하게 수정할 수 있도록** 쓰였습니다.
"무엇을 하는가"보다 **"왜 이렇게 되어 있는가"와 "건드리면 깨지는 것"**에 무게를 둡니다.

작성 시점: 2026-08-31 / 소유자: hyde69ciel@gmail.com

---

## 0. 한 문단 요약

**망고 · 노래하는 망고 (sarangbang-bot)** — Node.js(ESM) 디스코드 봇. 한 저장소에서 **봇 두 개**를 돌립니다.
하나의 봇 계정으로 세 가지 기능을 제공한다.
(1) 유튜브 링크/검색어 → 음성채널 음악 스트리밍, (2) 특정 채팅방 글 → 음성채널 TTS 낭독,
(3) 특정 채널의 이미지 → 로컬 폴더 자동 정리 + 다중 선택 개별 다운로드 웹 갤러리.
외부 유료 API를 쓰지 않는다. 상태는 대부분 메모리에 있고, 채널 설정과 이미지 메타데이터만 JSON 파일로 남는다.
소유자의 개인 서버 **2~3개**에서 함께 쓴다. 런타임 상태는 길드별로 분리되어 있다(9절 3번 참고).

---

## 1. 환경 전제

| 항목 | 값 | 비고 |
|---|---|---|
| Node.js | v24.15.0에서 개발·검증 | `engines: >=20`. top-level await, `Readable.fromWeb`, 전역 `fetch` 사용 |
| 모듈 시스템 | ESM (`"type": "module"`) | 모든 상대 import는 **확장자 `.js` 필수** |
| OS | Windows 11에서 개발, Linux 이식 고려 | 경로는 전부 `node:path`로 조립 |
| ffmpeg | `ffmpeg-static` npm 패키지 | 시스템 PATH에 의존하지 않음 |
| yt-dlp | `bin/yt-dlp.exe` (Windows) / `bin/yt-dlp` | `npm run update-ytdlp`로 받음. **git에 커밋 안 함** |
| Python | **불필요** | TTS도 Node 구현체(`msedge-tts`)를 씀 |

### 검증된 의존성 버전

```
discord.js        14.27.0
@discordjs/voice   0.19.2
prism-media         1.3.5   (@discordjs/voice가 내부적으로 사용)
libsodium-wrappers          (음성 암호화. 없으면 음성 연결 자체가 실패)
ffmpeg-static               (libopus 인코더 + Ogg Opus 먹서 포함 확인함)
msedge-tts                  (Microsoft Edge TTS 웹소켓 래퍼)
express                     (이미지 갤러리)
opusscript                  (예비용. 현재 경로에서는 실제로 쓰이지 않음 — 3.3절 참고)
dotenv
```

---

## 2. 파일 지도

```
src/
  index.js              진입점. 클라이언트 생성, 이벤트 라우팅, 웹서버 기동, 종료 처리
  config.js             .env 파싱 + 필수값 검증. 값이 없으면 여기서 프로세스를 죽인다
  commands.js           모든 슬래시 명령어를 모으는 곳 + /핑, /도움말
  channel-commands.js   /채널설정 (상태 보기 + 지정 + 해제 버튼을 하나로 합침)
  feature-commands.js   /기능 — 기능별 on/off 패널 (버튼)
  settings.js           ★ 명령어로 바꾼 설정을 기억 (data/settings.json). .env 를 덮어씀
  deploy-commands.js    슬래시 명령어를 디스코드에 등록 (npm run deploy)

  audio/
    ffmpeg.js           아무 오디오 → Ogg Opus 48kHz 스테레오 변환 (spawn 래퍼)
    guild-audio.js      ★ 서버당 음성 상태 전체. 음악 큐 + TTS 끼어들기 조정

  music/
    ytdlp.js            yt-dlp 프로세스 래퍼. 메타데이터 조회 + 오디오 스트림 생성
    commands.js         /재생 등 음악 명령어 + 채팅방 유튜브 링크 자동 감지

  timer/
    index.js            TTS 타이머 + 단어 등록 (data/timers.json 에 저장)

  tts/
    voices.js           쓸 수 있는 목소리 14종 (다국어 포함)
    synth.js            텍스트 → mp3 스트림 (msedge-tts) + 연결 예열
    index.js            메시지 정제(멘션/이모지/링크 처리) + TTS 명령어

  images/
    cleanup.js          오래된 사진 자동 정리 (예산 기준 + 안전장치)
    panel.js            갤러리 바로가기 버튼을 채널 맨 아래에 유지
    store.js            ★ 디스크 저장소. 경로 안전장치, 폴더 결정 규칙, 메타데이터
    commands.js         이미지 자동 저장 훅 + /폴더 등 명령어

  web/
    server.js           Express 갤러리. HTML/CSS/JS를 이 파일 안에 문자열로 들고 있음

scripts/update-ytdlp.mjs  yt-dlp 바이너리 다운로더
bin/                      yt-dlp 바이너리 (gitignore)
data/settings.json        /채널설정 으로 바꾼 값 (gitignore)
data/timers.json          진행 중인 타이머 + 등록한 알람 단어 (gitignore)
data/images/              저장된 이미지 + _meta.json + _folders.json (gitignore)
```

★ 표시된 세 파일이 이 프로젝트의 무게중심이다. 수정 전에 반드시 읽을 것.

---

## 2.1 한 저장소, 봇 **두 개** (`BOT_ROLE`)

**겸하는 모드는 없다.** 소유자가 완전한 분리를 요구했고, `all` 은 없앴다.
되살리지 말 것 — 셋이 되는 순간 아래 분담표가 전부 무너진다.

| `BOT_ROLE` | 이름 | 맡는 기능 | 실행 | 설정 파일 |
|---|---|---|---|---|
| `mango` (기본) | 망고 | tts, timer, images | `npm start` | `.env` |
| `music` | 노래하는 망고 | music | `npm run start:music` | `.env.music` |

### 반드시 애플리케이션이 두 개여야 한다

**같은 토큰으로 두 번 띄우면 두 프로세스가 같은 이벤트를 받아 모든 명령에 두 번 답한다.**
게다가 `deploy` 는 `PUT` 덮어쓰기라, 음악 봇을 같은 애플리케이션에 등록하면
**나머지 봇의 명령어 14개가 통째로 사라진다.**

이건 `.env.music` 을 만들면서 토큰만 안 바꾸면 바로 일어나는 사고다.
그래서 `config.js` 의 `assertDifferentApplications()` 가 `.env` 와 `.env.music` 의
`DISCORD_TOKEN` / `CLIENT_ID` 를 비교해 같으면 **실행을 멈춘다.** 이 검사를 없애지 말 것.

### 역할 표는 **하나뿐이다**

`config.js` 의 `ROLE_FEATURES` 가 역할 → 기능 목록을 정하고, 나머지는 전부
기존 `feature` 개념을 재사용한다. 새 표를 만들지 말 것.

- 명령어: `commands.js` 의 `tag(feature, ...)` 를 `inRole()` 로 거른다
- 설정 항목: `KEYS` 각 항목의 `feature` → `activeKeys()`
- 기능 on/off: `FEATURES` 키 자체가 기능 이름 → `activeFeatures()`
- `/도움말` 문단, `/음량` 옵션도 같은 `inRole()`

`inRole()` 과 `featureEnabled()` 는 **다른 것이다.**
역할에 없으면 명령어가 아예 등록되지 않는다. 역할에 있는데 꺼져 있으면
명령어는 보이고 "꺼져 있습니다" 라고 답한다.

### 공용 명령어 4개는 양쪽에 다 있다

`/도움말` `/기능` `/채널설정` `/나가기` 는 **봇마다 따로 설정해야 하므로** 양쪽에 등록된다.

**`/나가기` 가 여기 있는 이유를 지우지 말 것.** 원래 음악 명령어였는데,
읽어주기와 타이머 알람도 음성채널에 들어간다. 음악 쪽에만 두면
**망고가 음성채널에서 나올 방법이 사라진다.** 그래서 `src/leave-commands.js` 로 뺐다.
디스코드 명령어 목록에 같은 이름이 두 번 보이고, 봇 아이콘으로만 구분된다.
불편하지만 이게 맞다 — 음악 봇을 끄는 `/기능` 과 나머지 봇을 끄는 `/기능` 은 다른 일이다.
대신 각자 **자기 역할의 항목만** 보여준다.

`verify.mjs` 가 봇마다 실제로 프로세스를 띄워 `mango ∪ music = 18개`,
`mango ∩ music = 공용 4개`, **양쪽 다 `/나가기` 를 가짐** 을 검사한다. 이 불변조건을 깨지 말 것.

### 갈라놓아야 하는 것들

| | 이유 |
|---|---|
| `DATA_DIR` | 두 프로세스가 같은 JSON 을 쓰면 나중 쓰기가 앞의 것을 덮는다. 역할별로 소유하는 키가 겹치지 않으므로 폴더만 나누면 충돌이 없다 |
| 갤러리 웹서버 | 둘 다 띄우면 나중 쪽이 포트 충돌로 죽는다. `images` 를 맡은 봇만 띄운다 |
| 음성 커넥션 | **오히려 이게 나누는 이득이다.** 애플리케이션이 다르면 각자 커넥션을 갖는다 → 3.3절의 "음악을 멈추고 읽어주기" 가 사라지고 **동시에** 난다 |
| `/음량` | 음악 전용이 됐다(`src/music/volume-commands.js`). 읽어주기 음량 조절은 **소유자 요청으로 걷어냈다** — 되살리지 말 것 |

`.env.music` 에는 **다른 값만** 적으면 된다. `--env-file` 로 넣은 값이 `.env` 를 이기고,
없는 값은 `.env` 것이 채워진다(실측 확인). 그래서 보통 4줄이면 끝난다.

**주의: 나누면 유튜브 링크 배타 처리가 봇 사이에서는 성립하지 않는다.**
음악 봇이 링크를 처리해도 나머지 봇은 모르므로 그 메시지를 읽으려 한다.
`cleanText()` 가 링크를 '링크' 로 바꿔 읽기 때문에 큰 문제는 없지만, 이 성질에 기대고 있다.

---

## 3. 핵심 설계 결정과 근거

각 항목은 **"왜 다른 방법을 쓰지 않았는지"**를 포함한다. 되돌리기 전에 읽을 것.

### 3.1 유튜브 추출: `ytdl-core` 계열이 아니라 `yt-dlp` 바이너리

- `ytdl-core` / `@distube/ytdl-core` 같은 순수 JS 라이브러리는 유튜브가 서명 알고리즘을
  바꿀 때마다 깨지고, 수리까지 며칠씩 걸린다. 개인용 봇이 가장 자주 죽는 원인이다.
- `yt-dlp`는 대응이 며칠이 아니라 몇 시간 단위다. **음악이 안 나오면 90%는
  `npm run update-ytdlp` 로 해결된다.** 이 사실이 README와 에러 메시지에 박혀 있어야 한다.
- 바이너리를 npm 패키지(`youtube-dl-exec` 등)로 받지 않고 `scripts/update-ytdlp.mjs`로
  직접 받는 이유: npm 패키지의 postinstall이 회사 프록시/방화벽에서 자주 실패하고,
  버전을 우리가 원할 때 갱신할 수 없기 때문.

### 3.1-1 yt-dlp 오류 처리: 프로세스 단위 재시도 + 오진 방지

**일시적 오류는 `run()` 안에서 프로세스를 새로 띄워 재시도한다** (25초 × 최대 3회).

- 대표 사례 `The page needs to be reloaded.` 는 유튜브가 간헐적으로 뱉는 거부다.
  같은 영상이 몇 초 뒤엔 정상 동작한다 (실측 확인).
- yt-dlp 자체 `--extractor-retries`(기본 3회)로는 안 잡힌다. 이 오류를 재시도 대상으로
  분류하지 않기 때문이다. **프로세스를 새로 띄워야** 웹페이지와 player JSON을 다시 받는다.
- 재시도 대상은 `isTransient()` 의 목록으로 판단한다. `Video unavailable` 처럼
  다시 해도 안 되는 오류는 **재시도하지 않는다** (사용자를 75초 기다리게 하면 안 되므로).
- 재시도는 **메타데이터 조회(`getTracks`) 경로에만** 있다.
  스트림(`createStream`)은 이미 ffmpeg에 연결된 뒤라 같은 방식으로 재시도할 수 없다.
  스트림 단계에서 이 오류가 실제로 문제가 되는 것을 확인하기 전에는 트랙 단위 재시도를 만들지 말 것.
  (만든다면 `playbackDuration` 같은 추측이 아니라 `skip()`/`stop()` 이 세우는 명시적 플래그로 구분해야 한다)

**"일시적" 분류는 확정이 아니다.** `The page needs to be reloaded` 는 진짜 일시적 딸꾹질일 때도 있고,
**클라우드 IP 차단의 다른 얼굴**일 때도 있다(실측 확인: 같은 서버에서 재시도 3회 후에도 실패,
verbose 로 보면 `playability status: LOGIN_REQUIRED`). 그래서 재시도를 다 쓰고 실패하면
`run()` 이 "일시적이 아닐 수 있다 + IP 차단 가능성 + 진단 명령" 을 메시지에 덧붙인다.
"잠시 뒤 다시 시도하세요" 만 남기면 소유자가 원인을 영원히 못 찾는다.

**yt-dlp 에 JS 런타임을 항상 넘긴다.** `extraArgs()` 가 `--js-runtimes node:${process.execPath}` 를 붙인다.
yt-dlp 는 유튜브 추출에 JS 런타임이 필요한데 기본으로 찾는 건 `deno` 뿐이고 보통 안 깔려 있다
(서버에서 `JS runtimes: none` + deprecation 경고 확인). 이 봇은 Node 로 돌아가므로
`process.execPath` 가 항상 유효한 node 경로다 — 어느 OS에서도 추가 설치가 필요 없다.

**`friendlyError()` 에서 짧은 단어로 오류를 분류하지 말 것.**
예전에 `stderr.includes('bot')` 으로 유튜브 차단을 판별했는데,
프로젝트 폴더 이름이 `sarangbang-bot` 이라 **경로가 찍힌 아무 오류나 "유튜브 차단" 으로 오진**했다.
특히 쿠키 파일 경로가 틀렸을 때 "쿠키를 설정하세요" 라고 안내하는 최악의 조합이 된다.
지금은 `'sign in to confirm'` 전체 문구로만 판별하고, `verify.mjs` 에 회귀 검사가 있다.

### 3.1-1-1 클라우드에서 음악을 되게 하려면 — **쿠키 + JS 런타임 둘 다**

Oracle 서버에서 음악이 전혀 안 되던 문제의 **확정된 원인**이다. 하나만으로는 안 된다.

디버깅 과정이 헷갈렸던 이유는 **고치는 도중에 증상이 바뀌기 때문**이다.

| 단계 | 증상 | 원인 |
|---|---|---|
| 쿠키 ✗ · JS런타임 ✗ | `playability status: LOGIN_REQUIRED`<br>`Sign in to confirm you're not a bot` | 데이터센터 IP 차단 |
| 쿠키 ✓ · JS런타임 ✗ | `n challenge solving failed`<br>→ `The page needs to be reloaded` | 차단은 풀렸으나 서명 계산 불가 |
| 쿠키 ✓ · JS런타임 ✓ | **정상 재생** | — |

**두 번째 줄이 함정이다.** 쿠키를 넣으면 차단이 풀리면서 `web_embedded`, `tv downgraded` 같은
클라이언트를 실제로 시도하게 되는데, 그 클라이언트들은 **n challenge(서명 계산)** 를 요구한다.
그건 JS 런타임이 있어야 풀린다. 그리고 실패하면 마지막 줄이
`The page needs to be reloaded` 라서, **차단 문제로 되돌아간 것처럼 보인다.**

그래서 `friendlyError()` 는 **n challenge 를 "page needs to be reloaded" 보다 먼저** 검사한다.
순서를 바꾸면 원인을 영영 못 찾는다.

**JS 런타임은 `--js-runtimes node:${process.execPath}` 로 항상 넘긴다** (3.1-1절).
`.env` 의 `YTDLP_JS_RUNTIME=false` 는 이걸 끄므로, **클라우드에서는 절대 끄면 안 된다.**
(느린 서버에서 기동 비용을 줄이려고 만든 스위치인데, 차단 환경에서는 재생 자체를 막는다)

**확인해본 것들 — 효과 없음**
- `--js-runtimes` 를 켜도 `PO Token Providers` 는 여전히 `none` 이다.
  "JS 런타임을 깔면 PO 토큰을 생성한다" 는 통설은 이 버전에서 성립하지 않는다.
- JS 런타임을 켜도 쓸 수 있는 player client 는 `default`/`visionos` 뿐이다 (실측).
  `web`, `tv`, `ios`, `mweb`, `android_vr` 은 전부 실패한다.

**쿠키는 만료된다.** 만료되면 첫 번째 줄 증상으로 되돌아간다.
그때 `friendlyError` 가 "쿠키가 설정되어 있으니 만료된 것" 이라고 안내하도록,
`YTDLP_COOKIES_FILE` 설정 여부에 따라 문구를 바꾼다.

### 3.1-2 (길잡이) 재생이 왜 느렸고 무엇을 고쳤나 — **읽는 순서**

아래 절들이 번호 순서대로 놓여 있지 않습니다(2a·2b·2c·2d·2e 가 섞여 있음).
**고친 순서대로** 읽으면 이렇습니다. 2026-09-02 하루에 걸쳐 실측하며 고쳤습니다.

| 순서 | 절 | 무엇이 문제였나 | 결과 |
|---|---|---|---|
| 1 | **3.1-2** | 곡당 추출을 두 번 했다 | 5.5초 → 2.7초 |
| 2 | **3.1-2a** | 쿠키 서버는 0단계가 막혀 곧바로 가장 느린 길로 떨어졌다 | 파이프 3.5초 → 1.4초 |
| 3 | **3.1-2b** | 추출을 **동시에** 돌려 서로 굶겼다 | 73초 → 12초, 봇이 멈추던 것도 사라짐 |
| 4 | **3.1-2b** 아래 | 첫 시도 제한시간이 고정 20초라 **매번 잘렸다** | 잘림 사라짐 |
| 5 | **3.1-2d** | yt-dlp 기동만 3~6초 (매번 파이썬을 풀었다) | **3.0초 → 0.5초** |
| 6 | **3.1-2e** | 주소만 미리 뽑아둬도 첫 소리까지 9~11초 | **0.0초** (미리 열어둠) |
| — | **3.1-2c** | ⚠️ 속도를 줄이려 `YTDLP_JS_RUNTIME=false` 를 켰다가 **재생이 아예 안 됐다** | 되돌림. 켜지 말 것 |

**최종 결과 (소유자 서버, 1코어 ARM)**

| | 하루 전 | 지금 |
|---|---|---|
| yt-dlp 기동 | 3.0~5.7초 | **0.5초** |
| 곡 전환 | 9~11초 (심하면 31초) | **0.0초** (준비가 끝났을 때) |
| 첫 곡 | 22~25초 | 10초 안팎 |
| 추출 제한시간 | 매번 20초에 잘림 | 안 잘림 |

**이 과정에서 배운 것 — 다음에도 그대로 적용할 것**

1. **추측하지 말고 잰다.** 프로브 축소는 처음 한 번 재고 "1.6초 → 88ms" 로 보였지만,
   순서를 번갈아 3회씩 재니 **차이가 없었다**(3.1-2a 아래). 한 번 재고 판단하면 안 된다.
2. **로그가 없으면 원인을 못 찾는다.** `곡 정보 / 첫 소리까지 / 미리 뽑기 / 기동` 네 줄을
   넣은 뒤부터 매번 한 번에 짚혔다(3.1-2b 아래).
3. **바깥 도구가 한 말을 그대로 보여준다.** 모델명이 바뀐 것도, pip 의 `[default]` 누락도
   그 줄 하나로 끝났다(3.1-4).
4. **1코어에서 병렬은 손해다.** 빨라 보이지만 실측은 반대였다(3.1-2b).

### 3.1-2 추출은 곡당 딱 한 번만 — 속도의 핵심

**측정값 (2026-08-31, 가정용 IP):**

| 항목 | 시간 |
|---|---|
| yt-dlp 추출 1회 | **약 2.8초** — `--dump-single-json`, `--print`, `--get-url` 모두 비슷 |
| 추출 후 ffmpeg 이 주소에서 첫 바이트 받기 | **약 100~150ms** |

즉 **무엇을 요청하든 추출 비용은 고정**이다(유튜브 응답 대기). 우리가 줄일 수 있는 건 **횟수**뿐이다.

초기 구현은 `getTracks`(2.8초) → `createStream`(yt-dlp 재실행 2.75초)로 **두 번** 추출해서
첫 곡 재생까지 약 5.5초가 걸렸다. 지금은 `getTracks` 가 `--print` 로
**제목·길이·썸네일·재생주소를 한 번에** 받아오고, 재생할 때 그 주소를 ffmpeg 에 직접 넘긴다.
→ **5.5초 → 2.7초** (실측)

- `createSource(track)` 가 판단한다 — 자세한 단계는 **3.1-2a**.
- 재생목록은 `--flat-playlist` 로 목록만 가볍게 받는다. 수십 곡의 주소를 미리 다 뽑으면
  오히려 느리고, 대부분 재생 전에 만료된다.
- `-f` 로 **오디오 포맷을 하나만** 고르는 것이 중요하다. 여러 개가 뽑히면 `%(urls)s` 가
  여러 줄이 되어 `--print` 의 줄 순서가 어긋난다. 그래서 `%(urls)s` 를 **마지막**에 둔다.
- 주소는 직접 HTTP 로 받으므로 ffmpeg 에 `-reconnect` 계열 옵션을 붙였다.
  유튜브 CDN 은 긴 곡에서 간헐적으로 연결을 끊는다.

### 3.1-2a 원본 준비는 **세 단계**다 — 가운데 단계를 지우지 말 것

`createSource()` 는 세 단계 중 하나를 고른다. 위에서부터 빠르고, 실패하면 한 칸 내려간다.

| 단계 | 하는 일 | 첫 소리까지 (실측 2026-09-01, 3회 중간값) |
|---|---|---|
| **0 직접** | 뽑아둔 주소를 **ffmpeg 이 직접** 받는다. yt-dlp 를 아예 안 쓴다 | 약 **90ms** |
| **1 주소** | 뽑아둔 주소를 **yt-dlp 가** 받는다. 유튜브 추출만 건너뛴다 | **1,362ms** (1375·1350·1362) |
| **2 추출** | 유튜브에서 처음부터 다시 뽑는다 | **3,518ms** (3644·3518·3028) |

**1단계는 왜 생겼나 (지우면 안 되는 이유).**
예전에는 0 아니면 2 뿐이었다. 그런데 **쿠키를 쓰는 서버는 0단계가 거의 항상 거부된다**
(`[music] 직접 수신 실패` 가 곡마다 찍힘). 그러면 곧바로 2단계로 떨어져서
**한 곡에 유튜브 추출을 두 번**(대기열에 담을 때 + 틀 때) 하게 된다.

1단계는 그 두 번째 추출을 없앤다. yt-dlp 가 받으므로 **쿠키·프록시가 그대로 적용된다.**
→ 파이프 경로 3.5초 → 1.4초 (위 표)

부수 효과가 더 크다: 0단계가 꺼지면 `prefetchNext()` 로 미리 뽑아둔 주소가
**그냥 버려지고 있었다.** 1단계가 그 주소를 쓰므로 미리 뽑기가 비로소 이득이 된다.

⚠️ **1단계에서는 `-f` 를 넘기지 않는다.** 재생 주소에는 고를 포맷이 하나뿐이라
`bestaudio[acodec=opus]` 같은 조건을 걸면 도리어 못 찾고 실패한다.

⚠️ `MUSIC_DIRECT_STREAM=false` 는 **0단계만** 끈다. 1단계로 시작한다.
   (예전에는 이 스위치가 2단계로 떨어지는 것을 뜻해서 오히려 느렸다)

#### ⚠️ "두 번 연속 실패하면 끈다" 가 작동하지 않던 버그

0단계가 안 되는 서버에서 곡마다 헛걸음하지 말라고 `noteDirectFailure()` 를 두었다.
그런데 서버 로그에는 **곡마다** `직접 수신 실패` 가 찍혔다 — 자동으로 꺼지지 않은 것이다.

```
09:18:56  직접 수신 실패 → 뽑아둔 주소로 재시도
09:20:48  직접 수신 실패 → 뽑아둔 주소로 재시도
09:21:53  직접 수신 실패 → 뽑아둔 주소로 재시도    ← 두 번째에 꺼졌어야 한다
```

원인: 성공 판정을 `AudioPlayerStatus.Playing` 에서 했다. **직접 수신이 거부돼도
플레이어는 Playing 을 잠깐 지나간다** (소리는 안 나는데도). 그래서 실패할 때마다
`noteDirectSuccess()` 가 불려 세던 숫자가 0으로 돌아갔다.

→ `confirmDirectLater()` 로 5초 뒤에 **실제로 3초 넘게 재생됐는지** 보고 판정한다.
   기준은 `playedNothing()` 과 같다. 그 사이 곡이 바뀌었으면 판정하지 않는다.

⚠️ **성공 판정을 다시 Playing 으로 옮기지 말 것.** 이 이벤트는 "소리가 났다" 가 아니라
"플레이어가 재생 상태가 됐다" 는 뜻이다. 둘은 다르다.

#### 재봤지만 효과가 없던 것 — ffmpeg 프로브 축소

`-probesize 65536 -analyzeduration 0` 을 붙이면 빨라질 것으로 보였다. **아니었다.**

| 경로 | 그대로 | 프로브 축소 |
|---|---|---|
| 1단계 (주소 파이프) | 1,362ms | 1,351ms |
| 2단계 (전체 추출) | 3,518ms | 3,767ms |

차이가 측정 잡음 안에 있다. **처음 한 번은 1.6초가 걸렸지만** 그건 프로브가 아니라
googlevideo 호스트에 처음 붙는 비용(DNS·TLS)이었다 — 순서를 번갈아 3회씩 재서 알아냈다.
**한 번 재고 판단하면 안 된다는 사례로 남긴다.** 붙이지 않았다.

### 3.1-3 다른 봇보다 왜 느린가 — 측정값

소유자가 "다른 음악봇은 1초 내외인데 왜 2.5초인가" 라고 물어 측정했다.

| 항목 | 시간 |
|---|---|
| **yt-dlp 프로세스 기동만** (`--version`, 네트워크 없음) | **약 1.07초** |
| 추출 전체 (기동 + 유튜브 응답) | 약 2.6~2.7초 |
| 같은 영상 두 번째 추출 | 2.67초 (캐시 효과 없음) |
| `--js-runtimes` 제거 | 2.59초 (거의 차이 없음) |

**절반 가까이가 프로세스 기동 비용이다.** yt-dlp 는 PyInstaller 번들이라 실행할 때마다
파이썬 런타임을 풀고 초기화한다. 다른 봇이 빠른 이유는 대개 **Lavalink**(상주하는 Java 오디오
노드) 나 순수 JS 라이브러리를 쓰기 때문이다 — 프로세스 기동이 없고, 플레이어 JS·서명 함수를
메모리에 캐시하며, HTTP 연결을 재사용한다.

**그럼에도 yt-dlp 를 유지한다** (3.1절). 순수 JS 라이브러리는 유튜브가 바뀔 때마다 깨지고,
Lavalink 는 Java 프로세스를 하나 더 띄워야 해서 1GB 서버에 부담이다.

**대신 네 가지로 체감을 줄였다.**
1. **추출을 곡당 1회로** (3.1-2) — 5.5초 → 2.7초
2. **미리 뽑아둔 주소를 실제로 쓰기** (3.1-2a) — 쿠키 서버에서는 이게 버려지고 있었다
3. **다음 곡 미리 추출** (`prefetchNext()`) — 지금 곡을 트는 동안 대기열 다음 곡의 주소를
   미리 뽑는다. **첫 곡만 2.7초를 기다리고, 이후 곡 전환은 사실상 즉시**다.
   미리 뽑는 사이 대기열이 바뀔 수 있으므로 `this.queue.includes(next)` 로 확인한 뒤 반영한다.
   `playNext()` 끝에서만이 아니라 **`add()` 에서도** 부른다 — 재생 중에 재생목록을 담으면
   (`--flat-playlist` 라 주소가 비어 있다) 곡이 끝난 **뒤에야** 추출이 시작됐다.
   단 **대기 중일 때는 하지 않는다.** 곧 그 곡을 바로 틀 텐데 1코어 서버에서
   쓸데없는 yt-dlp 하나가 opus 인코딩과 CPU를 다툰다.
4. **이미 아는 곡은 추출하지 않는다** — 지난 곡 목록에는 제목·길이가 **이미 저장돼 있다**
   (`history.json`). 그런데 고른 곡마다 `getTracks()` 를 돌려서, 담기만 하는데도 곡당
   몇 초가 걸렸다. `enqueue({ tracks })` 로 추출을 통째로 건너뛴다.
   재생 주소는 어차피 90분이면 만료되므로 미리 뽑아둘 값어치도 없다 —
   틀 때가 되면 `prefetchNext()` 가 뽑는다.
   ⚠️ 그래서 **못 트는 곡은 담을 때가 아니라 틀 때 드러난다.** 실패 안내(3.1-4)가 그걸 맡는다.
5. **버튼은 기다리지 않고 바로 답한다** — `⏮️` `⏭️` 는 제어판 갱신 전에 400ms 를 잤다
   (다음 곡 정보가 아직 없어서). 지금은 바로 답하고, 소리가 실제로 바뀌는 순간
   `schedulePanelRefresh()` 가 따라 갱신한다. 버튼 응답(`interaction.update`)과 겹쳐
   서로를 덮어쓰지 않도록 **모아서 한 번만** 반영한다.

더 줄이려면 Lavalink 로 갈아타는 수밖에 없다. 그건 아키텍처 교체이므로 단독 작업으로만.

### 3.1-2c ⚠️ `YTDLP_JS_RUNTIME=false` 로 속도를 줄이려 하지 말 것 — 재생이 아예 안 된다

2026-09-02 에 실제로 했다가 깨졌다. 기동이 빨라지는 것은 맞다. 그런데 유튜브 서명
(n challenge)을 못 풀어서 **모든 곡이 실패**한다. 로그에는 이렇게만 보인다.

```
[music] 미리 뽑기 실패 (27.3초) · …: 유튜브가 일시적으로 요청을 거부했습니다
[music] 재시도 1/1 (다음 제한시간 40초): 유튜브가 일시적으로 요청을 거부했습니다
```

**"일시적" 이라는 말이 함정이다.** 같은 문장이라도 JS 런타임을 꺼뒀으면 원인이 그것이고,
다시 해봐야 똑같다. 그래서 `isTransient()` 가 이 경우 **재시도 대상에서 뺀다** —
안 그러면 곡마다 40~60초를 버리고도 결국 실패한다.
`friendlyError()` 도 이때는 "그 줄이 원인" 이라고 못박고 지우는 명령까지 적어준다.

`첫 소리까지 6.7초` 같은 줄이 찍혀도 **소리가 났다는 뜻이 아니다.** 그 로그는
플레이어가 재생 상태가 된 순간에 찍힌다 — 3.6-1a·0단계 판정과 같은 함정이다.

### 3.3-1 음성채널에서 나가는 조건 — **사람이 있으면 안 나간다**

> "망고가 음성채팅에서 나가는 제한시간을 없애줘. 사람이 한명이라도 음성채팅에 있으면
> 계속 남아있는걸로"

망고는 **읽어주기·타이머** 때문에 음성채널에 들어가 있다. 그런데 `MUSIC_LEAVE_AFTER_SEC`
(기본 300초)이 지나면 조용하다는 이유로 나가버렸다. 대화 중에 봇이 갑자기 사라지고,
다시 부르는 것도 사람 몫이 된다.

| 상황 | 동작 |
|---|---|
| 사람이 한 명이라도 있음 | **계속 남아 있는다.** 시간이 지나도 나가지 않는다 |
| 아무도 없어짐 | `VoiceStateUpdate` 가 **그때 바로** 내보낸다 |
| 아무도 없는데 그 이벤트를 놓침 | `scheduleLeave()` 타이머가 보험으로 내보낸다 |

`hasHumanListener()` 가 **봇을 빼고** 센다. 음악 봇과 망고가 같은 채널에 있어도
서로를 "사람" 으로 세면 안 된다.

### 3.1-2e 다음 곡은 **소리까지** 미리 열어둔다 — 전환 침묵 없애기

`prefetchNext()` 는 **주소만** 미리 뽑는다. 그것만으로는 부족했다. 실측:

```
[music] 첫 소리까지 11.1초 · 뽑아둔 주소 · Don't Go Insane
[music] 첫 소리까지 10.0초 · 뽑아둔 주소 · No Blueberries
[music] 첫 소리까지  9.0초 · 뽑아둔 주소 · Dazed & Confused
```

주소를 이미 뽑아둔 1단계인데도 **9~11초**다. yt-dlp 를 띄우고(3초) 받아오는 시간이고,
그게 곡과 곡 사이에 **그대로 침묵**으로 들어간다.

→ 다음 곡의 ffmpeg 스트림까지 미리 열어두고, `playNext()` 가 **그것을 그대로 쓴다.**

**언제 여는가 — "곡 끝나기 40초 전" 은 틀렸다.**
처음에 그렇게 만들었더니 아무 효과가 없었다. 로그를 보니 이랬다.

```
13:16:14  첫 소리까지 9.9초 · 뽑아둔 주소 · Barcelona Nights
13:16:28  첫 소리까지 9.5초 · 뽑아둔 주소 · Ache In My Heart      ← 14초 만에 다음 곡
```

**곡을 넘겨가며 듣는다.** 그러면 "끝나기 40초 전" 이라는 시점이 아예 오지 않는다.
그래서 `prefetchNext()` 가 주소를 뽑아내는 **즉시** 소리까지 연다. 지금 곡이 시작되고
20초쯤 뒤에는 준비가 끝나 있다. `PREPARE_LEAD_SEC` 예약은 **보험으로만** 남겼다 —
준비가 실패했거나 중간에 끊겼을 때 한 번 더 시도하는 길이다.

⚠️ 14초 만에 넘기면 여전히 9~10초가 걸린다. 정직하게 말하면
   **"20초쯤 듣고 넘기면 그때부터는 즉시"** 다.

CPU 는 거의 안 쓴다. 미리 연 소리는 파이프가 차면 yt-dlp·ffmpeg 이 알아서 멈춰 기다린다.

**⚠️ 준비도 세 단계(3.1-2a)를 탄다.** 처음에는 0단계로만 시도하고 실패하면 포기했다.
그래서 0단계가 안 되는 서버에서 **`다음 곡 준비 완료` 가 한 번도 안 찍혔다** —
미리 열어두기가 통째로 무용지물이었는데 로그만 보면 알 수 없었다.

```
14:05:06  첫 소리까지 0.4초 · 직접 수신 · Don't Go Insane   ← 준비된 게 아니라 그냥 실패
14:05:10  첫 소리까지 4.2초 · 뽑아둔 주소 · Don't Go Insane  ← 그러고 나서 진짜 재생
```

이제 준비도 소리가 안 나오면 **한 단계 내려 다시** 연다.
`MUSIC_DIRECT_STREAM` 설정이 어떻든 미리 열어두기가 동작한다.
**설정에 기대지 말 것** — 소유자가 그 값을 어떻게 두든 코드가 버텨야 한다.

**⚠️ 열어둔 소리는 죽는다.** 몇 분을 노는 연결로 기다리므로 유튜브가 끊는다.
죽은 줄 모르고 틀면 "소리 안 남 → 실패 판정 → 아래 단계로 재시도" 가 되어
**아무것도 안 한 것보다 나빠진다.** 그래서
`end`·`error` 에 `dead` 를 표시해 버리고, **쓰는 순간에 다시 확인**한다.
첫 바이트가 왔다는 것은 *그때* 살아 있었다는 뜻일 뿐이다.

**빠뜨리면 안 되는 것**

- 쓸지 말지는 **객체가 같은지**로 판단한다(`prepared.item === item`). 대기열이 바뀌었거나
  재시도로 새로 만든 항목이면 다른 객체라 자동으로 걸러진다.
- 안 쓰게 된 것은 **반드시 `kill()`** 한다. 안 그러면 yt-dlp·ffmpeg 이 남아서,
  코어가 하나뿐인 서버에서는 그게 그대로 다음 곡을 느리게 만든다.
  `stop()` · `clearQueue()` · `destroy()` · 대기열 편집 뒤에도 정리한다.
- **음량이 바뀌면 버리고 다시 연다.** 음량은 ffmpeg 을 띄울 때 정해지므로(3.2-1)
  미리 연 소리에는 옛 음량이 박혀 있다. 안 버리면 다음 곡만 옛 음량으로 나온다.

### 3.1-2d 추출 자체를 줄이려면 — **pip 로 깐 yt-dlp** (`YTDLP_PATH`)

`bin/` 에 받는 공식 바이너리는 **PyInstaller 묶음**이다. 실행할 때마다 파이썬 런타임을
통째로 `/tmp` 에 풀고 시작한다. 그 비용이 **곡을 틀 때마다 그대로** 깔린다.

| | 기동 (`--version`) |
|---|---|
| 집 PC (Windows) | 1.6초 |
| 소유자 서버 (1코어 ARM) | **3.1~5.7초** |

추출 전체가 12초쯤인데 그중 3~6초가 이것이다. **가장 크게 줄일 수 있는 한 곳**이다.
pip 로 깔면 푸는 과정이 없어진다.

```bash
# Ubuntu 최소 설치에는 venv 가 빠져 있다. 없으면 "ensurepip is not available" 이 뜬다.
sudo apt update && sudo apt install -y python3-venv
rm -rf ~/.venv-ytdlp                          # 실패한 흔적이 남아 있으면 지운다
python3 -m venv ~/.venv-ytdlp && ~/.venv-ytdlp/bin/pip install -q -U yt-dlp
time ~/.venv-ytdlp/bin/yt-dlp --version      # 먼저 비교해볼 것
```

⚠️ **`[default]` 를 빼먹으면 안 된다.** 이걸 몰라서 한참 헤맸다.

`pip install yt-dlp` 는 **최소 의존성만** 깐다. `bin/` 의 공식 바이너리는 PyInstaller
묶음이라 `websockets`·`brotli`·`pycryptodomex` 같은 것을 **통째로 품고 있다.**
그래서 이런 일이 생긴다.

```
$ ~/.venv-ytdlp/bin/yt-dlp --simulate "https://www.youtube.com/watch?v=..."   ← 잘 됨
[music] 추출 실패 … --cookies /home/ubuntu/sarangbang-bot/cookies.txt …       ← 봇에서는 실패
```

**손으로 돌리면 되는데 봇에서만 안 된다.** 차이는 봇이 덧붙이는 `--cookies` 였고,
그 경로가 빠진 의존성을 필요로 했다. `[default]` 가 바이너리에 든 것들을 같이 깐다.

> 이 사건이 `실패한 명령` 로그(3.1-4)를 넣게 된 계기다. 그 줄이 없었다면
> 봇이 실제로 무슨 인자를 넘겼는지 짐작으로 재구성해야 했다.

빨라지면 `.env.music` 에 `YTDLP_PATH=/home/ubuntu/.venv-ytdlp/bin/yt-dlp`.
봇이 켤 때 기동 시간을 재서 경로와 함께 찍으므로 **효과가 바로 보인다.**
되돌리려면 그 줄만 지운다.

⚠️ `YTDLP_PATH` 를 쓰면 `npm run update-ytdlp` 는 **엉뚱한 파일(bin/)을 갱신**하게 된다.
   그래서 그 스크립트가 이 경우를 알아보고 pip 갱신 방법을 안내하고 멈춘다.

### 3.1-2b 추출은 **한 번에 하나만** — 1코어에서 병렬은 언제나 손해다

가장 큰 원인이 여기 있었다. 2026-09-02 실측:

```
[music] 미리 뽑기 실패 (73.2초) · Barcelona Nights: yt-dlp 가 40초 안에 응답하지 않았습니다
[music] 미리 뽑기 실패 (62.9초) · Sins:             yt-dlp 가 40초 안에 응답하지 않았습니다
[music] 제어판 갱신 실패: Unknown interaction
```

미리 뽑기 **두 개가 동시에** 돌면서 서로를 굶겼다. 혼자면 12초쯤 걸리는 일이 60~73초가
되고, 제한시간을 40초로 늘려도 여전히 잘린다. 봇 전체가 멈춘 것처럼 보이기까지 했다 —
`Unknown interaction` 은 버튼에 3초 안에 답하지 못했다는 뜻이다.

둘을 나란히 20초씩 쓰느니 **하나를 12초에 끝내고 다음을 12초에** 끝내는 편이
첫 곡도 빠르고 전체도 빠르다. `serializeExtraction()` 이 `run()` 을 한 줄로 세운다.

⚠️ **병렬로 되돌리지 말 것.** 빨라 보이지만 실측은 정확히 반대다.
⚠️ **재생용 스트림(`createStream`)은 줄에 넣지 않는다.** 곡이 끝날 때까지 살아 있어서
   넣으면 미리 뽑기가 영영 차례를 못 받는다.

같은 이유로 **미리 뽑기는 `Playing` 이벤트에서 시작한다.** 예전에는 `playNext()` 끝에서
곧바로 시작했는데, 그때는 지금 곡도 아직 뽑히는 중이라 **지금 듣고 싶은 곡이 더 늦게**
나왔다(22~25초). 소리가 난 뒤에 시작하면 다투지 않는다.

#### 첫 시도 제한시간은 **서버 속도에 맞춰 정한다** — 고정값이면 매번 잘린다

소유자 서버 실측 (2026-09-02):

```
   yt-dlp 기동 5.7초 — 곡을 틀 때마다 이만큼이 깔립니다
[music] 첫 소리까지 22.4초 · 전체 추출 · …
[music] 첫 소리까지 25.0초 · 전체 추출 · …
```

**기동만 3.4~5.7초**다. 집 PC 는 1.6초였다 — 3배가 넘는다.
그런데 `run()` 의 첫 시도 제한시간은 **고정 20초**였다. 추출이 20초쯤 걸리는 서버에서는
첫 시도가 **매번** 잘리고, 1초 쉬었다가 다시 하므로 한 번 뽑는 데 40초가 넘는다.
그러면 미리 뽑기도 제때 못 끝내서 **다음 곡도 가장 느린 길(2단계)로 간다.**
느린 서버일수록 더 느려지는 악순환이다.

→ `timeoutsFor(기동시간, 가장느렸던성공, 잘려본제한시간)` 이 첫 제한시간을 정한다.
   `max(20초, 기동×5, 최장성공×2, 잘려본것×2)`, 상한 60초. 집 PC 는 그대로 20초다.

**세 번째 근거(잘려본 제한시간)가 실제로는 가장 확실하다.** 기동 시간으로 짐작만 하면
모자란다 — 같은 서버에서 기동이 **3.1초로도, 5.7초로도** 찍혔다. 3.1초로 찍힌 재시작에서는
짐작값이 20초에 머물러 **또 잘렸다**(10:09 로그). 반면 "20초에 잘려봤다" 는 사실은
짐작이 아니므로, 그다음부터 40초로 올린다.

⚠️ **성공 기록만으로 정하면 안 된다.** 제한시간이 짧아 한 번도 성공하지 못하면
   기록이 영영 안 쌓여 **스스로 빠져나오지 못한다.** 켤 때 잰 기동 시간이 바닥을 깐다.
⚠️ 단계를 셋으로 늘리지 말 것. 정말 멈춘 경우에 너무 오래 붙잡는다.

#### 느릴 때 어디가 느린지는 **로그가 답한다**

집 PC 에서 잰 값은 서버(1코어 ARM)에 그대로 적용되지 않는다. 그래서 봇이 직접 잰다.

```
   yt-dlp 기동 1.6초 — 곡을 틀 때마다 이만큼이 깔립니다     ← 켤 때 한 번
[music] 곡 정보 2.7초 (yt-dlp 추출) · 제목                  ← 대기열에 담을 때
[music] 첫 소리까지 1.4초 · 뽑아둔 주소 · 제목               ← 실제로 소리가 날 때
```

- **기동이 3초를 넘으면** 서버가 느린 것이지 유튜브 문제가 아니다. 봇이 경고까지 찍는다.
- **곡 정보만 느리면** 유튜브 응답이 느리거나 쿠키 파싱이 무거운 것이다.
- **첫 소리만 느리면** 어느 단계로 틀었는지가 같은 줄에 찍히니 3.1-2a 표와 대조한다.

추측하지 말고 이 세 줄을 먼저 볼 것.

### 3.1-4 재생 실패는 **조용히 넘어가면 안 된다**

실제 사고: 서버에서 곡이 대기열에 들어가자마자 사라지고 아무 소리도 안 났다. **오류 메시지조차
없었다.** 스트림 실패를 디스코드에 전혀 알리지 않았기 때문이다.

- `playedNothing()` — 3초 미만 재생됐는데 원곡이 그보다 길면 실패로 본다.
  **`intent === 'auto'` 일 때만** 따진다. 사용자가 `/다음` 을 누른 것은 실패가 아니다.
- 실패하면 `srcLevel` 을 하나 올려 **한 단계 아래 방식으로 자동 재시도**한다 (3.1-2a).
  이때 `resumeAt: this.positionSec()` 로 **듣던 위치를 넘긴다.** 안 넘기면 곡이 처음부터 다시 시작된다.
- ⚠️ **그 두 값은 그 시도에만 붙는다.** 곡을 대기열이나 기록으로 되돌릴 때는
  `rewind()` 를 통과시켜 떼어낸다. 안 떼면 (1) 🔁 반복·⏮️ 이전 이 곡 **중간**부터
  시작하고, (2) 한 번 실패한 곡이 그 실행 동안 **가장 느린 단계에 갇힌다** —
  미리 뽑아둔 주소가 생겨도 빠른 단계로 못 올라온다. 되돌리는 곳을 새로 만들면
  반드시 `rewind()` 를 거칠 것.
- 세 단계 모두 실패하면 **디스코드에 이유를 알린다.** ffmpeg stderr 를 `onError` 콜백으로
  받아 함께 보여준다.
- `.env.music` 의 `MUSIC_DIRECT_STREAM=false` 로 0단계를 끌 수 있다 (비상 스위치).

#### ⚠️ 오류를 **모르면서 아는 척하지 않는다** (`friendlyError`)

유튜브는 서로 다른 이유를 한마디로 뭉뚱그린다. 그걸 우리가 한 가지로 단정하면
소유자는 엉뚱한 곳을 뒤진다. 2026-09-02 에 실제로 겪은 두 건:

| 유튜브가 한 말 | 실제 이유 | 예전 안내 (틀림) |
|---|---|---|
| `Video unavailable` | 지역 차단 · 멤버십 전용 · 삭제 · 그 밖에 | "비공개이거나 삭제됨" |
| `Sign in to confirm …` | `your age` → 그 영상 하나 / `not a bot` → 서버 IP 차단 | 둘 다 "봇으로 판단해 차단" |

규칙 두 가지.

1. **갈라지는 것은 갈라서 안내한다.** 나이 쪽을 IP 차단보다 **먼저** 검사한다.
   순서를 바꾸면 연령 제한 영상 하나 때문에 멀쩡한 쿠키를 다시 뽑게 된다.
2. **모르겠으면 유튜브가 한 말을 그대로 보여준다** (`rawError()`).
   추측한 원인을 적으면 그 뒤로 아무도 진짜 원인을 못 찾는다.

#### ⚠️ 로그에는 **원인 한 줄**이 보여야 한다 (`user-error.js`)

소유자는 서버에서 `journalctl` 을 눈으로 훑는다. 오류마다 자바스크립트 스택이
20줄씩 붙으면 정작 읽어야 할 한국어 한 줄이 그 사이에 묻힌다. 실제로 `/재생` 이
실패했을 때 소유자가 원인 줄을 찾지 못했다.

- 원인이 분명한 오류는 `userError()` 로 만든다 → `logError()` 가 **메시지만** 찍는다
- 예상 못 한 오류(진짜 버그)는 **스택을 그대로 남긴다.** 고쳐야 하므로
- ⚠️ 경로 안전장치(`images/store.js`)·알 수 없는 설정 키(`settings.js`) 는
  **일부러 `new Error` 그대로 둔다.** 그건 코드가 잘못된 것이라 스택이 필요하다
- ⚠️ `unhandledRejection` 핸들러도 **`logError` 를 쓰지 않는다.** 처리되지 않은 오류는
  그 자체가 `catch` 가 빠진 버그다. 메시지만 찍으면 "연령 제한이 걸린 영상입니다" 처럼
  멀쩡해 보이는 한 줄이 되어, 정작 어디에 `catch` 가 빠졌는지 영영 못 찾는다

`getTracks` 의 `--print` 는 **한 줄에 `|::|` 구분자로** 받는다.
예전에는 항목당 한 줄씩 받았는데, 출력에 줄이 하나만 끼어도 순서가 밀려 엉뚱한 값이
재생 주소로 들어갔다 — 그러면 정확히 위 증상이 난다. 파싱한 주소는 `http` 로 시작하는지도 검증한다.

### 3.1-1 재생목록 — `list=` 가 붙었다고 다 목록이 아니다

재생목록은 `--flat-playlist --dump-single-json` 으로 **제목과 주소만** 가볍게 가져온다.
재생 주소는 각 곡을 틀 때 뽑는다(수십 곡의 주소를 미리 뽑으면 오히려 느리고, 어차피 만료된다).

#### ⚠️ 함정 1: 유튜브가 `list=RD…` 를 알아서 붙인다

자동재생이나 믹스로 넘어간 상태에서 링크를 복사하면 `watch?v=X&list=RDxxxx` 가 된다.
**공유한 사람은 그 곡 하나를 보낸 것**이지 목록을 보낸 게 아니다.
이걸 목록으로 처리하면 노래 하나 공유했는데 수십 곡이 쏟아진다.

`playlistIdOf()` 가 걸러낸다.

| 접두사 | 뜻 | 처리 |
|---|---|---|
| `RD` | 믹스·라디오 (유튜브가 자동 생성) | **한 곡만** |
| `LL` `WL` | 좋아요 · 나중에 볼 동영상 (개인 목록) | **한 곡만** (어차피 못 가져온다) |
| `PL` | 사람이 만든 목록 | 전체 |
| `UU` | 채널 업로드 전체 | 전체 |
| `OLAK5uy_` | 앨범 (자동이지만 의도가 분명함) | 전체 |

#### ⚠️ 함정 2: 상한이 없으면 대기열이 감당이 안 된다

실측으로 **183곡짜리 목록**을 확인했다. 그대로 담으면 되돌리려고 전부 지워야 한다.
`MUSIC_PLAYLIST_MAX`(기본 50)로 앞에서부터 자른다.

**자를 때는 반드시 알려야 한다.** 조용히 자르면 "왜 뒷곡이 없지?" 가 된다.
`getTracks()` 가 배열에 `totalFound` 를 붙여 돌려주고, 명령어 응답과 붙여넣기 경로
양쪽에서 "전체 N곡 중 앞 M곡" 을 보여준다.
**붙여넣기 경로는 제어판만 뜨고 응답이 없으므로** 별도 메시지를 보낸다 — 빠뜨리기 쉽다.

### 3.2 오디오 경로: `yt-dlp → ffmpeg(libopus) → Ogg Opus → discord.js`

```
yt-dlp -o -            ffmpeg -c:a libopus -f opus       @discordjs/voice
(webm/m4a 등)   ──►    (Ogg Opus 48kHz 2ch)        ──►   StreamType.OggOpus
                                                          = 컨테이너만 벗김
```

- 디스코드 음성은 **48kHz 스테레오 Opus**만 받는다.
- 만약 `StreamType.Arbitrary`로 넘기면 discord.js가 내부적으로 PCM으로 풀었다가
  JS Opus 인코더(`opusscript`)로 다시 인코딩한다. `opusscript`는 순수 JS라 느리고,
  1코어짜리 무료 VPS에서는 음이 끊긴다.
- 우리가 미리 ffmpeg의 **네이티브 libopus**로 인코딩해두면 discord.js는 Ogg 컨테이너만
  벗겨서 그대로 보낸다. 재인코딩이 없다 = CPU가 거의 안 든다.
- `opusscript`는 이 경로에서 실제로 호출되지 않지만, 누군가 `StreamType.Arbitrary`로
  바꿨을 때 즉시 죽지 않도록 남겨둔 안전망이다.
- 검증됨: 3분 33초짜리 곡에서 8초 만에 2.5MB Opus 수신, ffmpeg이 `Audio: opus, 48000 Hz, stereo` 출력.

### 3.2-1 음량은 ffmpeg 으로 조절한다 — `inlineVolume` 금지

음악과 읽어주기의 음량을 **따로** 둔다 (`settings.js` 의 `volumePercent` / `volumeScale`).
0~200%, 기본 100%. 서버별로 저장된다.

**`createAudioResource(..., { inlineVolume: true })` 를 쓰면 안 된다.**
실시간 조절은 되지만 opus → PCM → opus 재인코딩이 필요하고, 이 프로젝트에 설치된
opus 인코더는 **순수 JS 인 `opusscript` 뿐이다**(`@discordjs/opus` 는 네이티브 빌드가 필요해
Windows/ARM 설치가 번거로워 넣지 않았다). 1/8 OCPU 서버에서는 소리가 끊긴다.
**3.2절의 "재인코딩 회피" 가 통째로 무너진다.**

대신 ffmpeg 의 `-af volume=` 을 쓴다. 그러면 반영 시점이 달라진다.

| | 반영 시점 | 이유 |
|---|---|---|
| 읽어주기 | **즉시** (다음 문장부터) | 발화마다 새 ffmpeg 프로세스가 뜬다 |
| 음악 | 누른 뒤 **1~2초쯤 지나** 바뀐다 (끊기지는 않는다) | 이미 흐르는 ffmpeg 출력은 못 바꿔서 다시 틀어야 한다 |

#### 순서가 전부다 — 준비 → 바꿔치기 → 그 다음에 옛것 끊기

처음에는 옛 소리를 **먼저 끊고** 새 스트림을 만들었다. 두 가지가 동시에 잘못됐다.

1. 준비되는 몇 초가 통째로 **침묵**이었다.
2. 새 스트림이 실패하면 이미 끊은 뒤라 재시도 경로를 타고 **곡이 처음부터** 다시 시작됐다.

`restartAtCurrentPosition()` 은 그래서 이렇게 한다.

```
새 ffmpeg 시작 → waitForAudio() 로 첫 소리가 나올 때까지 대기
   (그동안 옛 소리는 계속 난다)
→ 잘라둔 지점에 닿을 때까지 마저 기다림 → play() 로 바꿔치기 → 그제서야 옛것 kill
```

- **준비에 실패하면 아무 일도 없던 것이 된다.** 듣던 소리가 그대로 이어진다.
- 준비하는 동안에도 옛 소리가 흐르므로, **그만큼 앞을 내다보고 잘라야** 이어붙는다.
  → `resumeAt = positionSec() + LEAD`. `LEAD` 는 직접 수신 1.5초, yt-dlp 3.5초
  (yt-dlp 는 시작만 1초가 넘는다 — 실측). 짧으면 조금 되감기고, 길면 반영이 늦어질 뿐 **끊기지는 않는다.**
- 준비 중에 또 눌리면 앞의 것은 버린다(`restartGen` 세대 번호).

#### 함정: `playbackDuration` 만 보면 안 된다

이 값은 *지금 리소스*가 재생한 시간만 센다. 다시 틀면 새 리소스는 0 부터 세므로,
**두 번째 음량 조절부터 곡이 처음으로 돌아간다.** 실제로 겪은 버그다.
건너뛴 만큼을 `currentOffsetSec` 에 남기고
`positionSec() = currentOffsetSec + playbackDuration/1000` 으로 구한다.

`playedNothing()`(재생 실패 판정)은 **반대로** `playbackDuration` 만 봐야 한다 —
이번 시도가 소리를 냈는지를 묻는 것이기 때문이다. 대신 "곡이 짧아서 3초만 난 것" 과
구분하기 위해 원곡 전체 길이가 아니라 **남은 길이**(`trackLen - currentOffsetSec`)와 비교한다.
전체 길이와 비교하면 곡 끝 무렵에 음량을 바꿨을 때 멀쩡히 다 들은 곡을 "재생 실패" 로 잘못 알린다.

**직접수신 실패 재시도에도 `resumeAt` 을 넘긴다.** 안 넘기면 재시도가 곡을 처음부터 다시 튼다.

**버튼 연타는 모아서 한 번만 반영한다**(500ms). `+10` 을 세 번 누르면 세 번 다시 튼다.
곡이 바뀌면 대기 중이던 반영은 취소한다(`playNext`), 타이머가 늦게 터져 엉뚱한 곡을 다시 트는 것을 막는다.

`-ss` 는 파이프 입력(yt-dlp stdout)에서도 동작한다 — 실측으로 확인했다(오차 약 1초).
**`AudioPlayer.play()` 를 재생 중에 부르면 Idle 이벤트가 나지 않으므로**(소스 확인함)
`onTrackEnd()` 가 "곡이 끝났다" 고 오해하지 않는다 — 이 성질에 의존하고 있으니
discord.js 를 올릴 때 확인할 것.

타이머 알람도 읽어주기 음량을 따른다 (같은 TTS 경로이므로).

### 3.3 서버당 음성 커넥션은 **하나**뿐이다 — 음악과 TTS의 공존 방식

**제약**: 디스코드 봇은 한 서버(길드)에서 음성채널에 동시에 하나만 접속할 수 있다.

> ⚠️ **2.1 절대로 봇을 나눈 뒤로, 이 절의 "끼어들기" 는 실제로는 거의 일어나지 않는다.**
> 망고와 노래하는 망고는 애플리케이션이 달라 각자 커넥션을 가지므로,
> 음악 위에 읽어주기가 **겹쳐서** 난다. 그게 나눈 이유 중 하나다.
>
> 그래도 이 코드는 **지우면 안 된다.** 같은 봇 안에서 읽어주기와 타이머 알람이
> 겹칠 때 여전히 이 경로를 탄다 (알람이 울리는 중에 채팅이 오는 경우).

**해결**: `GuildAudio` 하나가 커넥션 1개 + 플레이어 2개(`musicPlayer`, `ttsPlayer`)를 들고,
구독을 갈아끼우며 TTS가 음악에 **끼어든다**.

```
음악 재생 중  ──TTS 요청──►  musicPlayer.pause(true)
                             subscription.unsubscribe()
                             connection.subscribe(ttsPlayer)
                             ttsPlayer.play(...)  → Playing → Idle 대기
                             connection.subscribe(musicPlayer)
                             musicPlayer.unpause()   ──►  음악 이어서 재생
```

**⚠️ 반드시 지켜야 하는 것 — `connection.subscribe()`는 이전 구독을 끊어주지 않는다.**
`@discordjs/voice` 0.19.2 소스(`VoiceConnection.subscribe`)를 직접 확인했다.
이 메서드는 `state.subscription`만 덮어쓸 뿐, 이전 `PlayerSubscription`을 해제하지 않는다.
두 플레이어가 동시에 구독된 채로 남으면 **둘 다 오디오 패킷을 보내서 소리가 깨진다.**
그래서 `GuildAudio.subscribeTo()`가 항상 `this.subscription?.unsubscribe()`를 먼저 호출한다.
**이 순서를 바꾸거나 생략하지 말 것.**

**대안으로 검토했다가 버린 것들**
- TTS를 음악 큐 뒤에 붙이기 → 노래 끝날 때까지 안 읽어줘서 쓸모없음
- 음악을 끊고 TTS만 → 노래가 사라져서 화남
- 두 소리를 ffmpeg으로 실시간 믹싱 → 구현 복잡도가 v1에 안 맞음

### 3.4 TTS 엔진: `msedge-tts`

#### ⚠️ 낱자(자음·모음)만 있는 글은 **소리가 안 난다** — 실측

Edge TTS 는 `ㅋㅋ` `ㅎㅎ` `ㅇㅇ` 처럼 낱자만 이어진 글에 **0바이트를 돌려준다.**
에러도 없다. 실측:

| 입력 | 결과 |
|---|---|
| `ㅋ` | 정상 (21312바이트) |
| `ㅋㅋ` `ㅋㅋㅋ` `ㅎㅎ` `ㅠㅠ` `ㄱㄱ` | **0바이트** |
| `ㅋㅋㅋ 웃김` | 정상 — 다른 글자와 섞이면 괜찮다 |

`cleanText()` 가 반복을 3개로 줄이는 것까지는 맞았는데, 그 결과가 무음이었다.
그래서 `speakJamo()` 가 **낱자를 읽을 수 있는 글자로 바꾼다.** 순서가 중요하다.

1. **뜻이 있는 축약어** (`ABBREV`) — 낱말 전체가 낱자일 때만. `ㄷㄷ` → 덜덜
   소리대로 읽으면 "드드" 가 되므로 반드시 먼저 본다.
2. **소리를 흉내낸 반복** (`JAMO_SOUND`) — 글 중간이어도. `안녕 ㅋㅋ` → 안녕 크크
3. **나머지는 소리 나는 대로** (`JAMO_SOUNDOUT`) — 자음에 ㅡ, 모음에 ㅇ. `ㅅㅂ` → 스브

**3단계를 지우지 말 것.** 이게 있어서 *어떤* 낱자가 와도 무음이 되지 않는다.
`verify.mjs` 가 두 낱자 조합 961가지를 전부 돌려 낱자가 남지 않는지 확인한다.

그리고 무음을 그냥 재생하면 `entersState(Playing, 15_000)` 이 **15초를 기다리다 실패**하고,
`ttsChain` 이 직렬이라 그동안 뒤에 온 문장이 전부 밀린다.
그래서 `speakNow()` 가 `waitForAudio()` 로 미리 확인하고 건너뛴다.
이 안전장치는 낱자 말고도 **한국어 전용 목소리 + 일본어**(아래) 같은 경우를 같이 막아준다.

- 무료, 가입/API키 불필요, 한국어 음질이 좋다.
- **Edge TTS 가 실제로 주는 한국어 목소리는 3개뿐이다** (2026-08-31 `getVoices()` 실측):
  `ko-KR-HyunsuMultilingualNeural`, `ko-KR-SunHiNeural`, `ko-KR-InJoonNeural`.
  개발 중 Azure 전체 카탈로그에서 가져온 이름 6개를 선택지에 넣었다가, 실재하지 않아
  런타임에 실패할 뻔했다. `data.toJSON()` 은 이름이 문법적으로 유효하면 통과시키므로 못 잡는다.
  그래서 `verify.mjs` 가 **선택지를 `listVoices()` 결과와 대조한다.** 목소리를 건드리면 이 검사를 꼭 돌릴 것.
- **기본 목소리는 `ko-KR-HyunsuMultilingualNeural`(다국어)이다.** 실측 비교:

  | 입력 | SunHi (한국어 전용) | Hyunsu (다국어) |
  |---|---|---|
  | 영어 | 5.2초 (한국어 발음으로 늘어짐) | 3.6초 (자연스러움) |
  | 일본어 | **0 바이트 — 무음, 에러도 없음** | 2.7초 |
  | 중국어 | 2.6초 (한자 음독) | 2.5초 |
  | 한영 혼합 | 4.0초 | 4.0초 |

  Hyunsu 가 지는 입력이 없고 SunHi 에는 조용한 실패 모드가 있다. 언어 감지 로직을 넣는 것보다
  "전부 처리하는 목소리를 기본으로" 두는 쪽이 코드도 적고 오탐 경로도 없다.
- **TTS가 느린 원인은 합성이 아니라 "식은 연결"이다.** 실측:
  연결을 새로 맺은 뒤 첫 발화 **약 950~1900ms**, 따뜻한 연결에서는 **43~81ms**.
  마이크로소프트가 유휴 웹소켓을 끊기 때문에 채팅이 띄엄띄엄 오면 매번 첫 발화 비용을 문다.
  그래서 `synth.js` 가 아주 짧은 문장(`'음'`)을 45초마다 합성해 **연결을 데워둔다**.
  - `prewarm()` 은 봇 시작 시(TTS 설정된 서버가 있으면)와 **누가 음성채널에 들어올 때** 호출한다.
    음성채널 입장은 곧 읽어주기를 쓸 신호이므로 예열 타이밍이 정확하다.
  - 30분간 아무도 안 쓰면 예열을 멈춘다(쓸데없는 통신 방지). 다음 사용 시 다시 시작된다.
  - mp3→Ogg Opus 변환(ffmpeg)은 약 170ms 로 병목이 아니다. msedge-tts 의 webm/opus 출력은
    24kHz 모노라 디스코드(48kHz)에 그대로 못 쓴다 — 변환을 없애려 하지 말 것.
- **목소리는 14종이다** (`src/tts/voices.js`). Edge TTS 가 `ko-KR` 로 분류하는 목소리는 3개뿐이지만,
  **다국어(Multilingual) 목소리는 로케일과 무관하게 한국어를 읽는다.** 12종을 한국어 문장으로
  실측해 전부 정상 발화를 확인했다. 목록을 늘릴 때는 `npm run verify` 가
  `listVoices()` 와 대조하므로 실재하지 않는 이름이 들어가지 않는다.
- **목소리는 사람마다 다르게 쓸 수 있다.** 우선순위는 `voiceFor(guildId, userId)` 에 있다:
  **내 목소리(`/목소리`) > `.env` 의 `TTS_VOICE`**. 개인 설정은 `data/settings.json` 에 저장되어
  재시작해도 유지된다.

  한때 `/목소리`(서버 기본) 와 `/내목소리`(개인) 둘로 나뉘어 있었는데, **둘 다 있으면 어느 쪽을
  써야 할지 헷갈려서** 개인 설정만 남기고 이름을 `/목소리` 로 합쳤다(소유자 요청).
  서버 기본값은 `.env` 로 충분하다 — 자주 바꿀 값이 아니다.
  `settings.json` 에 예전 `voice` 키가 남아 있어도 이제 읽지 않는다.
- Google Translate TTS는 200자 제한 + 비공식 + 레이트리밋이 빡세다.
- 인터넷이 필요하다(마이크로소프트 서버와 웹소켓). **오프라인에서는 TTS가 동작하지 않는다.**
- 웹소켓 연결을 `synth.js`에서 캐시하고, 실패하면 한 번 새로 만들어 재시도한다.
  마이크로소프트가 유휴 연결을 끊기 때문에 이 재시도가 없으면 조용히 실패한다.
- 검증됨: "안녕하세요…" → 57KB mp3 생성 → ffmpeg으로 4.78초 Ogg Opus 변환 성공.

### 3.4-3 이모지는 읽지 않는다

`cleanText()` 가 **디스코드 커스텀 이모지와 유니코드 이모지를 모두 지웁니다.**

- 예전에는 `<:kekw:123>` 을 이름("kekw")으로 바꿔 읽었는데, 알아들을 수 없고 시끄러웠다.
- 유니코드 이모지는 TTS 엔진이 설명을 읽거나 이상하게 발음한다.
- 지우고 나서 **읽을 게 아무것도 안 남으면** `'이모지를 보냈어요.'` 로 알린다.
  그냥 무시하면 "봇이 내 메시지를 씹었나?" 싶어진다.

`EMOJI_RE` 는 피부톤(`👍🏽`), ZWJ 조합(`👨‍👩‍👧‍👦`), 국기(`🇰🇷`), 키캡(`1️⃣`),
변형선택자(`❤️`)까지 잡는다.

⚠️ **`ㅋㅋㅋ` 같은 한글 자음은 이모지가 아니다.** 정규식을 넓히다가 이걸 지우면
한국어 채팅에서 치명적이다. `verify.mjs` 에 회귀 검사가 있다.

#### 읽는 속도도 **사람마다** (`/목소리 속도:130`)

목소리와 같은 자리에 뒀다. 읽는 사람마다 취향이 다르다 — 누구는 빠르게 듣고 싶고,
누구는 또박또박 듣고 싶다. 100 = 원래 속도, 50~200.

⚠️ **명령어를 새로 만들지 않았다.** `/목소리` 에 칸을 하나 더했다 (3.6-6).
   `/목소리` 는 이미 "내 읽어주기 설정" 을 다루는 명령어다.
   둘 다 비우고 실행하면 **목소리·속도를 함께** 기본값으로 되돌린다.

⚠️ **속도는 연결(engine)이 아니라 호출에 붙인다.** `toStream(text, { rate })` 로 넘긴다.
   연결에 묶으면 사람이 바뀔 때마다 웹소켓을 다시 맺어야 하고,
   그러면 **예열(1.7초)이 매번 날아간다** (`synth.js` 맨 위 설명).

⚠️ 퍼센트를 **배수로 바꿔** 넘긴다 (130% → `1.3`). 그대로 넘기면 130배가 된다.
   100% 면 아무것도 넘기지 않아 기본 동작을 그대로 둔다.

```
{ rate: 1.3 } → <prosody rate="1.3">   (실제 SSML 확인)
```

#### 반복은 몇 개까지 읽나 — `TTS_MAX_REPEAT` (기본 6)

`ㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋ` 를 그대로 읽으면 그 소리만 한참 난다. 그래서 자른다.
다만 **웃음의 길이도 표현**이라 너무 짧게 자르면 심심하다.
처음에는 3개였는데 소유자가 **6개로** 늘렸다(2026-09-02).

```
ㅋ×3  → 크크크          (상한보다 짧으면 그대로)
ㅋ×6  → 크크크크크크     (상한까지는 다 읽는다)
ㅋ×12 → 크크크크크크     (넘으면 자른다)
대박!!!!!!!!!! → 대박!!!!!!   (글자 종류를 가리지 않는다)
```

⚠️ 자르기가 **낱자 바꾸기(speakJamo)보다 먼저**여야 한다. 순서를 바꾸면
   `ㅋ×12` 가 `크×12` 가 된 뒤에 잘려서 결과는 같지만, 헛일을 두 배로 한다.

#### 축약어는 **서버마다 등록**할 수 있다 (`/축약어`)

기본 표(`ABBREV`)는 코드에 있다. 그런데 친구들끼리 쓰는 말은 서버마다 다르고,
그때마다 코드를 고쳐 배포할 수는 없다. 그래서 `settings.js` 에 서버별로 저장한다.

| | |
|---|---|
| 등록 | `/축약어 단어:ㄱㅊ 읽기:괜찮아` |
| 목록·지우기 | `/축약어` (인자 없이) → 목록 + 지우기 드롭다운 |
| 우선순위 | **등록한 것이 기본 표를 이긴다** (기본값이 마음에 안 들면 덮어쓴다) |
| 상한 | 서버당 100개, 단어 20자, 읽기 50자 |

⚠️ **명령어를 하나만 만들었다** (3.6-6). `/축약어추가`·`/축약어삭제` 를 따로 두면
명령어가 또 불어난다. 인자 없이 실행하면 목록이 나오고, 지우기는 드롭다운이다.

⚠️ **낱말 전체가 같을 때만 바꾼다.** 글 중간의 일부만 맞아도 바꾸면
   멀쩡한 글자를 망가뜨린다 (`ㄱㅊㅊ` 이 `괜찮아ㅊ` 이 되면 안 된다).
   기본 표는 여전히 **낱자만인 낱말**에만 적용하지만, 등록한 것은 낱자가 아니어도 된다 —
   "갓생" 도 "wtf" 도 등록할 수 있어야 한다.

### 3.4-1 음성채널은 텍스트 채널이기도 하다

디스코드 음성채널에는 **자체 채팅창**이 있다. discord.js 에서 음성채널은
`isTextBased()` 와 `isVoiceBased()` 를 **둘 다 true** 로 돌려준다
(`BaseGuildVoiceChannel` 에 `TextBasedChannel` 이 적용되어 있어 `messages` 프로퍼티를 가진다).
그 채팅에 올라온 메시지의 `message.channel.type` 은 `GuildVoice` 다.

- 그래서 채널 종류를 검사할 때 **`ChannelType` 목록을 하드코딩하지 말고**
  `isTextBased()` / `isVoiceBased()` 를 쓴다.
  (초기 구현은 `[GuildText, GuildAnnouncement]` 목록으로 검사해서, 사용자가 음성채널 안의 채팅을
  읽어주기 채팅방으로 고를 수 없었다.)
- `resolveTtsVoiceChannel()` 은 **글이 올라온 곳이 음성채널이면 그 채널에서 읽어준다.**
  음성채널 채팅에 쓴 글을 다른 채널에서 읽는 건 말이 안 되기 때문이다.
  우선순위: 명시 설정 > 글이 올라온 음성채널 > 글쓴이가 들어가 있는 음성채널.

### 3.4-2 TTS 타이머: 재시작을 견뎌야 한다

`src/timer/index.js`. 시간이 되면 TTS로 음성채널에 알리고 채팅으로 멘션한다.

**`data/timers.json` 에 저장한다.** 진행 중인 타이머를 메모리에만 두면
배포로 재시작할 때마다 사라진다 — 60분 타이머를 걸어놓고 배포하면 그냥 없어진다.
`initTimers()` 가 시작 시 되살리고, **이미 지난 것은 3초 뒤 발동시키며
"몇 분 늦었다"고 밝힌다**(조용히 삼키면 사용자는 알람이 안 왔다고만 생각한다).

- `setTimeout` 은 약 24.8일이 한계다. 최대 24시간으로 제한해 그 근처에 가지 않는다.
- 등록 단어(`words`)는 서버별로 저장한다. `/타이머` 의 **자동완성**이
  프리셋과 등록 단어를 함께 보여주므로 사용자가 외울 것이 없다.
  자동완성은 `interaction.isAutocomplete()` 로 `index.js` 에서 분기한다.
- `/타이머` 는 **등록 단어를 시간 해석보다 먼저** 확인한다.
  "라면" 처럼 시간으로 읽히지 않는 값을 받으려면 이 순서여야 한다.
- `parseMinutes()` 는 `15` `15분` `1시간 30분` `90m` `1h30m` `30초` 를 받는다.
  `0` 과 음수는 거부한다(즉시 발동하는 타이머는 사용자 의도가 아니다).
- **알람은 멘션으로 알린다.** 음악 제어판은 `SuppressNotifications` 로 조용히 갱신하지만,
  알람은 알림이 울리지 않으면 존재 이유가 없다. 이 차이는 의도적이다.
- 버튼 `customId` 는 `t:` 로 시작한다 (음악은 `m:`). `index.js` 가 앞머리로 분기한다.

### 3.5 이미지 일괄 다운로드: ZIP이 아니라 **웹 갤러리**

**사용자의 원래 요구**: "여러 장 선택하고 다운로드 버튼을 누르면 한 장씩 다 내려받기.
디스코드는 개별적으로 눌러야 해서 귀찮다." — 즉 **ZIP은 명시적으로 원하지 않는다.**

- 디스코드 봇 업로드 한도: 부스트 없음 10MB / 레벨2 25MB / 레벨3 100MB.
  사진 몇 장이면 넘는다. ZIP 첨부는 애초에 성립하지 않는다.
- 디스코드 UI에는 "여러 장 선택 → 개별 저장" 기능이 없다. 봇으로도 만들 수 없다.
- 브라우저는 같은 출처의 `<a download>`를 연달아 클릭하면 파일을 각각 저장한다.
  그래서 Express로 작은 갤러리를 띄우고, 체크박스 다중선택 + 순차 클릭으로 구현했다.
  크롬은 최초 1회 "여러 파일을 다운로드하시겠습니까?"를 묻고, 허용하면 이후 조용하다.
- 클릭 간격 **350ms**는 임의값이 아니다. 너무 빠르면 브라우저가 일부 요청을 버린다.
  줄이려면 실제로 20장 이상으로 테스트해볼 것.

### 3.6 폴더 결정 규칙 (외울 명령어를 줄이는 쪽으로)

`store.js` `resolveFolder(channel, channelId)` — 위에서부터 먼저 맞는 것을 쓴다.

1. 메시지가 **스레드** 안에 있으면 → **스레드 이름**
2. 그 채널에 `/폴더 <이름>`으로 지정한 값이 있으면 → 그 이름 (`_folders.json`에 저장)
3. **채널 이름** (기본값)
4. 채널 이름을 알 수 없을 때만 → 오늘 날짜 `YYYY-MM-DD`

**감시 대상은 기본이 "전부"다.** `settings.js` 의 `imageChannelAllowed()` 가 판단하는데,
`imageChannelIds` 가 비어 있으면 **봇이 볼 수 있는 모든 채널**을 저장한다(소유자 요구).
목록이 있으면 그 채널들만. 즉 이 설정은 on/off 가 아니라 **필터**다.
실질적인 경계는 디스코드 권한이다 — 봇이 못 보는 채널은 메시지 자체가 오지 않는다.
`imagesEnabled()` 는 항상 `true` 를 돌려준다(하위호환용으로 남겨둠).

목록이 **둘**이다. 소유자 요구는 "기본은 전부 받되 몇 군데만 빼기" 였다.

| 설정 | 뜻 |
|---|---|
| `imageChannelIds` (이미지 채널) | 비어 있으면 전부, 지정하면 그 채널들만 |
| `imageExcludeChannelIds` (이미지 제외 채널) | 여기 있으면 **무조건 안 받는다** |

⚠️ **제외를 먼저 걸러야 한다.** 나중에 보면, 지정 목록이 비어 있을 때 이미 `true` 로
나가버려서 제외가 통째로 무시된다. 바로 그 조합("전부 받되 몇 개만 빼기")이
소유자가 원한 것이므로, 순서를 바꾸면 기능이 아예 동작하지 않는다.
스레드는 부모 채널을 따른다 — 제외한 채널의 스레드도 제외다.

⚠️ **이걸 위해 명령어를 새로 만들지 않았다.** `settings.js` 의 `KEYS` 에 항목을 넣으면
`/채널설정` 의 선택지와 해제 버튼이 **따라온다**(3.8). 채널 설정의 제자리는 거기다.

3번이 기본값인 이유: 사용자가 아무 설정도 하지 않아도 `#여행사진` 채널의 사진이
`여행사진` 폴더로 가는 게 가장 예측 가능하고, 외울 명령어가 0개다.
(초기 구현은 날짜가 기본값이었는데, 채널이 여러 개면 날짜 폴더 하나에 다 섞여서 쓸모없었다.)

⚠️ **채널 이름을 바꾸면 그때부터 새 폴더가 생긴다.** 이전 사진은 옛 이름 폴더에 남는다.
요구사항상 불가피하며, 데이터가 사라지는 것은 아니다. 갤러리의 "옮기기"로 합칠 수 있다.

⚠️ **폴더 이름 공간은 서버 전체에서 하나다.** 저장 경로에 길드 구분이 없으므로,
서로 다른 서버의 `#사진` 채널 둘은 같은 `data/images/사진` 에 쌓인다.
파일이 덮어써지지는 않지만(중복 이름에 `-1` 이 붙는다) 갤러리에서 섞여 보인다.

**의도적으로 이 상태를 유지하고 있다.** 경로에 길드 차원을 넣으려면
`folderPath` / `filePath` / `listFolders` / `listFiles` / `moveFiles` / `deleteFiles` /
`_meta.json` 키 형식 / 웹 라우트 3개 / 갤러리 템플릿 2개 / `/갤러리` URL 조립을 모두 고쳐야 하고,
그 중 앞의 두 개가 **불변조건 2(경로 탈출 방어)** 의 핵심이다. 이득 대비 위험이 크다.

**우회 수단이 이미 있다**: `/폴더 <이름>` 은 채널별로 저장되고 채널 이름 기본값을 이긴다.
한쪽 서버에서 `/폴더 B서버사진` 을 실행하면 영구히 분리된다.
사용자가 실제로 이 충돌에 불편을 겪는다고 말하기 전에는 재구조화하지 말 것.
할 때는 **명령어 등록 변경 같은 다른 작업과 묶지 말고 단독으로** 하고, 경로 검사를 다시 검증할 것.

`explainFolder()` 는 `/폴더확인` 이 "왜 이 폴더인지"를 사람 말로 보여주기 위한 짝 함수다.
규칙을 바꾸면 두 함수를 같이 고쳐야 한다.

### 3.6-1 음악 제어판: 버튼 + 메시지 재사용

`music/panel.js` 가 임베드 + 버튼 + 드롭다운을 만든다. `customId` 는 `m:` 로 시작하고
`index.js` 의 `InteractionCreate` 에서 `isButton() || isStringSelectMenu()` 로 분기한다.

**왜 슬래시 명령어가 아니라 버튼인가**: 이 봇은 비개발자 친구들이 함께 쓴다.
`/순서이동 3 1` 을 외우게 하면 아무도 안 쓴다. 소유자가 명시적으로 요구한 제약이다.

**제어판은 항상 채팅방의 맨 아래에 있어야 한다** (소유자 요구: "곡을 추가할 때마다
밀려올라가서 불편하다"). `showPanel()` 의 규칙:

1. 제어판이 **이미 맨 아래**면 → 그 메시지를 **수정**만 한다 (채팅방이 안 더러워짐)
2. 다른 메시지에 **밀려 올라갔으면** → 옛 제어판을 **지우고 맨 아래에 다시** 띄운다
3. 새로 보낼 때는 **`MessageFlags.SuppressNotifications`** 를 붙인다 → 푸시 알림이 울리지 않는다
   (소유자 요구: "알림은 따로 안 울렸으면 좋겠다")

맨 아래인지는 `channel.messages.fetch({limit:1})` 로 확인한다. API 호출 1회가 늘지만
"밀려났는데 그대로 수정" 하는 것보다 낫다.

**동시 호출을 `audio.panelChain` 으로 직렬화한다.** 곡 추가와 곡 전환이 겹치면
제어판이 두 개 생긴다.

그리고 **유튜브 링크 메시지는 성공 시 삭제한다**(`handleMusicMessage`).
이게 쌓이는 것이 제어판이 밀려나는 주된 원인이다.
**`Manage Messages` 권한이 필요하다** — 없으면 삭제가 실패하므로 ✅ 반응으로 대체하고
콘솔에 경고를 남긴다(재생 자체는 정상 동작). 초대 권한 숫자가 3263552 → 3271744 로 바뀌었다.
실패했거나 일부만 성공한 메시지는 **지우지 않는다** — 무엇을 보냈는지 봐야 하므로.

**드롭다운은 25개 제한**이 있다(디스코드 규격). 대기열이 더 길면 앞 25곡만 담긴다.

### 3.6-1a 제어판은 **살아남으면 안 된다** — 재시작·곡 종료 시 정리

실제로 겪은 버그 두 개다. 둘 다 "제어판이 거짓말을 한다" 는 같은 증상이었다.

**(1) 재시작하면 옛 제어판이 채팅방에 영원히 남았다.**
메시지 ID 를 `audio.panelMessage` (메모리) 에만 들고 있었다. 봇이 꺼지면 참조를 잃고,
다시 켜면 새 제어판을 하나 더 만든다. **재배포할 때마다 하나씩 쌓인다.**
갤러리 버튼도 같은 문제였다(`panels` Map).

→ `panel-registry.js` 가 ID 를 `data/panels.json` 에 적어둔다. 시작할 때:

| | 처리 | 왜 |
|---|---|---|
| 음악 제어판 | **지운다** | 재시작하면 음악이 이어지지 않으므로 "지금 재생 중" 은 거짓말이다 |
| 갤러리 버튼 | **되찾아 그대로 쓴다** (`adoptGalleryPanel`) | 링크 버튼이라 재시작 후에도 멀쩡히 동작한다. 지우면 다음 업로드까지 바로가기가 사라진다 |

기억에 **없는** 옛것(이 기능 이전에 남은 것, 저장 파일이 날아간 경우)은
`sweepOrphanPanels()` 가 채널을 훑어 지운다. **봇이 자기가 쓴 메시지 중 제어판인 것만**
지운다 — `msg.author.id !== botId` 면 절대 손대지 않는다. 남의 대화를 지우면 복구가 안 된다.

`guild.members.me` 는 캐시에 없을 수 있다. 그러면 권한 검사가 전부 실패해서
**청소가 조용히 아무것도 안 한다.** 없으면 `fetchMe()` 로 받아온다.

**(2) 곡이 다 끝났는데도 제어판이 "지금 재생 중" 이었다.**
`showPanel()` 은 **곡이 시작될 때만** 불렸다. 대기열이 비면 `scheduleLeave()` 만 하고
제어판은 마지막 곡 화면 그대로 굳었다.

→ 재생이 끝나는 두 지점(`playNext()` 의 대기열 없음, `onTrackEnd()` 의 다음 곡 없음)에서
`refreshPanel()` 을 부른다. 봇이 음성채널을 떠날 때(`destroy()`)는 아예 지운다.

### 3.6-1c **지정된 음악 채팅방**의 제어판은 반대로 — 절대 지우지 않는다

> "음악 채팅채널로 지정한곳에는 제어판이 항상 보였으면 좋겠어. 봇이 음성채널에 없거나
> 재생중인 곡이 없을때도, 재생중인 곡이 없다고 표시되겠지만 제어판 자체는 사라지지않고"

3.6-1a 와 어긋나 보이지만 아니다. **거짓말을 없애는 것**이 원칙이고, 지우는 건 그 수단
중 하나였을 뿐이다. "재생 중인 곡이 없습니다" 는 거짓말이 아니므로 남겨도 된다.

경계는 딱 하나다 — `isMusicHome(guildId, channelId)`, 즉 `/채널설정` 의 **음악 채팅방**인가.

| | 지정된 음악 채팅방 | 그 밖의 채널 |
|---|---|---|
| 재시작 | **남기고 "비었다" 로 고쳐 쓴다** (`adoptMusicPanel`) | 지운다 |
| 봇이 나갈 때 (`destroy`) | **남기고 고쳐 쓴다** | 지운다 |
| 봇 종료 | **남기고 고쳐 쓴다** | 지운다 |
| 없을 때 | **새로 띄운다** (`ensureHomePanel`) | 안 띄운다 |

⚠️ **지정하지 않은 채널까지 영구 제어판을 두지 말 것.** 아무 채팅방에나 남으면 어지럽다.

**놓치기 쉬운 곳 네 군데** (전부 verify 에 검사가 있다)

1. `sweepOrphanPanels()` 는 `"m:` 가 들어간 봇 메시지를 **전부** 지웠다. 갤러리와 똑같이
   `keepMusic = rememberedId(MUSIC, ch)` 예외가 필요하다. 없으면 **재시작마다 사라진다.**
   (이 훑기는 되찾기 **뒤에** 도므로, 그때 기억에 남은 음악 제어판은 남길 그것 하나뿐이다)
2. **되찾기와 고쳐쓰기는 한 번에 해야 한다.** 떼어놓으면 그 사이에 "지금 재생 중" 이 남는다.
   `adoptMusicPanel()` 이 둘을 같이 한다.
3. `destroy()` 는 맨 앞에서 `destroyed = true` 를 켠다. 그래서 거기서 `refreshPanel()` 을
   부르면 **조용히 아무 일도 안 한다.** 메시지를 직접 고쳐야 한다.
4. 재시작 후 첫 재생 때 `audio.panelMessage` 는 비어 있다. 기억해둔 것을 되찾지 않으면
   **제어판이 둘이 된다.** `showPanelNow()` 가 먼저 `rememberedId(MUSIC, …)` 를 찾아본다.

버튼도 손봐야 한다. 비어 있는 제어판에서도 **🔉 🔊 · 🔄 · 🕐 은 눌린다.**
`handleMusicComponent()` 가 `audio` 없이도 이 셋을 처리한다 — 음량은 서버 설정이라
재생 중이 아니어도 바꿔둘 수 있다. 나머지 버튼은 이미 `setDisabled` 라 눌리지 않는다.

**`refreshPanel()` 은 `showPanel()` 과 달리 메시지를 옮기지 않는다.** 그 자리에서 `edit` 만 한다.
곡이 끝나는 순간은 버튼 응답(`interaction.update`)과 겹칠 수 있는데,
여기서 메시지를 지워버리면 그 응답이 **"없는 메시지" 오류**를 낸다.
이 구분을 없애지 말 것.
**(3) 우클릭 → "연결 끊기" 로 내보내는 경우가 `/나가기` 보다 흔하다** (소유자 확인).
이 경로는 **아무 명령어도 거치지 않는다.** `VoiceConnectionStatus.Disconnected` 만 온다.

- 채널 **이동**이나 순간적 끊김이면 스스로 돌아온다 → 5초간 `Signalling`/`Connecting` 을 기다린다.
- 돌아오는 "중" 인 것만으로는 부족하다. **`Ready` 까지 확인한다** —
  여기서 멈추면 죽은 커넥션을 붙잡은 채 제어판이 계속 "재생 중" 이라고 한다.
- 끝내 못 돌아오면 → 안내를 한 줄 보내고 `destroy()` (제어판도 지워진다).

안내를 빼면 안 된다. 제어판만 조용히 사라져서 **"왜 멈췄지?"** 가 된다.
재생 중이던 게 있을 때만 보낸다(가만히 있던 봇을 내보낸 것까지 알릴 필요는 없다).
### 3.6-1b 지난 재생 목록 (`music/history.js`)

"며칠 전에 듣던 그 노래" 를 링크 없이 다시 트는 길이다.

`GuildAudio.history` 와 **다른 것이다.** 그건 ⏮️ 이전 곡용이고 메모리에만 있어
재시작하면 사라진다. 이쪽은 `data/history.json` 에 서버별 60곡까지 남는다.

**기록은 `AudioPlayerStatus.Playing` 에서만 남긴다.** 대기열에 넣을 때 남기면
재생에 실패한 곡(유튜브 차단 등)까지 쌓이는데, 다시 골라도 또 실패하므로 값어치가 없다.

**명령어를 새로 만들지 않았다**(3.6-6). 같은 화면이 두 곳에서 열린다.

- 제어판의 🕐 **지난 곡** 버튼
- `/재생` 을 **검색어 없이** 실행했을 때

드롭다운으로 **여러 곡을 한 번에** 고를 수 있다(`setMaxValues`). 고르면 대기열 맨 뒤로 담긴다.

주의할 점:

- 드롭다운 **값은 100자 제한**이다. 넘는 주소는 다시 고를 수 없으므로 `record()` 에서 아예 걸러낸다.
- 드롭다운은 **25개 제한**이므로 60곡을 저장해도 최근 25곡만 보인다.
- `m:hist*` 는 `index.js` 에서 **`handleMusicComponent` 보다 먼저** 가로챈다.
  그쪽은 재생 중이 아니면 바로 되돌려보내는데, 지난 곡 보기는 **재생 중이 아닐 때가 오히려 정상**이다.
- `record()` 는 **쓰기를 기다리지 않는다**(재생 중에 멈추면 안 되므로).
  "적고 → 바로 다시 읽기" 를 해야 하면 `flushHistory()` 를 거쳐라.
  **시간을 재서 기다리면(`setTimeout`) 느린 서버에서 어긋난다** — verify 가 실제로 실패했다.
  종료 시에도 `flushHistory()` 를 기다린다. 안 그러면 방금 튼 곡이 목록에 안 남는다.
- 담는 작업은 곡마다 유튜브 추출이 있어 수 초씩 걸린다. `deferUpdate()` 로 먼저 응답을 잡고,
  붙여넣기로 들어온 곡들과 순서가 엉키지 않게 `serialize()` 줄에 세운다.
### 3.6-2 대기열 이동의 "의도"를 추측하지 말 것

`requestPlay(intent)` / `playNext(intent)` 의 `intent` 는 `'auto' | 'next' | 'previous'` 다.

곡이 자연 종료된 것, 사용자가 `/다음` 을 누른 것, `/이전곡` 을 누른 것은 **전부
`musicPlayer` 를 Idle 로 만들어 같은 `onTrackEnd()` 경로를 탄다.** 그런데 해야 할 일이 다르다.

| intent | 동작 |
|---|---|
| `auto` | 반복재생이 켜져 있으면 같은 곡을 다시, 아니면 기록에 넣고 다음 곡 |
| `next` | 반복재생이라도 **다음 곡으로** (건너뛰기가 같은 곡을 또 트면 안 됨) |
| `previous` | 현재 곡을 대기열 맨 앞으로 되돌리고, `history` 에서 이전 곡을 꺼냄 |

`nextIntent` 필드에 담아 `onTrackEnd()` 가 읽어간다.
**재생 시간(`playbackDuration`)이나 `current` 가 null 인지 같은 것으로 추측하지 말 것.**
초기 구현이 `skip()` 에서 `loop` 를 잠깐 끄고 `current` 를 null 로 만드는 방식이었는데,
정상 동작이 순서의 우연에 의존하고 있어서 다음 사람이 반드시 깨뜨린다.

### 3.6-5 사진 자동 정리 — **데이터를 지우는 코드다**

`images/cleanup.js`. 고칠 때 특히 조심할 것. 되돌릴 수 없다.

**"디스크의 N%" 를 기준으로 삼지 않았다.** 디스크에는 OS·로그·node_modules 도 있다.
로그가 폭주해 디스크가 찼을 때 사진을 지우면 **원인은 그대로 둔 채 데이터만 잃는다.**
그래서 기본 기준은 `IMAGE_MAX_GB` — **이미지 폴더의 예산**이다.
다만 서버 생존을 위해 `IMAGE_MIN_FREE_GB` 라는 디스크 바닥 안전장치를 따로 둔다.

| 설정 | 기본 | 역할 |
|---|---|---|
| `IMAGE_MAX_GB` | 15 | 이미지 예산. 초과하면 정리 |
| `IMAGE_CLEANUP_TARGET_PERCENT` | 80 | 예산의 이 비율까지 내려감 (**히스테리시스**) |
| `IMAGE_MIN_KEEP_DAYS` | 7 | 이보다 최근 사진은 보호 |
| `IMAGE_MIN_FREE_GB` | 2 | 디스크 여유가 이보다 적으면 예산 무시하고 정리 |

**설계상 중요한 네 가지**

1. **히스테리시스** — 예산을 넘자마자 딱 그 아래로만 지우면 다음 업로드에 또 돈다.
   80% 까지 내려가서 한동안 다시 돌지 않게 한다.
2. **최근 사진 보호** — 버그나 대량 업로드로 방금 올린 사진이 통째로 날아가는 것을 막는다.
   이 보호 때문에 목표만큼 못 지우면 `shortfall` 로 표시하고 **설정을 바꾸라고 안내**한다.
   조용히 실패하면 디스크가 찬 채로 방치된다.
3. **디스크 바닥에서만 보호 해제** (`ignoreAge`) — 서버가 멈추는 것보다는 낫다.
   이때는 알림에 "보호를 해제했다"고 반드시 밝힌다.
4. **계획과 실행이 분리되어 있다** (`planCleanup` / `runCleanup`).
   `/정리` 미리보기와 자동 정리가 **같은 함수**를 쓰므로 "보여준 것과 지운 것" 이 어긋나지 않는다.
   확인 버튼을 누른 시점에 `planCleanup` 을 **다시** 부른다 — 미리보기 이후 사진이 더 올라왔을
   수 있고, 옛 목록으로 지우면 엉뚱한 파일을 건드린다.

**지운 내용은 반드시 디스코드에 알린다.** 사용자 데이터가 조용히 사라지면 안 된다.

### 3.6-4 갤러리 바로가기 패널

`images/panel.js`. 사진이 저장될 때마다 **그 채널 맨 아래에 "갤러리 열기" 버튼을 띄운다.**
피드백: 매번 `/갤러리` 를 치는 게 불편하다.

음악 제어판(3.6-1)과 규칙이 같다 — 맨 아래면 수정, 밀려났으면 지우고 다시,
새로 보낼 때는 `SuppressNotifications`. 다만 상태가 **채널 단위**다(음악은 서버 단위).
사진이 한 장도 없으면 띄우지 않는다.

버튼은 `ButtonStyle.Link` 라서 클릭 이벤트가 오지 않는다. 처리할 핸들러가 없어도 된다.

### 3.6-3 웹 갤러리 인증: 보기는 공개, 파괴적 작업만 잠금

소유자 결정: **"링크만 있다면 아무나 접근 가능하게 하자"** — 친구들이 비개발자라
로그인·암호 단계를 두면 아무도 안 쓴다. 디스코드 OAuth2 기반 채널 권한 연동도 검토했으나
공수 대비 이득이 낮다고 판단해 보류했다(필요해지면 그때 별도 작업으로).

그래서 인증을 **전역 미들웨어에서 경로별로** 나눴다.

| 경로 | 인증 | 이유 |
|---|---|---|
| `GET /f/:folder`, `/img/...`, `/dl/...` | **없음** | 친구들이 링크만 열면 되도록 |
| `GET /` | 없음 | 안내만. **폴더 이름을 노출하지 않는다** |
| `GET /folders` | `requireTokenPage` | 전체 폴더 목록은 소유자 전용 |
| `POST /api/*` | `requireToken` | 삭제·이동은 되돌릴 수 없음 |

인증 실패 응답이 두 종류인 이유:
- `requireToken` (API용) → **JSON 401, `WWW-Authenticate` 없음.**
  브라우저 로그인창이 뜨면 안 되므로. 페이지의 JS 가 직접 암호를 물어 헤더에 붙인다.
- `requireTokenPage` (페이지용) → **`WWW-Authenticate` 포함 401.**
  소유자만 오는 페이지이므로 브라우저 기본 로그인창을 띄우는 게 편하다.

**폴더 이름이 새어나가지 않게 하는 것도 요구사항이다** ("폴더목록을 볼 수 있는건 나만").
그래서 두 곳을 막았다.
1. 루트에서 폴더 목록을 없애고 안내 페이지로 대체
2. 갤러리 페이지의 **이동 대상 폴더 datalist 를 제거** — 인증 없이 렌더되는 HTML 이라
   다른 채널 폴더 이름이 그대로 노출됐다

또 **폴더 안에 "뒤로가기(폴더 목록)" 버튼을 두지 않는다.** 친구가 자기 채널이 아닌
폴더로 넘어갈 통로가 되기 때문이다. 되살리지 말 것.

`/갤러리` 명령은 **그 채널의 폴더**로 바로 링크한다(`resolveFolder`).
목록 링크를 주면 친구들이 401 페이지로 가버린다.

**보기까지 공개하면서 삭제도 공개하면 안 된다.** 공개 포트는 자동 스캐너에 금방 발견되고,
그때 사진이 통째로 지워지면 복구 방법이 없다. 친구들은 삭제·이동을 쓸 일이 없으므로
이 구분은 요청한 편의성을 전혀 해치지 않는다. **이 경계를 없애지 말 것.**

브라우저 쪽은 관리 요청에만 `Authorization: Basic` 을 붙이고, 암호를 `sessionStorage` 에
캐시해 탭 안에서 다시 묻지 않는다. 401 이 오면 캐시를 버리고 한 번 더 묻는다.

### 3.7 메시지 핸들러 우선순위 (채널이 겹칠 때의 동작)

`index.js`의 `MessageCreate` 처리 순서는 의도적으로 이렇게 되어 있다.

```js
await handleImageMessage(message);            // 막지 않음 (early return 없음)
if (await handleMusicMessage(message)) return; // 처리하면 여기서 끝
await handleTtsMessage(message);
```

- **이미지 저장은 다른 기능을 막지 않는다.** 사진에 설명글을 달아 올리면
  저장도 되고 그 글을 읽어주기도 한다. (초기 구현은 early return이라 캡션이 안 읽혔다 — 고침)
- **음악과 TTS는 배타적이다.** 유튜브 링크를 소리내어 읽는 건 의미가 없으므로
  음악이 처리한 메시지는 TTS로 넘기지 않는다.
- **`MUSIC_TEXT_CHANNEL_ID`가 비어 있으면 모든 채널에서 링크를 감지한다.**
  그래서 TTS 채팅방에 유튜브 링크를 써도 읽어주지 않고 재생된다.
  의도된 동작이며 `.env.example`에도 적어두었다. 바꾸려면 음악 채널을 명시적으로 지정하면 된다.

### 3.6-7 투표 (`src/poll/index.js`)

**디스코드 기본 투표를 쓰지 않았다.** 기본 기능은 선택지에 글자와 이모지만 넣을 수 있고,
**이미지를 못 넣는다.** "글자와 이미지의 조합으로 문항을 만들고 싶다" 가 요구사항이었다.

#### 만들기는 **모달**로 받는다 — 명령어 칸이 아니라

처음에는 슬래시 명령어 칸으로 받았다(`질문` `선택` `사진1`~`사진5` = 7칸).
소유자 피드백: **"하나의 채팅에서 투표를 생성하는 과정이 헷갈리고 번거롭다."** 맞는 지적이다.
명령어 칸은 한 줄에 죽 늘어서서 무엇을 채우는 중인지 보이지 않는다.

그래서 `/투표` 만 치면 **모달(`buildCreateModal`)** 이 뜬다. 칸은 4개다.

| 칸 | 컴포넌트 | 비고 |
|---|---|---|
| 질문 | `TextInput` Short | 필수 |
| 선택지 | `TextInput` Paragraph | 한 줄에 하나. 필수 |
| 질문 사진 | `FileUpload` (1장) | 질문 아래에 **크게**(`setImage`) |
| 선택지 사진 | `FileUpload` (5장) | **순서대로** 크게(`setImage`) |

**★ 모달에 파일 업로드 칸을 넣을 수 있다** (`FileUploadBuilder`, `ComponentType.FileUpload`).
오래된 자료에는 "모달은 글자만 받는다" 고 되어 있어서 처음엔 사진을 명령어 칸으로 받았다.
이 저장소의 discord.js 에서 실제로 조립되는 것을 확인했다(`getUploadedFiles` 로 읽는다).
이 컴포넌트를 빼면 사진을 다시 명령어 칸으로 돌려야 하고, 그때의 불편으로 되돌아간다.

모달 칸은 **5개가 한도**다. 지금 4개를 쓰므로 하나 남았다.

**명령어 인자(`질문` `선택`)는 남겨뒀다.** 둘 다 선택 사항이고, 비우면 모달이 뜬다.
모달이 안 뜨는 환경이 있어도 투표를 만들 길이 남아 있어야 하기 때문이다
(단계마다 동작하는 상태를 유지한다는 원칙). 대신 인자 경로에는 사진 칸을 두지 않는다 —
그걸 두면 명령어 칸이 다시 일곱 개가 된다.

#### 선택지는 한 칸에 몰아 받는다

선택지를 칸마다 따로 받으면 `선택1`~`선택10` 으로 칸이 열 개가 된다.
모달이든 명령어든 **선택지는 한 칸에** 몰아 받는다. 모달에서는 줄바꿈, 명령어에서는 쉼표.
`parseOptions()` 가 **둘 다** 받아준다.

```
/투표 질문:점심 뭐 먹지  선택:피자, 치킨, 초밥
```

사진만 번호로 붙인다(앞에서부터 5개). 칸 수가 `질문` + `선택` + `사진1~5` = **7개**로 줄었다.
**칸을 늘리는 방향으로 되돌리지 말 것.**

#### ⚠️ 첨부는 반드시 **다시 올려야** 한다

슬래시 명령어로 받은 첨부(`interaction.options.getAttachment`)의 URL 은 **만료된다.**
그 주소를 임베드에 그대로 박으면 나중에 사진이 깨진다.

그래서 `AttachmentBuilder(att.url, { name })` 로 **투표 메시지에 다시 올리고**,
임베드에서는 `attachment://poll-1.png` 로 참조한다. 그러면 그 메시지가 살아 있는 한 계속 보인다.

#### 그 밖의 결정

| | |
|---|---|
| 사진은 `setImage` (썸네일 아님) | **모바일에서는 썸네일을 눌러도 확대되지 않는다**(PC 는 된다 — 실사용 확인). 사진을 보고 고르는 기능인데 폰에서 크게 못 보면 쓸모가 없다. 메시지가 길어지는 것보다 보이는 쪽이 낫다. **되돌리지 말 것** |
| 표는 **익명** | 누가 뭘 골랐는지는 안 보이고 수만 보인다. 본인에게만 ephemeral 로 알린다 |
| 같은 버튼 다시 = 취소 | "취소" 버튼을 따로 두지 않아도 되므로 버튼이 하나 준다 |
| 마감은 만든 사람 + `ManageMessages` | 아무나 마감하면 곤란하고, 만든 사람이 자리에 없을 수도 있다 |
| `data/polls.json` 에 저장 | **재시작을 견뎌야 한다.** 버튼은 메시지에 남아 있으므로 표만 잃으면 안 된다 |
| 자동 마감은 **칸 하나** | 켜고 끄는 스위치를 따로 두지 않았다. **비우면 안 함** 이 곧 off 다. 스위치를 따로 두면 칸이 6개가 되어 모달 한도(5)를 넘는다 |
| 마감 시각은 디스크에 | `setTimeout` 은 재시작하면 사라진다. `closesAt` 을 저장해두고 켜질 때 `restorePollDeadlines()` 로 다시 건다. 꺼져 있는 동안 지난 것은 **즉시 닫는다** (타이머와 같은 방식, 3.4-2) |
| 남은 시간은 **본문**에 | `<t:초:R>` 은 보는 사람 시간대로 "3분 후" 를 세어준다. **푸터에서는 렌더링되지 않아** `<t:...>` 글자가 그대로 보인다 |
| `channelId` 를 저장 | 마감할 때 메시지를 다시 찾아야 한다. 채널을 모르면 화면을 못 고친다 |
| 90일 지난 것은 버림 | 파일이 무한정 커진다. 지난 투표의 버튼은 "더 이상 집계하지 않습니다" 로 답한다 |

버튼 앞머리는 `v:` 다. `index.js` 에 등록하고 `inRole('poll')` / `featureEnabled` 로 거른다.

### 3.6-8 영화 고르기 (`src/movie/`)

친구들이 "오늘 뭐 볼지" 정할 때 쓴다. 장르 + OTT 를 고르면 TMDB 에서 후보를 가져와
**한 편을 뽑아주거나**, **여러 편을 후보로 투표를 만든다.**

기획안: `영화룰렛-기획안.md` (2026-09-01). 아래는 그 기획을 이 프로젝트 관례에 맞춰 조정한 것이다.
**구현 완료 (2026-09-01).** 아래 수치는 전부 실측값이다.

#### 왜 TMDB 인가 — 넷플릭스·쿠팡플레이에 직접 붙지 않는다

| 방법 | 왜 안 쓰나 |
|---|---|
| 각 OTT 공식 API | **없다.** 서드파티용 카탈로그 조회 API 를 제공하지 않는다 |
| 비공식 크롤링 | 약관 위반 소지 + 구조가 바뀔 때마다 깨진다 + 계정·IP 차단 위험 |
| **TMDB** ✅ | 무료 공식 API. `watch_region=KR` + `with_watch_providers` 로 **"한국에서 이 OTT 로 볼 수 있는 작품"** 을 준다 (JustWatch 데이터) |

유튜브를 `yt-dlp` 로 감싼 것과 같은 판단이다(3.1). **깨질 것을 알면서 크롤링하지 않는다.**

#### ★ 명령어는 **하나**, 나머지는 전부 버튼

기획안은 `/영화뽑기 장르:… ott:…` 와 `/영화투표 장르:… ott:… 개수:…` 두 개를 제안했다.
**그대로 만들지 않는다.** 이 프로젝트는 같은 이유로 이미 두 번 되돌린 적이 있다.

- 명령어가 30개까지 불어나 한 번 걷어냈다 (3.6-6)
- 투표를 명령어 칸 7개로 받았다가 "헷갈리고 번거롭다" 는 피드백을 받고 모달로 바꿨다 (3.6-7)

장르와 OTT 를 명령어 칸에서 고르게 하면 **똑같은 실수를 세 번째로 반복하는 것이다.**
비개발자 친구가 `/영화뽑기` 를 치고 장르 자동완성을 타이핑하게 만들면 안 된다.

```
/영화              ← 칸 없음. 이것만 치면 고르기 판이 뜬다 (나만 보임)

  🎬 무엇을 볼까요?
  [ 장르 고르기        ▾ ]   ← 드롭다운 (안 고르면 전체)
  [ OTT 고르기         ▾ ]   ← 드롭다운, 여러 개 선택 가능
  [ 🎲 한 편 뽑기 ] [ 🗳️ 투표 만들기 ]
```

- **명령어 1개, 결과 2가지.** 뽑기와 투표는 같은 조건에서 갈라지는 두 행동일 뿐이다.
- 장르는 **안 골라도 된다** (= 전체). 고민 없이 바로 뽑고 싶은 게 이 기능의 목적이다.
- OTT 는 **여러 개** 고를 수 있다. "넷플릭스 아니면 쿠팡플레이 중 아무거나" 가 실제 상황이다.

#### 고른 상태는 `customId` 에 담는다 — 메모리에 두지 않는다

드롭다운 두 개를 거쳐 버튼을 누르는 동안 "무엇을 골랐는지" 를 들고 있어야 한다.
메모리 Map 에 두면 재시작에 날아가고(3.6-1a 와 같은 실수), 사람마다 판이 여러 개 뜨면 엉킨다.

**고른 값을 컴포넌트의 `customId` 에 실어 판을 다시 그린다.**

```
mv:g:<providers>            장르 드롭다운  (지금까지 고른 OTT 를 물고 있음)
mv:o:<genre>                OTT 드롭다운   (지금까지 고른 장르를 물고 있음)
mv:draw:<genre>:<providers> 🎲 한 편 뽑기
mv:poll:<genre>:<providers> 🗳️ 투표 만들기
```

`customId` 는 100자 제한이다. 장르 ID 는 최대 5자리, provider ID 를 쉼표로 이어도
여유가 크다. 상태가 메시지에 들어 있으므로 **재시작해도 그 판이 그대로 동작한다.**

#### ⚠️ 영화와 드라마는 **장르 번호가 다르다**

액션은 영화가 `28`, 드라마는 `10759`(Action & Adventure) 다. 섞어서 뽑기로 했으므로
`GENRES` 가 종류별 번호를 **같이** 들고 다닌다. 하나로 합치려 들지 말 것.

로맨스·스릴러·공포는 **드라마에 없는 장르**다(`tv: null`). 그때는 영화만 찾는다.
`verify` 가 TMDB 의 실제 장르 목록과 대조하므로, 번호가 바뀌면 검증에서 걸린다.

#### 뽑기 결과와 다시 뽑기

포스터 + 제목 + 연도 + 평점 + 줄거리(축약) 임베드에 버튼을 붙인다.

```
[ 🎲 다시 뽑기 ]  [ 🔀 조건 바꾸기 ]
```

- **다시 뽑기**: 같은 조건으로 재추첨. **직전 작품은 제외**한다 (연달아 같은 게 나오면 고장으로 보인다).
  제외 대상은 `customId` 에 실은 직전 TMDB ID 하나면 충분하다. 전부 기억하려 들지 말 것.
- **조건 바꾸기**: 고르기 판으로 되돌아간다. 명령어를 다시 칠 필요가 없다.

**포스터는 `setImage` 로 붙인다. `setThumbnail` 을 쓰지 말 것** — 모바일에서 썸네일은
눌러도 확대되지 않는다(3.6-7 에서 실사용으로 확인). 포스터는 보라고 있는 것이다.

#### 투표 만들기는 **기존 투표를 그대로 쓴다**

새 투표 시스템을 만들지 않는다. `poll/index.js` 의 `createPoll()` 을 `export` 해서 부른다.
버튼 투표·익명 집계·마감·재시작 견디기가 전부 딸려 온다.

```js
// movie 쪽에서
await createPoll(interaction, '오늘 뭐 볼까?', titles, posters);
//   posters = [{ url: 'https://image.tmdb.org/t/p/w500/...jpg', name: 'poster.jpg' }, ...]
```

`createPoll()` 이 이미 **`AttachmentBuilder` 로 다시 올린다**(3.6-7). TMDB 포스터 주소를
임베드에 그대로 박으면 안 되는데, 이 경로를 타면 자동으로 해결된다.
**후보는 최대 5편**이다 — 사진이 5장까지이고, 포스터 없는 후보를 섞으면 비교가 안 된다.

#### OTT 는 **전부 지원하되, 쓸 것은 서버마다 고른다**

소유자 결정: 넷플릭스·쿠팡플레이만 넣지 말고 **TMDB 가 주는 한국 OTT 를 다 지원**하되,
**우리가 실제로 구독한 것만** 고르기 판에 뜨게 한다.

친구들에게 안 보는 OTT 를 보여줘봐야 고르면 "볼 수 없는 작품" 만 나온다.
그렇다고 코드에 두 개만 박아두면 구독을 바꿀 때마다 배포해야 한다.

**`settings.js` 에 서버별로 저장한다** — 채널 설정·기능 on/off 와 같은 방식(3.8).
`/기능` 처럼 **버튼 판**으로 켜고 끈다. 명령어를 새로 만들지 않는다.

```
/영화  →  [ ⚙️ 쓰는 OTT 고르기 ]      ← 고르기 판 안의 버튼
          ☑️ 넷플릭스  ☑️ 쿠팡플레이  ☐ 티빙  ☐ 웨이브  ☐ 디즈니+  ☐ 왓챠 …
```

- 아무것도 안 고른 서버는 **전체**로 본다 (설정 전에도 동작해야 한다).
- 목록은 시작할 때 TMDB 에서 받아온 것으로 채운다. 코드에 이름을 박지 않는다.

#### ★ provider ID 는 **추측하면 안 된다** — 실측표

기획안은 쿠팡플레이를 `356` 으로 적어뒀는데 **`356` 은 실제로 wavve 다.**
2026-09-01 에 `/watch/providers/*?watch_region=KR` 로 실측한 값이다.

| OTT | provider_id | TMDB 영화 | TMDB 드라마 |
|---|---|---|---|
| wavve | 356 | 14,781 | 2,582 |
| 왓챠 | 97 | 11,903 | 5,671 |
| TVING | 1883 | 6,323 | 5,378 |
| 넷플릭스 | 8 | 5,208 | 3,872 |
| 디즈니+ | 337 | 1,788 | 1,004 |
| **쿠팡플레이** | **1881** | **0** | **8** |

#### 쿠팡플레이는 TMDB 에 **두 가지로** 존재한다

위 표의 "볼 수 있는 곳"(provider 1881) 으로는 영화 0 · 드라마 8 뿐이다.
그런데 소유자가 TMDB 홈페이지에서 **31개**를 보고 이상하다고 했고, 재보니 원인이 나왔다.

| | TMDB 개념 | ID | 건수 |
|---|---|---|---|
| **볼 수 있는 곳** | watch provider | 1881 | 영화 0 · 드라마 8 |
| **만든 곳** | TV network | **5169** | **31** |

홈페이지에서 보이는 목록이 **network 쪽(오리지널)** 이다.
오리지널은 쿠팡플레이에서 볼 수 있으므로, provider 로 찾은 것에 **network 로 찾은 것을 보탠다**
(`PROVIDERS` 의 `network` 필드 → `findCandidates` 가 `with_networks` 로 한 번 더 조회).
실측으로 8편 → **22편**이 됐다.

다른 OTT 도 network 가 있지만(넷플릭스 213, 왓챠 3898, 디즈니+ 2739) provider 자료가
이미 수천 건이라 보탤 이유가 없다. **쿠팡플레이만 예외로 둔다.**

⚠️ 그래도 **쿠팡플레이는 영화가 0편이다.** 목록에서 지우지는 않았다 —
지우면 "왜 없냐" 는 질문이 반복된다. `sparse: true` 로 표시해 드롭다운에 경고를 달고,
결과가 0건일 때 "영화 정보가 없고 오리지널 드라마만 나온다" 고 안내한다.

#### 그래도 시작할 때 **대조한다**

TMDB 의 provider ID 는 지역·시점에 따라 바뀔 수 있다. 그런데 매번 조회하면 느리다.

그래서 **상수로 박아두고, 시작할 때 `/watch/providers/movie?watch_region=KR` 로 한 번
확인해 이름이 다르면 경고를 찍는다.** `.env` 중복 항목 경고나 두 봇 토큰 대조와 같은 방식이다 —
**조용히 틀리는 것보다 시끄럽게 알려주는 쪽**을 택한다.

장르 목록도 시작할 때 한 번 받아 캐시한다(24시간). **영화 후보 자체는 캐시하지 않는다** —
"뽑을 때마다 다른 결과" 가 이 기능의 전부다.

#### 예외 처리

| 상황 | 처리 |
|---|---|
| 조건에 맞는 작품 0건 | "조건에 맞는 작품이 없어요" + **조건 바꾸기 버튼**. 장르를 빼보라고 안내 |
| TMDB 호출 실패·타임아웃 | 원인을 구체적으로 + **다시 시도 버튼**. `friendlyError()` 관례(3.1) |
| 후보가 요청한 개수보다 적음 | 있는 만큼만 뽑고 **몇 편인지 알림** (재생목록 자르기와 같은 원칙, 3.1-1) |
| 포스터가 없는 작품 | 뽑기에서는 글자만, **투표 후보에서는 제외** (사진 비교가 목적이므로) |

#### 어디에 붙나

- **망고** 쪽이다. `BOT_ROLE` 의 `mango` 에 `movie` 를 추가한다 (2.1).
- `FEATURES` 에 `movie` 를 넣으면 `/기능` on/off 에 자동으로 편입된다 (3.7-1).
- `commands.js` 에서 `tag('movie', movieCommands)`.
- 버튼 앞머리는 **`mv:`**. `index.js` 에 등록한다.
- `TMDB_API_KEY` 는 `.env` (망고). 없으면 `/영화` 가 **"API 키가 없습니다 + 발급 방법"** 을 안내하고
  기능은 조용히 꺼둔다 — 키 없이 봇 전체가 죽으면 안 된다.

#### 이번 범위에서 뺀 것

영화/드라마 구분(소유자 결정: **섞어서 뽑는다**), 최근 본 것 제외, 연도·평점 세부 필터.
전부 나중에 붙일 수 있다. **처음부터 칸을 늘리면 이 기능을 만든 이유가 사라진다.**

### 3.6-9 방송 기록 / 타임머신 (`src/stream/`)

여러 명이 각자 유튜브 라이브를 켜고 협동 게임을 할 때, 재미있던 순간을 **버튼 한 번으로
찍어두고** 방송이 끝난 뒤 유튜브 설명란에 붙일 타임라인을 뽑는다.

기획·확정 사항·Phase 2 계획은 **[게임방송-기획.md](게임방송-기획.md)** 에 있다.
여기에는 **이 프로젝트의 다른 부분과 충돌하는 지점만** 적는다.

#### ★ t=0 은 `release_timestamp` 다. 명령 실행 시각이 아니다

마킹 시각을 영상 타임스탬프로 바꾸려면 방송이 시작된 시각이 필요하다.
`/방송` 을 친 시각을 쓰면 **라이브를 켜고 등록하기까지의 시간만큼 전부 밀린다.**
유튜브 설명란에 붙이는 것이 목적인데 전부 어긋난 타임라인이 나온다.

`ytdlp.js` 의 `liveInfo()` 가 이 값을 읽는다. 진행 중인 라이브에서도 나온다 (실측):

```
release_ts=1645318623 ts=1645314636 is_live=True status=is_live dur=NA
```

⚠️ **`timestamp` 를 쓰면 안 된다.** 그건 게시 시각이고, 위 실측에서 66분 차이가 났다.

계산은 `store.js` 의 한 줄이다. **부호를 헷갈리기 쉬우니 정의부터 읽을 것.**

```
markSecondsFor = mark.at − stream.startedAt − stream.offsetSec
→ 이 값이 (mark.at − 진짜시작) 이어야 하므로  offsetSec = 진짜시작 − startedAt
```

봇이 8분 진행으로 알지만 실제로 68분 켜져 있었으면 `offsetSec` 은 **−3600** 이고
마킹은 60분씩 **늘어난다**. verify 가 이 부호를 숫자로 못 박아 검사한다.

#### 어긋난 것을 **등록하는 순간** 잡는다

`release_timestamp` 가 틀리면 조용히 틀린다. 오프셋 버튼이 있어도 "지금 조절해야 한다"는
사실을 모르면 소용없다. 그래서 등록 확인과 제어판에 **계산된 경과 시간을 항상 보여준다.**

```
✅ 등록했습니다 · 방송 시작 21:04 · 지금 8분 진행 중
```

등록하는 사람은 자기가 언제 켰는지 안다. 한 시간 전에 켰는데 "8분" 이라고 나오면
**읽는 순간 잡힌다.** 이게 1차 방어선이고 `⏱️ 시간 어긋남` 버튼은 그 뒤의 수단이다.

#### 마킹은 **입력을 받지 않는다**

원래 기획은 모달로 상황 설명을 받는 것이었다. **뒤집었다.**
게임 중이다. 버튼 → 창 → 입력 → 제출을 하는 동안 그 순간은 지나가고, 결국 아무도 안 누른다.
설명은 방송이 끝난 뒤 요약판에서 붙인다 — 그때가 여유도 있고 영상을 보면서 쓰니 더 정확하다.

#### ★ 마킹은 **찍은 사람의 방송에만** 들어간다

처음에는 모두의 타임라인에 넣었다. 원래 기획이 **"협동 게임"** 전제였기 때문이다.
**그 전제가 틀렸다.** 소유자 지적: "동시에 방송은 켰지만 각자 다른 게임을 하고 있을 수도 있거든."

그러면 남의 게임에서 있었던 일이 내 타임라인에 섞이고, **되돌릴 방법도 없다.**
그래서 기본이 **"내 방송만"** 이다 (`mark.forUserId`). `timelineFor()` 가 그 칸을 보고 걸러낸다.

다 같이 하던 순간은 **찍은 뒤에** 넓힌다 — 찍은 직후의 나만 보이는 답에
**`👥 다 같이 하던 순간`** 버튼이 붙는다(`tm:share:<markId>` → `forUserId = null`).
그 순간은 이미 잡혀 있으니 한 번 더 누를 여유가 있다.
**이게 마킹을 한 번 클릭으로 유지하면서 협동 게임도 담는 방법이다** — 찍는 순간에
"내 것이냐 다 같이냐" 를 묻는 것은 모달과 같은 실수다.

- 등록을 **안 한** 사람이 누르면 갈 곳이 없으므로 **그때만** 모두의 것으로 둔다.
- 넓혀도 **늦게 켠 사람의 음수 마킹은 여전히 빠진다.** 그 장면이 영상에 없다.
- `↩️ 내 마지막 취소` 는 **내가 찍은 것 중** 마지막을 지운다
  (`removeLastMark(session, userId)`). 그냥 마지막을 지우면 남이 방금 찍은 것을 지운다.
- 제어판의 "최근 마킹" 도 **그 마킹 주인의 시간**으로 보여준다. 첫 사람 기준으로
  전부 보여주면 다른 사람 것이 엉뚱한 시각으로 나온다.

#### 그래도 마킹은 세션에 한 줄씩만 저장한다

마킹은 "몇 시 몇 분에 무슨 일이 있었다" 는 **벽시계 사건 하나**다.
사람마다 다른 것은 그것을 자기 영상 시간으로 바꾼 결과뿐이고, 그건 계산이다.
사람별로 복사해두면 늦게 합류한 사람이 생겼을 때 어긋나고, 오프셋을 고칠 때마다 전부 고쳐야 한다.

**"누구 것인가" 도 복사가 아니라 한 칸(`forUserId`)이다.**
`byUserId`(누가 찍었나)와 다를 수 있다 — 내가 찍고 나중에 넓히면 `forUserId` 만 `null` 이 된다.

#### 제어판은 음악과 **반대로** 다룬다

| | 음악 제어판 | 방송 제어판 |
|---|---|---|
| 재시작하면 | **지운다** — 곡이 끊겨서 "재생 중" 이 거짓말 | **남긴다** — 기록이 디스크에 남아 이어진다 |
| 판별 | customId 가 `m:` | customId 가 **`tm:panel:`** |
| 훑기 | `keepMusic` 하나만 남김 | `keepStream` 하나만 남김 |

⚠️ **`tm:` 으로 넓게 판별하면 안 된다.** 요약판에도 `tm:desc:` 버튼이 붙어 있어서,
그러면 훑기가 **요약판을 제어판으로 오인해 지운다.** (3.6-1a, 3.6-1c 와 같은 계열의 함정)

#### ⏹️ 종료는 되돌릴 수 있어야 한다

누구나 누를 수 있는 버튼이고, 6명이 아직 게임 중인데 눌릴 수 있다.
저장이 비파괴인 것만으로는 부족하다 — **닫힌 제어판의 `▶️ 이어서 기록` 이 있어야**
실제로 비파괴다. 끝난 세션도 지우지 않는다(180일). 클립은 며칠 뒤에도 뽑는다.

#### 요약판은 사람당 한 장, 그리고 **그 자리에서 고쳐 쓴다**

버튼은 메시지당 25개, 드롭다운 선택지도 25개가 상한이다. 6명 × 마킹 여러 개를 한 장에
넣으면 넘친다. 그리고 **하나씩 순서대로** 보낸다 — 한꺼번에 던지면 채널 전송 한도에 걸린다.
**제어판을 맨 마지막에 다시 올려** 채널 맨 아래에 남게 한다.

⚠️ 설명을 채우거나 클립을 만들 때 요약판을 **새로 올리면 안 된다.**
클립 5개를 만들면 같은 타임라인이 채널에 6장 쌓인다. 제어판을 메시지 하나로 고쳐 쓰는 것과
같은 이유다(3.6-1). 그래서 보낸 메시지 ID 를 `session.summaryMsgs` 에 기억해두고
`refreshSummary()` 가 **`msg.edit`** 으로 고친다.

설명을 채우면 글자가 늘어 **장수가 바뀔 수 있다.** 그때만 새로 올리고(`postSummary`),
새로 올렸을 때만 제어판을 맨 아래로 되돌린다. 고쳐 쓴 경우에는 채널에 아무것도 안 늘어나므로
제어판을 건드리지 않는다 — 클립을 여러 개 만들어도 채널이 조용하다.

#### 망고가 yt-dlp 를 쓰게 됐다 — `.env` 에도 설정이 필요하다

yt-dlp 인프라 일체가 `src/music/ytdlp.js` 에 있고 **`.env.music` 으로 설정된다.**
그런데 망고는 `npm start` = **`.env` 만 읽는다** (음악 봇만 `--env-file=.env.music`).

`YTDLP_PATH` 와 `YTDLP_COOKIES_FILE` 을 **`.env` 에도** 넣어야 한다.
안 넣으면 기동에 3초씩 깔리거나(3.1-2d) 유튜브에 막힌다(3.1-4).

그래도 망고에 두는 것이 맞다. 음악 프로세스는 반응 속도를 오래 다듬어놨으므로 거기에
무거운 작업을 얹지 않는다. **이건 의식적인 선택이고 위 설정 중복이 그 대가다.**

⚠️ `serializeExtraction` 은 **프로세스 안에서만** 통한다. 프로세스 간 락은 아직 없다 —
Phase 2 의 클립 추출은 재인코딩을 하지 않아(복사 모드) CPU 를 거의 안 쓰기 때문이다.
음악이 실제로 끊기는 것을 확인한 뒤에 붙일 것.

#### 매번 링크를 붙이지 않게 — **고정 주소**를 기억한다 (OBS 웹훅 대신)

원래 기획의 Phase 3 은 "OBS Webhook 으로 자동 시작" 이었다. **실용적이지 않다.**

obs-websocket 은 **OBS 가 서버**이고 외부 도구가 클라이언트로 붙는 구조다
([obs-websocket](https://github.com/obsproject/obs-websocket)).
즉 봇이 각자의 집 PC 로 **접속해 들어가야** 하는데, 6명분 포트 개방이 필요하다.
그리고 스크립트로 우리 엔드포인트를 호출하게 해도 **유튜브 주소를 알려줄 수 없다** —
OBS 는 스트림 키만 알고 방송 URL 을 모른다. 사람이 링크를 붙이는 일이 그대로 남는다.

대신 **같은 목적을 싸게 이루는 길**이 있다. 유튜브의 채널 고정 주소는 방송마다 바뀌지 않고,
**yt-dlp 가 그걸 지금 하는 방송으로 풀어준다.** 실측 (2026-09-03):

```
youtube.com/@ABCNews/live → id=iipR5yUp36o  release_ts=1746107641  status=is_live
```

그래서 `liveInfo()` 가 `%(id)s` 도 함께 읽고, `settings.js` 가 사람마다 고정 주소를 기억한다.
등록은 같은 게임이라면 제어판의 **`🎬 지난 게임으로 등록`** 버튼 하나로 끝난다.

#### ⚠️ 그런데 **고정 주소는 공개 방송만 찾는다** — 이 프로젝트에는 반쪽만 쓸모있다

**처음에 이걸 놓치고 README 에 "추천" 으로 적었다.** 위 실측은 **공개** 뉴스 채널이었고,
그걸 소유자의 **일부공개** 설정에 일반화한 것이 잘못이었다. 소유자가 바로 막혔다:

> "일부공개로 하고 방송시작했는데 /방송 으로 등록하면 지금 방송중이 아니라고 떠"

라이브 중이 아닌 채널의 `/live` 에 대해 yt-dlp 는 이렇게 답한다 (실측):

```
ERROR: [youtube:tab] @Google: The channel is not currently live
```

**일부공개는 채널 목록에 안 뜨므로 같은 답이 온다.** `일부공개` 의 정의가 그것이다.
그리고 `friendlyError` 는 이 문장을 **영어 그대로 넘긴다** — 그래서 사람이
"분명히 방송 중인데" 하고 멀쩡한 주소를 들여다보게 됐다.

그래서 `registerStream` 이 이 문장을 **가로채서** 사정을 설명한다.
0절의 확정 전제가 "전원 일부공개" 이므로, **안내는 이번 방송 주소를 앞세운다.**
고정 주소는 접어둔 선택지로 남긴다 — 공개로 바꾸면 그때 쓸모가 있다.

⚠️ 이 교훈을 일반화하면: **실측을 한 조건 밖으로 결론을 늘리지 말 것.**
공개 스트림으로 확인한 것은 공개 스트림에 대한 사실이다.

- ⚠️ **`watch?v=` 형태는 저장하지 않는다.** 그건 그 방송 하나만 가리켜서, 저장하면
  **매번 지난 방송을 등록하게 된다.** 경과 시간 되읽기(2.2a)가 잡으려는 바로 그 종류의
  조용한 오류다. `parseLiveHome()` 이 고정 주소 형태만 통과시킨다.
- ⚠️ 고정 주소로 조회했는데 방송이 없으면 **주소 문제가 아니다.** 아직 안 켠 것이다.
  "주소를 확인하세요" 라고 하면 멀쩡한 주소를 들여다보게 만든다.
- 저장은 **등록이 성공한 뒤에** 한다. 틀린 주소를 저장하면 매번 실패한다.
- 명령어를 새로 만들지 않는다. `/방송` 을 인자 없이 치면 상태 보기라서 겹치고,
  자주 하는 조작은 버튼이어야 한다 (3.6-6).

#### 클립 용량 자동 정리 — **예산은 사진과 따로 둔다**

`cleanupByBudget()` 이 `STREAM_CLIP_MAX_GB`(기본 5GB)를 넘으면 오래된 것부터 지운다.

⚠️ **사진 예산(`IMAGE_MAX_GB`)과 절대 합치지 마라.** 합치면 클립이 늘어날 때
사진이 지워진다 — 3.6-5 가 경고하는 "원인은 그대로 둔 채 데이터만 잃는" 그 실패다.
`clips.js` 는 `images/cleanup.js` 를 import 하지 않는다. **논리만 따라 쓰고 코드는 따로 둔다.**

안전장치는 3.6-5 와 같은 모양이다 (그 파일 머리말이 이유를 자세히 적어뒀다):

1. **최근 것은 안 지운다** (`STREAM_CLIP_MIN_KEEP_DAYS`, 기본 3일).
   방금 만든 클립이 사라지면 사람이 뭘 잃었는지도 모른다.
2. **예산의 80% 까지** 내려간다. 경계선에서 매번 재실행되는 것을 막는다.
3. **디스코드에 알린다.** 조용히 사라지면 안 된다.
   예산을 넘겼는데 1번 때문에 못 지웠으면 그 사실을 로그에 남긴다.

⚠️ 어느 서버에 알릴지는 **세션이 알려준다** (`sessionById(폴더)?.guildId`).
못 찾으면 "첫 번째 서버" 같은 짐작을 하지 말고 **로그에만** 남긴다 —
엉뚱한 채널에 남의 방송 이야기를 올리게 된다.

#### 클립 추출 (`src/stream/clips.js`) — **재인코딩하지 않는다**

2026-09-04 클립 페이지 삭제 인증 수정: API는 여전히 JSON 401을 반환하며,
클라이언트가 이를 받아 WEB_TOKEN 입력창을 표시하고 UTF-8 Basic 인증으로 한 번 재시도한다.
암호는 페이지 메모리에만 보관하고 401이면 폐기한다. 취소·오인증·통신 실패 시 카드를 지우지 않으며,
성공 응답에서도 `deleted === 1`을 확인한다. 보기·받기는 공개 경계를 유지한다.
verify는 실제 HTML 스크립트로 입력·취소·오인증·한글 암호·삭제 0건·통신 실패를 검사한다.

같은 날 받은 20초 요청 클립은 실제 컨테이너 길이 5초이며, 기본 ffmpeg 읽기에서는 영상·음성 0프레임이었다.
사용자 서버의 ffprobe도 영상 5초·음성 0.023220초·전체 703,487바이트로 같은 결과를 확인했다.
`-ignore_editlist 1`로 읽으면 영상 300프레임(60fps, 약 5초)이 나온다.
즉 웹 표시만의 문제가 아니며 재생 구간 메타데이터 문제도 확인됐다.
후속 원본 조회에서 `post_live`, 전체 길이 8,925초, 라이브 DASH 포맷만 반환됐다.
방송이 전날 종료됐다는 사용자 확인과 모순되지 않는다. yt-dlp의 `post_live`는 종료 후 VOD 처리 중을 뜻한다
([공식 필드 정의](https://github.com/yt-dlp/yt-dlp#output-template)).
같은 5,780~5,800초 구간은 로컬 ffmpeg에서 `Invalid data found when processing input`으로 실패했다.
첨부 파일과 동일한 5초 결과를 재현한 것은 아니므로, 정확한 15초 누락 원인을 확정하지 않는다.
`makeClip()`은 파일 생성·덮어쓰기 전에 `ensureClipReady()`로 최신 상태를 조회해 `post_live`를 차단한다.
조회 실패도 추출로 넘기지 않는다. 상태를 캐시하지 않아 처리 완료 후 재시도할 수 있다.
처리 시간은 보장하지 않으며, 이는 미완성 VOD 추출 방지이지 기존 파일 복구/모든 재생 오류 해결이 아니다.
기존 파일을 덮어쓰거나 임의로 20초로 늘리지 않는다.

`ytdlp.js` 의 `downloadSection()` 이 `--download-sections "*915-955"` 로 그 구간만 받는다.
전체를 받았다가 자르는 것이 아니다.

⚠️ **`--force-keyframes-at-cuts` 를 붙이지 마라.** yt-dlp 도움말이 직접 말한다:
"This is slow due to needing a re-encode". 1코어에서 몇 분씩 걸린다.
대신 키프레임 경계에서 잘려 ±2~3초 오차가 나는데, 40초 클립에서는 감수한다.

⚠️ **H.264(avc1)를 우선한다.** `height<=720` 만 주면 유튜브가 **AV1** 을 준다(실측 확인).
브라우저·기기 호환이 떨어진다. 화질을 낮추는 것도 재인코딩이 아니라 **낮은 포맷을 고르는 것**이라 공짜다.

⚠️ **`--ffmpeg-location` 을 반드시 넘긴다.** `--download-sections` 는 ffmpeg 이 필요하고,
서버에 ffmpeg 이 깔려 있다는 보장이 없다. 기본은 `ffmpeg-static` 이다.

##### ★★ `ffmpeg exited with code 183` — **화면 없는 방송이었다** (그리고 내가 원인을 잘못 짚었다)

소유자 서버에서 **`ffmpeg exited with code 183`** 이 났다. 183 이 무엇인지는 계산과
실측으로 확인했다 (아래). 그런데 **원인을 "쿠키" 로 짚은 것은 틀렸다.**

> "이전 테스트에선 음성만 녹화에 잡혀서 오류가 났나본데, 화면까지 녹화하고
> 클립 추출하니까 잘 된다."

**같은 쿠키 설정으로 잘 된다.** 진짜 원인은 그 방송에 **화면이 없었던** 것이었다.

⚠️ **교훈:** 사실 두 개(183 의 뜻 · ffmpeg 에 Cookie 헤더가 없다는 것)를 확인하고,
그 둘이 그럴듯하게 맞물린다는 이유로 **원인을 단정했다.** 정황이 맞는 것은 원인이 아니다.
`@handle/live` 를 공개 스트림으로만 확인하고 일반화한 것과 같은 실수다 (위).
`clipError` 는 이제 **실제로 겪은 원인을 첫 번째로** 적는다.

##### 그래서: **영상을 먼저 요구하고, 없으면 소리만 잘라준다**

문제는 "음성만" 인 것이 아니라 **그걸 조용히 내주는 것**이었다. 소유자 지적:

> "음성만 녹화된 경우도 음성파일로 추출되게 할수는 없어? 영상이 반드시 있어야만하나?"

된다. 그리고 훨씬 싸다 (실측, 15초 구간):

| | 걸린 시간 | 용량 | 결과 |
|---|---|---|---|
| 영상 | 14.0초 | 2.4 MB | `.mp4` (H.264 · copy) |
| **소리만** | **4.3초** | **238 KB** | `.m4a` (AAC · copy) |

두 단계로 한다.

1. 포맷 선택식 맨 뒤의 `/b`(무엇이든)를 빼고 `b[vcodec!=none]` 로 **영상을 반드시 요구**한다.
   영상이 없으면 yt-dlp 가 이렇게 답한다(실측):
   `ERROR: Requested format is not available. Use --list-formats …`
2. 그 답을 받으면(`looksLikeNoVideo`) **`ba[ext=m4a]/ba` 로 다시** 시도한다.
   만들어지면 답에 **`🎧 소리만 잘라냈습니다`** 를 붙인다.

⚠️ **처음부터 소리만 받으면 안 된다.** 그러면 영상이 있는 방송에서도 조용히 소리만 내주고,
사람은 뭘 받았는지 모른다. **영상을 먼저 요구하고 "없다" 는 답을 받은 뒤에만** 내려간다.
`/b` 를 되살리는 것도 같은 이유로 금지다 — 그건 이 두 단계를 한 단계로 뭉개는 것이다.

⚠️ **소리만일 때 `--merge-output-format mp4` 를 주지 마라.** 소리를 mp4 로 감싸 버려서
브라우저가 영상처럼 열려다 **검은 화면**을 보여준다. `.m4a` 로 두는 것이 맞다.

⚠️ 그래서 **결과 파일의 확장자를 가정할 수 없다.** `findProduced()` 가 `<stem>.*` 에서
아는 확장자(`VIDEO_EXT`/`AUDIO_EXT`)를 찾는다. `cleanLeftovers` 도 `.mp4` 만 남기게
짜면 **성공한 음성 클립을 지운다.**

⚠️ 웹페이지는 소리 클립을 **`<audio>`** 로 보여준다. `<video>` 로 보여주면 검은 화면이 뜨고
사람은 "재생이 안 된다" 고 생각한다. `listClips()` 가 항목마다 `audio` 를 알려준다.

##### 183 의 뜻과, 확인해둔 다른 사실

183 이 무엇인지 계산하고 실측으로 확인했다 (2026-09-03):

```
AVERROR_INVALIDDATA = -1094995529 → & 0xFF = 183
$ ffmpeg -i garbage.mp4 …  →  "Invalid data found when processing input" · 종료코드 183
```

**즉 ffmpeg 이 받아온 것이 영상이 아니었다.** 왜 그런지 yt-dlp 를 `-v` 로 돌려
ffmpeg 명령줄을 직접 봤다:

```
[debug] ffmpeg command line: … -headers "User-Agent: Mozilla/5.0 …" -ss 30 -t 3 -i "https://…googlevideo.com/videoplayback?…"
```

**`Cookie` 헤더가 없다.** `--cookies` 는 yt-dlp 의 *추출*에만 쓰이고
**ffmpeg 의 미디어 요청에는 전달되지 않는다.**

⚠️ 이건 **참인 사실이지만 위 사고의 원인은 아니었다.** 지금 이 서버에서 클립이 잘 되므로,
이 경로는 쿠키 없이도 통한다는 뜻이다. 나중에 정말 막히면 이 사실이 출발점이 된다 —
**그때 확인할 것**으로 남겨둔다. (3.1 의 `MUSIC_DIRECT_STREAM=false` 와 같은 계열의 문제다)

만약 쿠키 때문에 막히는 날이 오면 고를 수 있는 길:

| 방법 | 되는가 | 대가 |
|---|---|---|
| `YTDLP_EXTRA_ARGS` 로 다른 유튜브 클라이언트 시도 | **모름** | 없음. 코드를 안 고치고 시험 가능 |
| `--downloader-args` 로 ffmpeg 에 쿠키 넘기기 | 될 것 | ⚠️ **쿠키 값이 명령줄에 실린다.** 이 프로젝트는 "쿠키는 경로만" 을 지켜왔다(3.1-4) |
| 클립을 포기 | — | 타임라인 텍스트는 그대로 된다 |
| 전체를 받아서 자르기 | 될 것 | 3시간 방송이면 수 GB. 1코어·작은 디스크에 불가 |

**진단은 서버에서 이 한 줄이면 끝난다.** ffmpeg 이 남긴 진짜 이유가 보인다:

```bash
~/.venv-ytdlp/bin/yt-dlp -v --no-progress --ignore-config --no-playlist \
  --ffmpeg-location /usr/bin/ffmpeg --cookies ~/cookies.txt \
  --download-sections "*30-45" \
  -f "bv*[height<=720][vcodec^=avc1]+ba[ext=m4a]/b[height<=720]" \
  --merge-output-format mp4 -o /tmp/t.%\(ext\)s "<그 방송 주소>" 2>&1 | tail -40
```

⚠️ 그래서 `clips.js` 는 실패할 때 **원문을 통째로** 로그에 남긴다
(`[stream] 클립 실패 원문`). 예전에 첫 줄만 남겨서 `code 183` 만 보이고
진짜 이유가 버려졌다. `runOnce` 가 `err.stderr` 에 원문을 붙여준다.

##### ★ 묶음 ffmpeg 이 **죽는** 서버가 있다 (소유자 서버 실측)

소유자 서버(1코어 ARM)에서 클립을 뽑을 때 이렇게 났다:

```
ffmpeg exited with code -11
```

**음수 코드는 신호 번호다.** -11 = SIGSEGV(세그폴트) — 정상 실패가 아니라 **죽은 것**이다.
(-9 = SIGKILL, 대개 메모리 부족. -6 = abort)

같은 `ffmpeg-static` 바이너리로 **음악은 잘 돌아간다**(오디오 변환). 그래서 바이너리 자체가
아니라 그 코드 경로(HTTPS 구간 받기)의 문제로 보이지만, **원인은 확정하지 못했다.**
그래서 원인을 짐작해 안내에 적는 대신 **다른 ffmpeg 으로 넘어간다** (3.1-4).

- 후보 순서: `FFMPEG_PATH`(있으면 **이것만**) → 묶음 → `/usr/bin/ffmpeg` → `/usr/local/bin/ffmpeg` → `ffmpeg`
- 켤 때 `pickFfmpeg()` 이 `-version` 으로 훑어 쓸 수 있는 것을 골라 **로그에 찍는다.**
  안 그러면 "클립 버튼을 눌렀는데 안 된다" 로만 알게 된다.
- ⚠️ **`-version` 이 통해도 실제 구간 받기에서 죽을 수 있다** (실제로 그랬다).
  그래서 `clips.js` 가 `looksLikeFfmpegCrash()` 로 **죽음만 골라** 다음 후보로 한 번 더 시도한다.
- ⚠️ **정상 실패(코드 1)에 이걸 하면 안 된다.** 아무 오류에나 ffmpeg 을 갈아치우게 된다.
  그래서 판정을 "음수 코드 / segfault 문구" 로 좁혔다.
- `FFMPEG_PATH` 를 사람이 정했으면 **넘기지 않는다.** 정해준 것을 봇이 갈아치우면 안 된다.
- 다시 시도하기 전에 **반쪽 파일을 치운다.**

실측 (집 PC, 2026-09-03):

```
15초 구간 → 14.0초 · 2.4MB · Stream #0:0 -> #0:0 (copy) · h264 (avc1) 1280x720
```

##### ★ 길이 상한이 **용량** 상한이다

720p 는 초당 약 0.19MB 다. 개수 상한(`STREAM_CLIP_PER_SESSION`·`_TOTAL`)은
**바이트를 전혀 막지 못한다** — 한 번에 30분(약 340MB)을 요청할 수 있다.

그리고 디스크가 차면 **사진 자동 정리(3.6-5)가 돈다.** `IMAGE_MIN_FREE_GB` 안전장치는
디스크 여유로 발동해서 **사진을 지운다.** 클립이 원인인데 사진이 지워진다 —
그 파일 머리말이 경고하는 바로 그 실패다.

그래서 `STREAM_CLIP_MAX_SEC`(기본 180초)를 모달 처리에서 검사한다. **이걸 풀지 마라.**

##### 경로

뿌리는 `data/clips/` 다. **`data/` 에 두면 안 된다** — `settings.json` · `streams.json` ·
`panels.json` 이 상위 경로 한 칸 거리에 놓인다.

폴더 이름은 세션 id 이고 무작위 소문자·숫자다. 그래서 **이름을 다듬는 대신 모양을 검사한다**
(`/^[a-z0-9]{4,12}$/`). `safeFolderName` 보다 훨씬 좁다.
`src/images/store.js` 를 고쳐 쓰지 말고 여기 따로 둔다 — 사진 경로는 이 저장소에서 가장 위험한 곳이다.

실패하면 **반쪽 파일을 치운다.** `runOnce` 가 제한시간에 자식을 죽이면 `.part`·`.ytdl` 이 남는다.
`--force-overwrites` 는 *다음* 실행 이야기라 도움이 안 된다. 목록은 `.mp4` 만 본다.

##### 시간 칸도 `parseElapsed` 를 쓴다

⚠️ `parseElapsed` 는 **숫자만 적으면 분으로 본다** (`90` → 90분). 클립 구간에서는 위험하다.
그래서 창의 기본값과 안내를 **`01:20:30` 형식으로 채워** 그 길로 가지 않게 한다.

##### 공용 제한시간 사다리를 오염시킨다

`noteSuccessDuration` / `noteTimeout` 은 `ytdlp.js` 의 모듈 전역이다. 90초짜리 클립
다운로드가 `slowestOkMs` 를 올리고, 그게 `timeoutLadder()` 에 먹는다.

지금은 망고의 모든 호출(`liveInfo`·`downloadSection`)이 제한시간을 직접 넘기므로 문제가 없다.
**망고에서 yt-dlp 를 새로 부를 때는 반드시 `timeouts` 를 함께 넘겨라.**

##### 웹페이지는 갤러리와 같은 경계

`/c/:folder`(목록) · `/clip/:folder/:file`(재생) · `/cdl/:folder/:file`(받기) ·
`POST /api/clip-delete`(삭제, `WEB_TOKEN`).

보기·받기는 주소를 알면 누구나 — 라이브 자체가 일부공개라서 클립을 더 잠글 이유가 없다(소유자 확인).
`res.sendFile` 을 쓰면 **Range 요청이 자동 처리**되어 재생 중 건너뛰기가 된다(206 확인).

일괄 선택·일괄 다운로드는 만들지 않았다. 한 개가 수 MB 라 여러 개를 한꺼번에 받을 일이 없다.
(사진과 다르다 — 3.5 의 ZIP 논쟁이 여기엔 해당하지 않는다)

#### 구글 드라이브 (`src/stream/drive.js`) — **미검증. 더하기만 한다**

⚠️ **이 파일은 실제 구글 API 로 검증하지 못했다** (2026-09-03). 이 기능의 다른 부분은
전부 돌려보고 만들었지만(`--download-sections`, `@handle/live` 해석), 드라이브는
자격증명이 필요해서 그러지 못했다. `verify` 는 `fetch` 를 가로채 **"우리 코드가 의도한
요청을 만드는가" 만** 본다 — 구글이 그 요청을 받아주는지는 확인되지 않았다.
`1039개 통과` 가 이 부분까지 보증하지 않는다.

그래서 두 가지를 지킨다.

- ★ **로컬 파일을 지우지 않는다.** 사본만 올린다. `GDRIVE_DELETE_LOCAL` 같은 것을
  만들지 마라 — 올렸다고 해놓고 반쪽만 갔을 때 원본까지 지우면 클립이 사라지고
  웹페이지가 404 가 된다. 되돌릴 방법이 없다. 로컬 파일은 예산·개수 상한이 관리한다.
  → **드라이브는 더하기만 하는 기능이다.** 안 되면 답에 오류 한 줄이 붙고 끝이다.
- **구글이 한 말을 원문으로 보여준다.** 원인을 지어내지 않는다(3.1-4).
  처음 쓸 때 오류가 나면 그 원문이 답이다.

`googleapis` 패키지는 쓰지 않는다. REST 세 번(토큰 → 업로드 → 권한)이면 되는 일에
의존성을 늘릴 이유가 없다 — TMDB·제미나이도 `fetch` 만 쓴다.
클립은 180초 상한(약 34MB)이라 `uploadType=multipart` 한 번으로 충분하다
(이어올리기는 필요 없다).

#### 화면 만드는 함수는 `panel.js` 에 둔다

모달을 핸들러 안에서 조립하면 **verify 가 `toJSON()` 을 불러볼 수 없다.**
`buildOffsetModal` / `buildDescModal` / `buildSummary` / `buildStreamPanel` 을
전부 `panel.js` 에서 export 하고, verify 6x 의 화면 목록에 넣었다. (규칙 7-1)

설명 채우기 창에서 특히 두 가지:

- 칸마다 **`setRequired(false)`** — 안 하면 5칸 중 2칸만 채운 사람이 빠져나갈 수 없다.
- 설명이 없는 마킹에는 **`setValue` 를 아예 호출하지 않는다** — `setValue('')` 는
  7-1 의 조용한 실패다. 창이 안 뜨고 버튼이 고장난 것처럼 보인다.

### 3.6-6a 나만 보이는 화면을 **공지로** — 📢 버튼 (`share.js`)

> "/도움말 같은 걸 한번씩 전체공지하고 싶다. 자주는 아니라서 명령어를 추가하긴 싫다."

세 가지를 놓고 골랐다.

| 방법 | 왜 안 골랐나 / 골랐나 |
|---|---|
| `/도움말 공개:true` 인자 추가 | 명령어마다 인자가 늘고, **그 인자가 있다는 걸 알아야** 쓴다. 자주 안 쓰는 기능일수록 잊힌다 |
| `/공지` 명령어 추가 | 소유자가 원하지 않았고 3.6-6 에도 어긋난다 |
| ★ **그 화면에 버튼** | 화면을 본 사람에게만 보이고, 누르면 그 자리에서 올라간다. **외울 것이 없다** |

**상태를 저장하지 않는다.** 누른 그 메시지의 글·임베드를 그대로 다시 올린다.
그래서 어느 화면에든 `withShareButton()` 한 줄로 붙고, 화면을 고쳐도 `share.js` 는
손댈 일이 없다.

⚠️ **버튼·드롭다운은 같이 올리지 않는다.** 그건 누른 사람의 나만 보이는 화면에 묶인
   것이라 남이 눌러도 동작하지 않는다. 올리는 것은 글과 임베드뿐이다.
⚠️ **알림을 쏘지 않는다** (`SuppressNotifications` + `allowedMentions.parse: []`).
   공지라도 자고 있는 사람을 깨울 이유는 없다.
⚠️ 올린 뒤 **버튼을 없앤다.** 두 번 올리면 채팅방이 지저분해진다.
⚠️ 줄이 5개면 **버튼을 포기한다.** 있으면 좋은 기능이 원래 화면을 깨뜨리면 안 된다.
⚠️ 어느 기능에도 속하지 않아 **기능을 꺼도 동작한다** (`/도움말` 과 같은 취급).

지금 붙은 곳: `/도움말`, `/축약어` 목록. 더 필요하면 그 화면에서 한 줄 감싸면 된다.

### 3.6-6 명령어 수를 의도적으로 줄였다 (30 → 17)

소유자 요청: "명령어가 과하게 많다". **버튼으로 되는 것을 명령어로도 만들어둔 중복**이 대부분이었다.

없앤 것과 그 대체 수단:

| 없앤 명령어 | 대신 쓰는 것 |
|---|---|
| `/다음` `/정지` `/일시정지` `/이어재생` `/반복` `/이전곡` | `/대기열` 제어판 **버튼** |
| `/대기열제거` `/대기열비우기` | 제어판 🗑️ **드롭다운** |
| `/목소리목록` | `/목소리` 선택지에 14종이 다 나옴 |
| `/폴더확인` | `/폴더` 를 인자 없이 실행 |
| `/채널확인` `/채널해제` | `/채널설정` 을 인자 없이 실행 (상태 + 해제 버튼) |
| `/핑` | `/도움말` 이 응답하면 살아있는 것 |

**원칙: 같은 일을 두 가지 방법으로 제공하지 않는다.** 비개발자 친구들은 버튼만 쓰고,
명령어 목록이 길면 오히려 뭘 써야 할지 모른다.

`/순서이동` 은 남겼다 — 드롭다운으로는 "맨 앞으로" 밖에 안 되므로 유일한 정밀 조작 수단이다.
`/나가기` 도 남겼다 — 제어판이 안 보이는 상황에서 봇을 내보낼 방법이 필요하다.

**되살리지 말 것.** `verify.mjs` 가 없앤 13개가 다시 나타나지 않는지 검사한다.

### 3.8-1 비공개 채널을 `/채널설정` 으로 지정하기

> 소유자: "채널설정은 비공개 채널에서는 안돼? 선택지에 안뜨네"

**채널 고르기 칸이 왜 그 채널을 안 보여주는지는 확정하지 못했다.** 검색으로는
클라이언트가 "사용자가 볼 수 있는 채널" 로 거르는지 "봇이 볼 수 있는 채널" 로 거르는지가
갈렸다. **추측을 코드 주석이나 안내에 적지 않는다** (3.1-4).

확실한 것은 하나다: **피커의 규칙이 무엇이든 봇이 그 채널을 볼 수 있어야 아무것도 안 된다.**
제어판을 띄우고 글을 읽으려면 접근 권한이 필요하다. 그래서 규칙을 알아내는 대신
**피커에 의존하지 않는 길**과 **조용한 실패를 없애는 것** 두 가지로 해결했다.

1. **채널 칸을 비우면 `interaction.channel` 로 지정한다.**
   그 채널에 들어가서 `/채널설정 종류:방송 채널` 만 치면 된다. 피커를 거치지 않는다.
2. **지정하는 순간 봇 권한을 검사해서 없는 것만 이름으로 알려준다** (`permissionWarnings`).

2번이 없으면 이렇게 된다: 비공개 채널을 지정하면 **지정은 성공하고**, 그다음부터
제어판이 안 뜨고 읽어주기도 안 되는데 **아무 오류도 안 난다.** 사람은 "왜 안 되지" 만 하게 된다.
조용히 안 되는 것이 제일 나쁘다.

- 권한 이름은 **디스코드 한국어판 표기**로 적는다(`채널 보기` · `메시지 보내기` · `연결` · `말하기`).
  영어로 적으면 어디를 눌러야 할지 못 찾는다.
- **있는 권한은 나열하지 않는다.** 넷을 다 적으면 무엇이 문제인지 안 보인다.
- 종류에 따라 다른 것을 본다 — 텍스트는 전송·기록·링크, 음성은 연결·말하기,
  카테고리는 검사할 것이 없다.
- 봇 정보를 못 읽으면 **조용히 넘긴다.** 엉뚱한 경고보다 침묵이 낫다.

### 3.7-2 관리자 전용 명령어는 **디스코드가 막는다**

서버 설정을 바꾸거나 데이터를 지우는 명령어는 아무나 쓰면 안 된다.

| 명령어 | 왜 |
|---|---|
| `/채널설정` | 서버 전체 설정을 바꾼다 |
| `/기능` | 기능을 통째로 끌 수 있다 |
| `/정리` | **사진을 영구히 지운다** (3.6-5) |

`setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild)` 를 쓴다.
**코드에서 검사하지 않는다** — 명령어를 추가할 때 반드시 빠뜨리게 된다(3.7-1 과 같은 발상).
디스코드가 목록에서 아예 숨겨주므로 친구들이 실수로 누를 일도 없다.

서버 주인이 특정 사람에게 열어주고 싶으면 **서버 설정 → 연동** 에서 명령어별로 바꿀 수 있다.
우리가 그 UI 를 만들 필요가 없다.

⚠️ 반대로 **친구들이 늘 쓰는 것은 막으면 안 된다.**
`/일정` `/일정새로` `/정산` `/투표` `/영화` `/타이머` `/목소리` `/갤러리` 는 누구나 쓴다.
`verify` 가 양쪽을 다 검사한다 — 실수로 막아도 걸린다.

### 3.7-1 기능 on/off — 차단은 한 곳에서만

`settings.js` 의 `FEATURES` / `featureEnabled()` / `setFeature()`, 패널은 `feature-commands.js`.

**왜 필요한가**: 소유자 서버에 이미 다른 음악봇·TTS봇이 있어서 같은 링크에 둘이 반응해 겹친다.
그때 서버에 SSH로 들어가 프로세스를 끄는 것은 현실적이지 않다.

**차단은 세 지점에서만 한다. 명령어마다 검사하지 않는다.**

1. **슬래시 명령어** — `commands.js` 가 모듈 단위로 `tag('music', musicCommands)` 처럼
   `feature` 를 붙이고, `index.js` 가 `command.feature` 를 보고 한 번에 막는다.
   명령어를 새로 추가해도 표를 고칠 일이 없다.
   **명령어 안에서 각자 검사하게 만들지 말 것** — 반드시 빠뜨리는 것이 생긴다.
2. **버튼** — `customId` 접두(`m:`=음악, `t:`=타이머, `f:`=기능패널)로 판단.
   `f:` 는 **항상 통과해야 한다.**
3. **메시지 핸들러** — 각 `handleXxxMessage()` 첫 줄에서 `featureEnabled` 확인.

**`/기능` `/채널설정` `/핑` `/도움말` 에는 태그를 붙이지 않는다.**
전부 꺼놓은 상태에서 되살릴 방법이 없으면 안 되기 때문이다. 이건 실수로 태그가 붙기 쉬우니
`verify.mjs` 가 "태그 없음"을 검사한다.

**부수 효과 처리**
- 음악을 끄면 `peekGuildAudio(g)?.stop()` — 안 하면 껐는데도 계속 흘러나온다.
- 음악·읽어주기가 **둘 다** 꺼지면 음성채널에서 나간다. 아무것도 안 하면서 앉아 있을 이유가 없다.
- 이미지를 끄면 **새 저장만** 멈춘다. 웹 갤러리는 이미 모아둔 파일을 보여주는 별개 서비스이므로
  계속 동작한다 (의도적).

**기본값은 "켜짐"** 이고, 명시적으로 끈 것만 `data/settings.json` 에 `features: {key: false}` 로
저장한다. 이렇게 하면 새 기능을 추가해도 기존 서버에서 자동으로 켜진다.

### 3.8 설정은 명령어가 `.env` 를 덮어쓴다

`settings.js` 가 채널 설정의 단일 진입점이다. 우선순위는:

1. `/채널설정` 으로 지정한 값 — `data/settings.json`, 서버(길드)별로 저장
2. `.env` 값
3. 없음 → 그 기능은 꺼진 것으로 취급

**이 구조의 위험은 "조용한 무시"다.** 사용자가 `.env` 를 고치고 재시작했는데
명령어 설정이 이미 있으면 아무 일도 안 일어난다. 에러도 없다.
그래서 `getWithSource()` 가 값과 **출처**를 함께 돌려주고, `/채널확인` 과 시작 로그가
항상 `명령어로 지정` / `.env 기본값` 을 같이 표시한다. **이 표시를 없애지 말 것.**

부수 효과: 채널이 런타임에 바뀔 수 있으므로 **슬래시 명령어는 기능 on/off와 무관하게 전부 등록한다.**
(예전에는 `.env` 가 비면 해당 모듈 명령어를 등록하지 않았는데, 그러면
"설정하려는데 설정할 명령어가 없는" 상태가 된다.)

기능 on/off 판정도 `config` 가 아니라 `ttsEnabled(guildId)` / `imagesEnabled(guildId)` 를 쓸 것.

### 3.9 슬래시 명령어는 길드 단위로 등록한다

`deploy-commands.js`가 `Routes.applicationGuildCommands(clientId, guildId)`를 쓴다.
전역(`applicationCommands`) 등록은 디스코드 전파에 최대 1시간이 걸려서,
초보자가 "봇이 고장났다"고 오해하는 대표적인 지점이다. 개인용 단일 서버이므로 길드 등록이 옳다.

---

### 3.6-10 게임 검색 · 포럼 연결 (`src/game/`)

친구용 방송/클립 사용 안내는 `/guide/stream`에서 공개 HTML(`src/web/guide/stream.html`)로 제공합니다.
정적 캡처만 `/guide/assets`에 공개하며 폴더 열람·쓰기·업로드 API는 추가하지 않습니다.
외부 폰트/분석/영상 임베드 요청 없이 동작하고, 캡처가 없으면 이미지 로딩 실패 안내가 남습니다.
현재는 사용자가 제공한 실제 PNG 13장을 원본 그대로 연결했습니다. 클릭하면 원본을 새 탭에서 엽니다.
로딩 전 숨긴 이미지에는 lazy 로딩을 쓰지 않습니다. 숨김 상태에서는 로드가 시작되지 않아 대체 안내만 남을 수 있습니다.
1단계 3장, 나머지 각 2장입니다. 클립 선택 목록→입력창 순서와 작업 소요시간/영상 길이 구분을 캡션에 명시합니다.
이미지 로딩 실패 시 대체 안내를 표시합니다. 공개 정적 파일이므로 캡처 속 방송 URL·서버 주소·표시 이름도 노출됩니다.
6단계·FAQ·운영자 준비·캡처 교체 설명을 제공하며 모바일 레이아웃과 인쇄 스타일을 포함합니다.
기본 클립 설정값은 가이드에 기본값으로 명시하며, 실제 입력창의 서버 설정 안내가 우선입니다.
웹 첫 화면에서 접근 가능하고 기존 관리자/삭제 인증 경계는 변경하지 않습니다.

Steam 검색 결과는 게임의 안정적인 키(`steam:<appid>`)로 저장하고, 목록에 없는 게임은
정규화한 이름 키(`name:<이름>`)를 씁니다. 검색 API가 실패해도 직접 입력 경로는 살아 있어야 합니다.

`data/forum-posts.json`은 길드별로 게임 키와 기존 스샷·녹화 포스트 ID를 연결합니다.
**포스트 제목으로 추측하지 않습니다.** 별칭이 많아 조용히 잘못 연결되는 것보다,
포스트 안에서 스레드 관리자가 `/게임 검색:<게임>`을 한 번 실행하는 수동 연결이 안전합니다.
봇은 새 포스트를 만들지 않습니다.

`data/game-aliases.json`은 길드별 게임 정보와 포럼 제목 별칭을 저장합니다. 사람이 포스트에서
Steam 게임을 확정한 **뒤에만** 그 제목을 별칭으로 기억하므로, 제목만 보고 게임을 추측하는
위험은 없습니다. 검색할 때는 NFKC 정규화 뒤 띄어쓰기·문장부호·이모지를 빼고 비교합니다.
로컬 별칭 결과를 먼저 내므로 한 번 연결한 게임은 `/게임`과 `/방송`에서 한글로 즉시 찾습니다.
같은 별칭이 여러 게임에 붙으면 정확 일치도 자동 확정하지 않고 자동완성에서 직접 고르게 합니다.

방송 게임은 세션 전체가 아니라 `session.streams[]`의 **방송자별 값**입니다. 같은 시각에
한 세션에 들어와도 서로 다른 게임일 수 있기 때문입니다. 종료 시 각 방송은 자기 게임의
녹화 포스트로 가며 방송자, 시작 날짜(`:d`), 라이브 링크와 타임라인을 남깁니다.
녹화방 원본 URL은 `<URL>`로 감싸지 않아 Discord의 유튜브 미리보기(썸네일)를 허용합니다.
별도 썸네일 다운로드/API 호출/커스텀 임베드는 없으며, 방송 채널 요약판의 링크 표시 방식은 유지합니다.
링크는 첫 페이지에만 실리며 설명 수정/지난 요약판 다시 올리기로 이전 글에도 반영됩니다.
실제 미리보기 렌더링은 Discord 설정·링크 임베드 권한·유튜브 응답에 의존하므로 자체 검사만으로 보장하지 않습니다.
마지막 페이지에는 `tm:clipsopen:<session>:<user>` 클립 추출 버튼을 붙입니다.
`buildClipPicker()`는 본인 전용 선택 화면이며, `tm:pickpage`로 그 화면만 갱신합니다.
녹화방 원문을 `buildSummary()`로 덮어쓰지 않습니다. 선택→기존 `tm:clip`/`tm:clipm` 경로로 연결되어
방송자별 원본, `post_live` 검사, 클립 상한을 그대로 적용합니다. 마킹이 없으면 안내만 표시합니다.
종료 요약판은 드롭다운 대신 진입 버튼을 표시합니다. 레거시 `tm:cpage`는 기존 요약판 갱신 경로로 남깁니다.
포럼 동기화 payload는 `components: []`도 명시해 페이지 수 변경 시 이전 마지막 장의 버튼을 없앱니다.
이전 녹화 게시물은 설명 재제출 또는 `/방송`의 지난 요약판 다시 올리기로 갱신합니다. 재시작 시 일괄 수정은 하지 않습니다.
클립 선택 화면은 재열기 시 최신 저장 기록으로 ✅와 보기 링크를 구성합니다. 디스코드 실기기 확인은 별도입니다.
설명 수정 시 방송 요약판과 녹화 포스트를 함께 갱신합니다. 공유 마킹을 포함한 다른 방송자도 대상입니다.
`recordPages()`는 헤더·코드블록까지 2,000자 내로 분할합니다. 저장된 메시지는 edit하고 부족한
페이지만 send, 남는 페이지만 delete합니다. 성공한 ID를 단계별 저장하고 `forumPosted.complete`로
완료 여부를 기록하므로 부분 실패도 재연결/설명 재제출로 복구합니다. 같은 방송의 요청은 순차 실행합니다.
이전 날짜·링크 전용 게시물도 설명 수정/재종료 시 같은 ID를 유지하며 타임라인을 붙입니다.
동기화 실패 시 설명은 보존하고 사용자에게 권한 확인과 재제출을 안내합니다.
연결이 없거나 전송이 실패하면 `forumPosted`를 기록하지 않아 보류 상태가 유지되고,
나중에 녹화 포스트를 연결하면 같은 게임의 닫힌 세션을 다시 훑어 전송합니다.

스샷 포럼 아래 포스트는 별도 이미지 채널 허용 목록 없이 이미지 저장 대상으로 봅니다.
단, 명시적 제외 채널은 계속 우선합니다. 협동 여부를 이용한 자동 세션 분리는 후속 기획입니다.

수동 별칭은 `/게임 별칭:…` 옵션으로 스레드 관리자가 추가합니다. 제미나이 호출은 없습니다.

단순 확인 응답은 `src/notices.js`가 사용자·채널·앱별 최신 한 개로 유지합니다.
전송 성공 뒤 이전 확인만 지우며, 오류·링크·임베드·컴포넌트가 있는 화면은 제외합니다.
상호작용 토큰 만료 전에 타이머로 마지막 확인도 지웁니다. 재시작 전 메시지/만료 토큰으로
보낸 과거 메시지는 복원하거나 소급 정리하지 않습니다.

## 4. 불변조건 (Invariants) — 어기면 조용히 깨진다

1. **`GuildAudio.subscribeTo()`를 거치지 않고 `connection.subscribe()`를 직접 부르지 말 것.**
   3.3절 참고. 소리가 깨진다.
2. **디스크에 경로를 쓰기 전 반드시 `store.js`의 `folderPath()` / `filePath()`를 통과시킬 것.**
   이 두 함수가 `..`, 드라이브 문자, Windows 금지문자를 걷어내고,
   최종 경로가 `data/images` 안쪽인지 `startsWith` 로 다시 검사한다.
   웹 갤러리의 `/img/:folder/:file`, `/dl/...`이 사용자 입력을 그대로 받으므로
   **이 검사를 우회하면 서버의 임의 파일이 읽힌다.**
3. **`MessageContent` 인텐트가 없으면 `message.content`가 빈 문자열이 된다.**
   개발자 포털에서 켜야 하고, 코드는 정상인데 TTS와 링크 감지만 조용히 죽는다.
   증상이 "에러는 없는데 아무 반응이 없다"라면 여기부터 볼 것.
4. **`libsodium-wrappers`를 지우지 말 것.** 음성 암호화용이고, 없으면 음성 연결 시점에
   예외가 난다. 순수 JS라 Windows에서 빌드툴이 필요없다(`sodium-native`는 컴파일러가 필요).
5. **ESM이므로 상대 import에 `.js` 확장자를 반드시 붙일 것.**
6. **`.env`를 절대 커밋하지 말 것.** 토큰이 노출되면 디스코드가 자동으로 무효화한다.
7. **슬래시 명령어를 추가·개명하면 반드시 `npm run deploy` 를 실행할 것.**
   `PUT applicationGuildCommands` 는 명령어 집합을 통째로 교체하므로 옛 이름은 자동으로 사라지지만,
   deploy 를 안 돌리면 디스코드에는 옛 이름이 그대로 남아 "고쳤는데 안 바뀐다"가 된다.
8. **채널 설정을 읽을 때 `config.*` 를 직접 보지 말고 `settings.js` 를 거칠 것.**
   `config` 만 보면 `/채널설정` 으로 지정한 값이 무시된다. 3.8절 참고.
9. **`/목소리` 선택지에 새 목소리를 넣으면 `npm run verify` 로 실재를 확인할 것.**
   Azure 카탈로그에는 있어도 Edge TTS 무료 엔드포인트에는 없는 목소리가 많다. 3.4절 참고.
10. **숨김 응답은 `ephemeral: true`가 아니라 `flags: MessageFlags.Ephemeral`로 쓸 것.**
   discord.js 14.27에서 `ephemeral` 옵션은 deprecated 되어, 쓸 때마다 콘솔에 경고를 찍는다
   (`InteractionResponses.js`에서 확인). 동작은 하지만 경고가 도배되면 소유자가
   "뭔가 고장났다"고 오해한다. 새 명령어를 추가할 때도 `MessageFlags`를 import 해서 쓸 것.

---

## 5. 상태 모델: `GuildAudio`

서버 ID → `GuildAudio` 인스턴스를 `guild-audio.js`의 모듈 전역 `registry` Map이 들고 있다.
**서버 재시작하면 전부 사라진다. 큐는 의도적으로 영속화하지 않는다.**

```
없음 ──getGuildAudio()──► 생성됨 ──connect()──► Ready
                                                  │
                            ┌─────────────────────┼──────────────────────┐
                            │                     │                      │
                     playNext() 루프         speak() 체인          leaveTimer
                     (musicPlayer)          (ttsPlayer)          (기본 300초)
                            │                     │                      │
                            └─────────────────────┴──────────────────────┘
                                                  ▼
                                            destroy() → registry에서 제거
```

- `destroy()`는 여러 번 불릴 수 있다(`destroyed` 플래그로 방어). 타이머·자식 프로세스·
  커넥션을 전부 정리한다.
- `peekGuildAudio()`는 파괴된 인스턴스를 `null`로 걸러준다. **명령어 핸들러는
  `getGuildAudio()`가 아니라 `peekGuildAudio()`를 써야 한다** — 안 그러면 "재생 중이 없다"고
  답해야 할 상황에서 빈 인스턴스를 새로 만든다.
- `killCurrent`는 현재 곡의 yt-dlp + ffmpeg 자식 프로세스를 죽이는 함수다.
  곡을 넘길 때 반드시 호출해야 좀비 프로세스가 안 쌓인다.
- TTS는 `ttsChain` 프로미스 체인으로 **직렬화**된다. 동시에 여러 메시지가 와도
  한 번에 하나씩 순서대로 읽는다. 이걸 없애면 소리가 겹쳐서 뭉개진다.
- `speak(makeStream, targetChannelId)`의 두 번째 인자는 **줄 서 있는 동안 봇이 다른
  음성채널로 옮겨갔는지** 확인하기 위한 것이다. `TTS_VOICE_CHANNEL_ID`가 비어 있으면
  "말한 사람이 있는 음성채널"로 따라가므로, A채널 사용자와 B채널 사용자가 연달아 글을 쓰면
  커넥션이 옮겨간다. 이때 앞 문장을 그대로 재생하면 **엉뚱한 채널에서 읽고, 음악도 엉뚱한
  곳에서 재개된다.** 그래서 채널이 바뀌었으면 그 문장은 버린다. 이 가드를 지우지 말 것.

---

## 6. 데이터 형식

### `data/images/_meta.json`
키는 `"<폴더명>/<파일명>"`.
```json
{
  "2026-08-31/20260831-141900_photo.png": {
    "folder": "2026-08-31",
    "file": "20260831-141900_photo.png",
    "originalName": "photo.png",
    "size": 184320, "width": 1920, "height": 1080,
    "author": "someone", "authorId": "123...",
    "channelId": "222...", "messageId": "333...",
    "messageUrl": "https://discord.com/channels/...",
    "uploadedAt": "2026-08-31T14:19:00.000Z"
  }
}
```
파일이 지워져도 항목이 남을 수 있다. `listFiles()`는 **디스크를 진실의 원천으로 삼고**
메타데이터는 있으면 붙여주는 방식이라, 불일치가 있어도 갤러리는 정상 동작한다.

쓰기는 `writeChain` 프로미스 체인으로 직렬화한다(동시 저장 시 JSON 깨짐 방지).

### `data/settings.json`
```json
{
  "<길드ID>": {
    "musicTextChannelId": "<채널ID>",
    "musicVoiceChannelId": "<채널ID>",
    "ttsTextChannelId": "<채널ID>",
    "ttsVoiceChannelId": "<채널ID>",
    "imageChannelIds": ["<채널ID>", "..."]
  }
}
```
키 목록은 `settings.js` 의 `KEYS` 가 단일 출처다. 설정을 추가하려면 거기에만 넣으면
`/채널설정` 선택지와 `/채널확인` 표시가 자동으로 따라온다.

### `data/images/_folders.json`
```json
{ "<채널ID>": "<폴더명>" }
```

### Track 객체 (`ytdlp.js` → `guild-audio.js`)
```js
{ title: string, url: string, duration: number|null, uploader: string|null, thumbnail: string|null }
```
큐에는 `{ track, requestedBy }` 형태로 들어간다.

---

## 7. 알려진 함정 (Gotchas)

| 증상 | 원인 | 대처 |
|---|---|---|
| 음악이 갑자기 전부 안 나옴 | 유튜브가 추출 방식을 바꿈 | `npm run update-ytdlp` |
| `The page needs to be reloaded` | 유튜브의 일시적 거부 | 자동 재시도됨(아래 참고). 계속되면 update-ytdlp |
| `Sign in to confirm you're not a bot` | 데이터센터 IP 차단 (AWS/GCP/Oracle에서 흔함) | `.env`의 `YTDLP_COOKIES_FILE`에 쿠키 파일 지정 |
| TTS·링크감지가 에러 없이 무반응 | `MessageContent` 인텐트 꺼짐 | 개발자 포털에서 켜고 재시작 |
| 슬래시 명령어가 안 보임 | `npm run deploy` 안 함 / 봇이 `applications.commands` 없이 초대됨 | 재초대 후 deploy |
| 명령어 이름이 옛것으로 보임 | 개명 후 deploy 안 함 | `npm run deploy` |
| `.env` 를 고쳤는데 반영 안 됨 | `/채널설정` 값이 우선함 | `/채널확인` 으로 출처 보고 `/채널해제` |
| 일본어 TTS가 무음 | 한국어 전용 목소리 | 다국어 목소리로 변경 (3.4절) |
| 음성 연결 시 예외 | `libsodium-wrappers` 누락 | `npm install` |
| 소리가 깨지고 겹침 | 두 플레이어가 동시 구독됨 | 3.3절, `subscribeTo()` 확인 |
| 갤러리에서 일부만 다운로드됨 | 클릭 간격이 짧음 / 브라우저가 다중 다운로드를 막음 | 350ms 간격 조정, 브라우저 권한 허용 |
| 이미지 폴더가 `2026 08 31`처럼 깨짐 | `safeFolderName`의 문자 클래스를 잘못 수정함 | 하이픈·공백은 **지우면 안 됨** |
| 사진이 "어제" 날짜 폴더에 들어감 | 서버 OS 시간대가 UTC | `timedatectl set-timezone Asia/Seoul` (아래 참고) |

**날짜는 현지 시각 기준이다.** `store.js`의 `localDate()` / `localStamp()`가 `getFullYear()` 등
현지 시각 메서드를 쓴다. `toISOString()`으로 되돌리지 말 것 — UTC가 되어 한국시간 오전 0~9시에
올린 사진이 전날 폴더로 들어간다. 대신 **서버 OS의 시간대를 Asia/Seoul로 맞춰야** 의도대로 동작한다.

**과거에 실제로 났던 버그**: `safeFolderName`의 정규식이 하이픈까지 치환하도록 잘못 들어가서
날짜 폴더 `2026-08-31`이 `2026 08 31`이 될 뻔했다. 현재는
`/[<>:"/\\|?*\x00-\x1f]/g` 로, **Windows 금지문자와 제어문자만** 지운다.
여기 손대면 `2026-08-31`이 그대로 유지되는지 반드시 다시 확인할 것.

---

## 7-1. ★ 빌더에 `null` · `''` 을 넘기지 말 것 — 창이 아예 안 뜬다

discord.js 빌더는 **`toJSON()` 안에서** 값을 검사한다. 잘못된 값을 넣어도 그 자리에서는
조용하고, 실제로 보낼 때 `Received one or more errors` 로 터진다.
사용자에게는 **버튼을 눌렀는데 아무 일도 안 일어나는 것**으로 보인다.

2026-09-01 실측으로 확인한 것:

| 호출 | 결과 |
|---|---|
| `LabelBuilder.setDescription(null)` | **거부** |
| `LabelBuilder.setDescription('')` | **거부** |
| `EmbedBuilder.setDescription('')` | **거부** |
| `TextInputBuilder.setValue('')` | 통과 |
| `TextInputBuilder.setPlaceholder('')` | 통과 |
| 선택지의 `description: undefined` | 통과 (키가 지워진다) |

**값이 없으면 그 메서드를 아예 부르지 않는다.**

```js
// ✗ 이렇게 하면 hint 가 없을 때 터진다
new LabelBuilder().setLabel(label).setDescription(hint ?? null)

// ✓ 있을 때만 부른다
const lbl = new LabelBuilder().setLabel(label);
if (hint) lbl.setDescription(hint);
```

`?? ` 는 `null`·`undefined` 만 잡는다. **빈 문자열은 통과**하므로 `||` 를 써야 하는 곳이 있다
(`cut(overview) || '줄거리 정보가 없습니다.'`).

### 그래서 verify 가 **화면 만드는 함수를 전부 조립해본다**

`verify.mjs` 의 6x) 절이 판·창 16개를 만들고 `toJSON()` 을 실제로 부른다.
**값이 하나도 없는 경우**(장소 없음, 할 일 없음)를 반드시 같이 넣는다 — 그때 `null` 이 흘러간다.

**화면 만드는 함수를 새로 만들면 그 목록에 한 줄 추가할 것.**
이 검사를 넣기 전에 `LabelBuilder.setDescription(null)` 이 그대로 배포됐고,
「고치기」 와 `/일정` 등록 창이 **둘 다** 안 떴다.

---

## 8. 검증 방법 (토큰 없이 가능한 것들)

디스코드 토큰 없이도 아래는 전부 확인할 수 있다. 큰 변경 후 돌려볼 것.

```bash
# 문법 검사 (전체 파일)
node --check src/index.js

# yt-dlp 동작 + 버전
./bin/yt-dlp.exe --version

# ffmpeg 인코더 확인 (libopus / opus 먹서가 나와야 함)
node -e "import('ffmpeg-static').then(m=>console.log(m.default))"
```

그리고 저장소 루트의 **`verify.mjs`** 가 나머지를 자동으로 확인한다. 코드를 고쳤으면 이걸 돌릴 것.

```bash
npm run verify
```

더미 `process.env` 값을 채운 뒤 각 모듈을 동적 import 해서 아래를 검사한다.
(마지막에 Windows에서 `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` 이
찍힐 수 있는데, `process.exit()`와 소켓 종료가 겹쳐서 나는 libuv 잡음이다.
그 위에 `✅ 전부 통과`가 찍혔으면 정상이다.)

- 슬래시 명령어 20개 전부 `data.toJSON()` 통과, `execute` 존재, 이름 중복 없음, 영문 이름 잔존 없음
- `/목소리` 선택지가 `listVoices()` 결과에 실재하고, 기본 목소리가 다국어인지
- 폴더 결정 4순위(스레드 > /폴더 > 채널명 > 날짜)가 순서대로 적용되는지
- 설정 우선순위: 명령어 지정이 `.env` 를 덮어쓰고, 해제하면 되돌아가는지
- `index.js`가 import 하는 이름이 각 모듈에 실제로 존재 (index.js는 실행하면 로그인을
  시도하므로 직접 import 할 수 없어, 소스를 정규식으로 읽어 검사한다)
- `MessageFlags.` 를 쓰는 파일이 전부 그걸 import 했는가 (누락되면 런타임 ReferenceError)
- `ephemeral: true` 잔존 없음 (4절 불변조건 7)
- `"../../Windows"` → `"Windows"`, `"C:\\Windows"` → `"C Windows"` 로 무해화되고
  최종 경로가 `data/images` 안에 머무름
- `"2026-08-31"` 이 변형되지 않음
- `resolveFolder(null, id)` → 날짜, `resolveFolder(스레드, id)` → 스레드 이름
- TTS 정제: `<@1> 야 https://youtu.be/a 봐 <:kek:9> **굵게** ㅋㅋㅋㅋㅋ`
  → `누군가 야 링크 봐 kek 굵게 ㅋㅋㅋ`
- 유튜브 링크 감지 / 일반 문장은 `null`
- 웹 갤러리: 인증 없이 401, 틀린 암호 401, 맞으면 200, 경로 탈출 요청은 4xx

**검증이 닿지 못하는 곳**: 실제 디스코드 토큰을 쓰는 모든 것.
인텐트가 실제로 켜졌는지, `npm run deploy` 가 통과하는지, `joinVoiceChannel` 이 Ready 까지
가는지, 반응(✅) 권한이 있는지는 **봇을 실제로 띄워봐야만 알 수 있다.**
README의 "잘 되는지 확인하는 순서" 1~7단계가 그 역할을 한다.

---

## 9. 아직 안 한 것 / 다음에 할 만한 것



우선순위 순.

1. **자동 테스트가 없다.** 위 8절의 검증을 `node:test`로 옮기면 좋다.
2. **큐 영속화 없음.** 재시작하면 재생목록이 날아간다. 개인용이라 의도적이지만,
   필요하면 `data/queue.json`에 저장.
3. **다중 서버는 지원한다.** 런타임 상태(`registry`, `settings.js`, TTS 설정)가 모두 길드 ID로
   분리되어 있고, `GUILD_ID` 는 쉼표 목록을 받아 `deploy-commands.js` 가 서버별로 등록한다
   (한 곳이 실패해도 나머지는 등록되고, 실패한 길드 ID를 이름으로 알려준다).
   남은 한계는 **이미지 폴더 이름 공간이 서버 전체에서 공유된다**는 것 — 3.6절 참고.
4. **이미지 중복 저장 방지 없음.** 같은 사진을 두 번 올리면 두 번 저장된다.
   해시(sha256) 기반 중복 제거를 넣을 수 있다.
5. **갤러리에 페이지네이션 없음.** 한 폴더에 수천 장이 쌓이면 브라우저가 느려진다.
6. **웹 갤러리 인증이 Basic Auth 단일 비밀번호.** 외부에 열어둘 거라면
   HTTPS(리버스 프록시) + 더 나은 인증이 필요하다.
7. ~~볼륨 조절 명령어 없음~~ → 구현됨. 3.2-1 절 참고.

---

## 10. VPS 이전 시 체크리스트

1. Node 20+ 설치
2. `git clone` 후 `npm install`
3. **`npm run update-ytdlp`** — 바이너리는 커밋되지 않으므로 반드시 다시 받아야 하고,
   리눅스용을 받는다(스크립트가 플랫폼을 자동 판별)
4. `.env` 생성. 특히 바꿔야 할 것:
   - `IMAGE_DIR` — 리눅스 절대경로(예: `/home/ubuntu/sarangbang-bot/data/images`)
   - `WEB_PUBLIC_URL` — `http://<서버IP>:3000`
   - `WEB_TOKEN` — **외부에 열리므로 반드시 긴 문자열로 설정**
5. 방화벽/보안그룹에서 웹 포트 개방 (기본 3000)
6. `npm run deploy` 후 `npm start`
7. 상시 구동은 `pm2` 또는 systemd 서비스
8. **유튜브 IP 차단을 각오할 것** — 7절 표 참고. 무료 티어에서 가장 흔한 실패 원인이다.

### 무료 호스팅 현실 평가
- t3.micro(1GB RAM)면 개인 서버 1개 기준 성능은 충분하다. 3.2절 덕분에 CPU도 여유.
- 음성 스트리밍 트래픽은 대략 30MB/시간. 프리티어 한도로 문제없다.
- 진짜 병목은 성능이 아니라 **유튜브의 데이터센터 IP 차단**이다.
- Oracle Cloud Always Free가 AWS 프리티어보다 조건이 낫다(기간 무제한). 단 IP 문제는 동일하고,
  **놀고 있으면 인스턴스를 회수해가는 정책**이 있어 조용한 개인 봇에는 오히려 위험하다.
  구체적인 절차와 대응은 **[ORACLE-CLOUD.md](ORACLE-CLOUD.md)** 에 따로 정리했다.
  (2026-08-31 확인 기준 ARM Always Free 총량은 2 OCPU / 12GB이고, 이 봇은 1 OCPU / 6GB로 충분하다.
  linux-arm64용 ffmpeg-static에 `--enable-libopus`가 들어 있음을 바이너리에서 직접 확인했으므로
  3.2절의 오디오 경로가 ARM에서도 그대로 성립한다.)
- 이미지 저장은 디스크를 계속 먹는다. 프리티어 스토리지 한도를 주기적으로 볼 것.

---

## 11. 재시도 제한시간은 **늘려가며** 잡는다

실제 사고: 서버에서 `yt-dlp 응답이 너무 오래 걸립니다 (타임아웃)` 이 3번 반복되며 75초를 버렸다.

원인은 IP 차단이 아니라 **서버가 느린 것**이었다. 그런데 25초 제한으로 세 번 시도하면
30초 걸리는 서버에서는 **세 번 다 실패한다.** 같은 조건으로 반복하는 재시도는 의미가 없다.

지금은 `TIMEOUT_LADDER = [20초, 60초]` 다.
- 1차는 짧게 끊어 **빠른 실패**를 잡는다 (일시적 딸꾹질이면 바로 재시도)
- 2차는 넉넉히 줘서 **느린 서버도 성공**하게 한다

그리고 **타임아웃과 차단은 안내를 다르게 한다.** 예전에는 둘 다 "IP 차단일 수 있다" 로
안내해서 쿠키를 뒤지게 만들었는데, 실제로는 서버 성능 문제였다.
타임아웃일 때는 `YTDLP_JS_RUNTIME=false`, `free -h`, `time ./bin/yt-dlp --version` 을 안내한다.
