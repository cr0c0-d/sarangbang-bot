# 망고 · 노래하는 망고 (sarangbang-bot)

개인용 디스코드 봇. **한 저장소에서 봇 두 개**를 돌립니다.

| 봇 | 하는 일 |
|---|---|
| 🥭 **망고** | 📅 일정·정산 · 🗣️ 읽어주기(TTS) · ⏰ 타이머 · 🗳️ 투표 · 🎬 영화 고르기 · 🖼️ 이미지 정리 |
| 🎤 **노래하는 망고** | 🎵 유튜브 음악 재생 |

**둘은 완전히 다른 봇입니다.** 디스코드 애플리케이션도 따로 만들고, 따로 초대하고,
따로 켜고 끕니다. 겸하는 모드는 없습니다.

나뉘어 있어서 좋은 점: **음악을 듣는 중에 채팅이 와도 음악이 멈추지 않습니다.**
(봇 하나였을 때는 음성 연결이 하나뿐이라 음악을 멈추고 읽어준 뒤 이어 붙였습니다)

음악이 필요 없으면 **망고만** 띄우면 됩니다.

---

## 처음 한 번만 하는 설정

프로그래밍을 몰라도 됩니다. 순서대로 따라가면 됩니다.
**망고만 쓰면 4단계 20분, 음악까지 쓰면 5단계 30분쯤 걸립니다.**

### 1단계 — 디스코드에서 봇 계정 만들기

먼저 **망고**부터 만듭니다. (음악 봇은 5단계에서 똑같은 방법으로 하나 더 만듭니다)

1. https://discord.com/developers/applications 접속 → 오른쪽 위 **New Application**
2. 이름에 `망고` 입력 → **Create**
3. 왼쪽 메뉴 **General Information** → **APPLICATION ID** 를 복사해둡니다 → 이게 `CLIENT_ID`
4. 왼쪽 메뉴 **Bot** 으로 이동
5. **Reset Token** 클릭 → 확인 → 나온 긴 문자열을 복사해둡니다 → 이게 `DISCORD_TOKEN`
   > ⚠️ 이 토큰은 **비밀번호와 같습니다.** 남에게 보여주거나 인터넷에 올리지 마세요.
   > 창을 닫으면 다시 볼 수 없습니다. 잃어버리면 Reset Token을 다시 누르면 됩니다.
6. 같은 **Bot** 페이지를 아래로 스크롤 → **Privileged Gateway Intents** 항목에서
   **MESSAGE CONTENT INTENT** 를 **켭니다** → 아래 **Save Changes**

   > 🔴 **이걸 안 켜면 읽어주기와 링크 자동감지가 조용히 동작하지 않습니다.**
   > 에러도 안 나서 원인을 찾기 매우 어렵습니다. 반드시 켜세요.

### 2단계 — 봇을 내 서버에 초대하기

#### 2-1. 먼저 "서버에 설치"를 켜야 합니다 ⚠️

최근에 만든 앱은 **서버 설치가 기본으로 꺼져 있습니다.** 이걸 안 켜고 초대 링크를 열면
`이 애플리케이션에 지원되지 않는 설치 유형` 이라는 오류가 납니다.

1. 왼쪽 메뉴 **Installation** (설치) 으로 이동
2. **Installation Contexts** 항목에서 **Guild Install** 을 **체크**합니다
   (User Install 은 켜져 있어도 무관합니다)
3. 아래 **Default Install Settings** → **Guild Install** 에서
   - **Scopes**: `bot` 과 `applications.commands` 를 **둘 다** 추가
   - **Permissions**: 아래 항목들을 체크
     `View Channels` `Send Messages` `Read Message History` `Add Reactions`
     `Attach Files` `Embed Links` `Connect` `Speak`
     (`Manage Messages` 는 망고에는 필요 없지만, 5단계의 음악 봇에는 꼭 필요합니다)
4. 맨 아래 **Save Changes**

#### 2-2. 초대하기

같은 **Installation** 페이지의 **Install Link** 를 **Discord Provided Link** 로 두면
바로 아래에 초대 링크가 나옵니다. **그 링크를 쓰는 게 가장 확실합니다.**

직접 만들고 싶으면 아래 주소의 `여기에_CLIENT_ID` 자리에 1단계에서 복사한 APPLICATION ID를 넣으세요.

```
https://discord.com/api/oauth2/authorize?client_id=여기에_CLIENT_ID&permissions=3271744&scope=bot%20applications.commands&integration_type=0
```

서버를 고르고 승인하면 끝입니다.
(`permissions=3271744` = 메시지 보내기·관리, 반응 달기, 파일 첨부, 음성채널 연결·말하기.
`integration_type=0` 은 "사용자가 아니라 서버에 설치" 라는 뜻입니다)

### 3단계 — 채널 ID 알아내기

1. 디스코드 앱에서 **설정(톱니바퀴) → 고급 → 개발자 모드** 를 켭니다
2. 이제 채널이나 서버를 **우클릭하면 "ID 복사"** 가 생깁니다
3. 아래 것들을 복사해둡니다
   - 서버(맨 왼쪽 서버 아이콘) 우클릭 → ID 복사 → `GUILD_ID`
   - 읽어줄 글을 쓸 채팅방 → `TTS_TEXT_CHANNEL_ID`
   - 사진을 올릴 채팅방 → `IMAGE_CHANNEL_ID`
   - (음악까지 쓴다면) 링크를 올릴 채팅방 → `MUSIC_TEXT_CHANNEL_ID`
   - (음악까지 쓴다면) 음악을 틀 음성채널 → `MUSIC_VOICE_CHANNEL_ID`

   > 💡 **채널 ID를 지금 다 모으지 않아도 됩니다.** 봇을 켠 뒤 디스코드에서
   > `/채널설정` 명령으로 채널을 골라서 지정하는 게 훨씬 쉽습니다.
   > 이 단계는 건너뛰고 `GUILD_ID` 만 채워도 됩니다.
   > 자세한 설명은 `.env.example` 파일 안에 항목마다 적혀 있습니다.

### 4단계 — 설정 파일 만들고 실행

`.env.example` 파일을 복사해서 `.env` 라는 이름으로 만들고, 위에서 모은 값들을 채웁니다.

```bash
cp .env.example .env
```

그다음 순서대로 실행합니다.

```bash
npm install
```

```bash
npm run update-ytdlp
```

```bash
npm run deploy
```

```bash
npm start
```

```
✅ 로그인 완료: 망고#0000
   맡은 역할: mango (tts, timer, images, poll)
```

이렇게 뜨면 **망고**가 성공적으로 떴습니다.

#### 나중에 넣어도 되는 것 (안 넣어도 봇은 정상 동작합니다)

기능마다 키가 하나씩 필요합니다. **안 넣으면 그 기능만 조용히 꺼지고**, 쓰려고 하면
발급 방법을 알려줍니다. 나중에 `.env` 에 넣고 재시작하면 됩니다.

| 기능 | 넣을 값 | 어디서 |
|---|---|---|
| `/영화` 영화 고르기 | `TMDB_READ_TOKEN` | https://www.themoviedb.org → 설정 → API (무료) |
| `/망고야` 물어보기 | `GEMINI_API_KEY` | https://aistudio.google.com/apikey (**카드 없이** 무료 등급) |

> ⚠️ `/망고야` 는 **ChatGPT Plus·Google AI Pro 구독과 완전히 별개**입니다.
> 구독을 해도 API 크레딧은 0입니다. 위 주소에서 API 키를 따로 발급받아야 합니다.
> 제미나이를 고른 이유가 이것입니다 — **카드 등록 없이** 무료 등급으로 시작할 수 있습니다.
> (분당·하루 요청 수 제한이 있고, 무료 등급은 보낸 내용이 구글의 서비스 개선에 쓰일 수 있습니다)

음악까지 쓰시려면 아래를 이어서 하세요.

### 5단계 — 노래하는 망고 띄우기 (음악을 쓸 때만)

음악은 **별도의 봇**입니다. 안 쓰실 거면 이 단계는 건너뛰세요.

#### 5-1. 디스코드에서 봇을 하나 더 만듭니다

[Developer Portal](https://discord.com/developers/applications) → **New Application** →
이름은 `노래하는 망고`. **1~2단계와 똑같이** 하시면 됩니다.

- **Bot** 탭 → Reset Token → 복사
- **Bot** 탭 → `MESSAGE CONTENT INTENT` **켜기** → **Save Changes**
  > 🔴 **가장 많이 빠뜨리는 곳입니다.** 안 켜면 봇이 아예 로그인을 못 하고
  > `로그인 실패: Used disallowed intents` 로 계속 재시작합니다.
  > 망고에서 켰어도 **새 앱에서 또 켜야 합니다.**
- **Installation** 탭 → 권한 체크 → 초대 링크로 서버에 넣기
  (`Manage Messages` 도 꼭 — 링크 메시지를 지우는 데 필요합니다)

> ⚠️ **꼭 새로 만드세요.** 망고와 같은 토큰을 쓰면 모든 명령에 **두 번** 답하고,
> 명령어 등록이 서로를 지웁니다. (봇이 알아서 잡아내고 실행을 멈춥니다)

#### 5-2. 설정 파일

```bash
cp .env.music.example .env.music
```

`.env.music` 을 열어 **새 봇의 토큰과 CLIENT_ID** 를 넣습니다. 그 외에는 건드릴 게 없습니다.
나머지 설정(서버 ID 등)은 `.env` 것을 그대로 물려받습니다.

#### 5-3. 등록하고 실행

```bash
npm run deploy:music
```

```bash
npm run start:music
```

```
✅ 로그인 완료: 노래하는 망고#0000
   맡은 역할: music (music)
```

**두 봇을 동시에 띄워두시면 됩니다.** 창을 두 개 쓰거나, 서버라면 systemd 서비스를 두 개 만드세요.

---

## 잘 되는지 확인하는 순서

한 번에 다 해보지 말고 **순서대로** 확인하세요. 어디서 막혔는지 알 수 있습니다.

**망고 (`npm start`)**

1. `/도움말` → 기능 목록이 보이면 슬래시 명령어 등록 성공
2. `/채널설정` 으로 읽어주기·이미지 채널을 지정 (인자 없이 실행하면 현황이 보입니다)
3. 읽어주기 채팅방에 아무 글이나 쓰기 → 읽어주면 TTS 성공
4. `/타이머 1분` → 1분 뒤 소리로 알려주면 타이머 성공
5. `/투표 질문:테스트 선택:가, 나` → 버튼을 누르면 표가 오르면 투표 성공
6. 이미지 채팅방에 사진 올리기 → ✅ 반응이 붙으면 저장 성공
7. `/갤러리` → 나온 주소를 브라우저로 열기 → 사진이 보이면 성공

**노래하는 망고 (`npm run start:music`)** — 음악까지 쓴다면

8. `/채널설정` 으로 음악 채팅방·음성채널 지정
9. 음성채널에 들어간 뒤 `/재생 아무 노래 제목` → 음악이 나오면 성공
10. 음악 채팅방에 유튜브 링크만 붙여넣기 → 자동 재생되면 링크 감지 성공

---

## 쓰는 법

### 🔌 기능 켜고 끄기 — 서버에 안 들어가고

`/기능` 을 치면 버튼이 나옵니다. **서버에 SSH로 들어가 프로세스를 끌 필요가 없습니다.**

```
🟢 🗣️ 읽어주기    — 켜짐          ← 망고
🟢 ⏰ 타이머      — 켜짐
🟢 🗳️ 투표        — 켜짐
🟢 🖼️ 이미지 정리 — 켜짐

[🗣️ 읽어주기] [⏰ 타이머] [🗳️ 투표] [🖼️ 이미지 정리]
[전체 켜기] [전체 끄기]
```

**봇마다 따로입니다.** 노래하는 망고의 `/기능` 에는 🎵 음악만 나옵니다.
명령어 목록에 `/기능` 이 두 번 보이는데, 봇 아이콘으로 구분하세요.

**언제 쓰나** — 서버에 이미 다른 음악봇·TTS봇이 있으면 같은 링크·같은 채팅에 둘이
동시에 반응해 겹칩니다. 그때 겹치는 기능만 끄면 됩니다.
고치면서 켜고 끄기를 반복할 때도 편합니다.

- 설정은 **서버별로 저장**되고 봇을 재시작해도 유지됩니다.
- 기능을 끄면 그 기능의 **명령어와 버튼도 막힙니다** ("꺼져 있습니다" 안내가 나옵니다).
- **음악을 끄면 재생 중인 것도 멈춥니다.**
- **이미지를 끄면 새 사진만 저장하지 않습니다.** 이미 모아둔 갤러리는 계속 볼 수 있습니다.
- `/기능` `/채널설정` `/도움말` `/나가기` 는 **기능을 꺼도 동작합니다** — 다 꺼놓고 되살릴 수 있어야 하므로.

> 🔒 **`/기능` `/채널설정` `/정리` 는 서버 관리 권한이 있는 사람만 쓸 수 있습니다.**
> 친구들 목록에는 **아예 안 보입니다.** 서버 설정을 바꾸거나 사진을 지우는 명령어라서입니다.
> 특정 사람에게 열어주려면 **서버 설정 → 연동** 에서 명령어별로 바꿀 수 있습니다.

### ⚙️ 채널 지정 — 여기부터 하세요

어떤 채널을 무엇에 쓸지는 **디스코드 안에서 바로 정할 수 있습니다.** `.env` 를 열 필요 없습니다.

| 쓰는 법 | 하는 일 |
|---|---|
| `/채널설정` (인자 없이) | 지금 무엇이 어디로 지정됐는지 보기 + **해제 버튼** |
| `/채널설정 종류:... 채널:...` | 그 자리에서 지정 |
| `/채널설정 종류:...` (채널 비우고) | **지금 이 채널**로 지정 |

> 🔒 **비공개 채널이 채널 고르기 목록에 안 뜰 때가 있습니다.**
> 그럴 땐 **그 채널에 들어가서** 채널 칸을 비우고 `/채널설정 종류:...` 만 실행하세요.
>
> 그리고 비공개 채널은 **봇도 따로 초대해야 합니다.** 채널 이름 옆 ⚙️ → 권한 →
> 봇을 멤버로 추가하세요. 권한이 없으면 지정은 되지만 **조용히 동작하지 않습니다** —
> 그래서 지정하는 순간 봇이 **없는 권한을 이름으로 알려줍니다.**

> ⚠️ **명령어로 지정한 값이 `.env` 보다 우선합니다.**
> 그래서 `.env` 를 고쳤는데 안 바뀌는 것처럼 보일 수 있습니다.
> `/채널설정` 이 각 항목 옆에 `명령어로 지정` / `.env 기본값` 을 표시해주니 그걸로 판단하세요.

### 🎵 음악 — 노래하는 망고

**친구들에게 알려줄 건 두 가지뿐입니다.**

1. 채팅방에 **유튜브 링크를 붙여넣기** (여러 개를 한꺼번에 붙여도 됩니다)
2. 조작은 **제어판 버튼**으로 (`/대기열` 로 띄웁니다)

> 💡 링크를 붙여넣으면 **그 메시지는 자동으로 지워지고**, 제어판이 항상 채팅방
> **맨 아래**에 남습니다. 곡을 추가해도 제어판이 위로 밀려나지 않습니다.
> 제어판이 갱신될 때 **푸시 알림은 울리지 않습니다.**
>
> 링크 메시지가 안 지워지면 봇에게 **메시지 관리(Manage Messages)** 권한이 없는 것입니다.
> 서버 설정 → 역할 → 봇 역할에서 켜주세요. (권한이 없어도 재생은 정상 동작하고 ✅ 반응만 붙습니다)

### 버튼으로 조작하기 (추천)

`/대기열` 을 실행하면 이런 제어판이 나옵니다.

| 버튼 | 하는 일 |
|---|---|
| ⏮️ 이전 | 직전에 들었던 곡으로 |
| ⏸️ / ▶️ | 일시정지 / 다시 재생 |
| ⏭️ 다음 | 다음 곡으로 |
| 🔁 반복 | 현재 곡 반복 켜기·끄기 |
| ⏹️ 정지 | 멈추고 대기열 비우기 |
| ⬆️ 드롭다운 | 고른 곡을 **다음에 재생**하도록 올리기 |
| 🗑️ 드롭다운 | 고른 곡을 대기열에서 빼기 |
| 🔉 🔊 | 음악 음량 −10 / +10 |
| 🕐 지난 곡 | 예전에 들은 곡을 골라 다시 담기 (여러 곡 가능) |
| 🔄 새로고침 | 화면 갱신 |

곡이 바뀌면 **이 메시지가 알아서 갱신됩니다.** 채팅방에 "재생 중" 메시지가 쌓이지 않습니다.

### 명령어로 조작하기

| 명령어 | 설명 |
|---|---|
| `/재생 <링크 또는 검색어>` | 재생. 재생목록 링크를 넣으면 전부 대기열에 들어갑니다 |
| `/대기열` | **제어판 열기** — 이전·다음·일시정지·반복·정지·순서변경·빼기가 전부 버튼입니다 |
| `/순서이동 <번호> <새번호>` | 정밀 조작 (버튼 드롭다운은 "맨 앞으로"만 됩니다) |
| `/나가기` | 음성채널에서 나가기 |
| `/음량 크기:70` | 소리 크기 (비우면 지금 값만 봅니다) |
| `/재생` (검색어 비우고) | 🕐 **지난에 들은 곡 목록**에서 골라 담기 |

> 이전에 있던 `/다음` `/정지` `/일시정지` `/반복` 등은 **제어판 버튼과 완전히 겹쳐서 없앴습니다.**
> 전부 `/대기열` 안에 있습니다.

**여러 곡 한 번에 넣기** — 링크를 여러 개 붙여넣으면 **보낸 순서 그대로** 대기열 뒤에 붙습니다.
한 메시지에 여러 줄로 넣어도 되고, 메시지를 연달아 보내도 순서가 지켜집니다.

**재생목록 링크를 보내면 그 안의 곡을 전부 담습니다.** 링크 하나면 됩니다.

- 너무 긴 목록은 **앞에서부터 50곡**만 담고 "전체 N곡 중 앞 50곡" 이라고 알려줍니다.
  (`.env.music` 의 `MUSIC_PLAYLIST_MAX` 로 바꿀 수 있습니다)
- 유튜브에서 **자동재생으로 넘어간 뒤 복사한 링크**에는 `list=RD…` 가 붙는데,
  이건 유튜브가 만든 믹스라 **그 곡 하나만** 담습니다. 노래 하나 공유했는데
  수십 곡이 쏟아지는 일을 막기 위해서입니다.

마지막 곡이 끝나고 5분간 조용하면 알아서 나갑니다. (`MUSIC_LEAVE_AFTER_SEC`)

### 🗣️ 읽어주기 — 망고

지정한 채팅방에 글을 쓰면 음성채널에서 읽어줍니다.

> 💡 **음성채널 안의 채팅창을 골라도 됩니다.** (디스코드 음성채널에는 자체 채팅창이 있습니다)
> 그렇게 지정하면 **그 음성채널에서 그대로 읽어주므로**, 읽어주기 음성채널을 따로 지정할 필요가 없습니다.
> 가장 자연스러운 설정입니다.

- 맨 앞에 **`//`** 를 붙이면 읽지 않습니다. (예: `//이건 안 읽힘`)
- 링크는 "링크"라고만 읽고, 멘션은 사람 이름으로, `ㅋㅋㅋㅋㅋ`는 3개로 줄여 읽습니다.
- **자음·모음만 쓴 글도 읽어줍니다.** 뜻이 있는 축약어는 뜻으로, 웃음소리는 소리로 바꿔 읽습니다.

  | 쓴 글 | 읽는 말 |
  |---|---|
  | `ㅋㅋㅋㅋㅋ` `ㅎㅎ` `ㅠㅠ` | 크크크 · 흐흐 · 흑흑 |
  | `ㅇㅇ` `ㄴㄴ` `ㄱㅅ` `ㅇㅋ` `ㄷㄷ` `ㅎㅇ` | 응응 · 노노 · 감사 · 오케이 · 덜덜 · 하이 |
  | 그 밖의 낱자 (`ㅅㅂ` `ㅑ` …) | 소리 나는 대로 (스브 · 야) |

  축약어를 더 넣고 싶으면 `src/tts/index.js` 의 `ABBREV` 에 한 줄 추가하면 됩니다.
- **이모지는 읽지 않습니다.** 글에 섞여 있으면 이모지만 빼고 읽고,
  이모지만 보내면 **"이모지를 보냈어요."** 라고만 말합니다.
  (디스코드 커스텀 이모지 `:kekw:` 도 이름을 읽지 않습니다)
- `/읽어주기 켜기:false` 로 끄기
- **`/목소리`** — 내 글을 읽을 목소리를 고릅니다. **사람마다 다르게 쓸 수 있습니다.** (14종)
  비우고 실행하면 기본 목소리로 되돌립니다.
  서버 전체 기본값은 `.env` 의 `TTS_VOICE` 로 정합니다.
- **음악을 듣는 중에 글이 와도 음악은 멈추지 않습니다.** 봇이 달라 각자 소리를 냅니다.

**한국어 말고 다른 언어를 쓰면?** — 목소리에 따라 크게 갈립니다. 실제로 측정한 결과입니다.

**목소리는 14종입니다.** Edge TTS가 "한국어"로 분류하는 건 3종뿐이지만,
**다국어 목소리는 한국어도 자연스럽게 읽습니다** (12종 전부 실측 확인). 그래서 14종을 제공합니다.

| 입력 | 다국어 목소리 (기본값 포함 12종) | 선희·인준 (한국어 전용 2종) |
|---|---|---|
| 한국어 | 정상 | 정상 |
| 한국어 + 영어 섞임 | 정상 | 정상 |
| 영어 문장 | 자연스러운 영어 발음 | 한국어 발음으로 어색하게 읽음 |
| 일본어 | 정상 | 🔴 **아무 소리도 안 남** (에러도 없음) |
| 중국어 | 정상 | 한자를 한국식 음독으로 읽음 |

그래서 기본 목소리를 **현수(다국어)** 로 두었습니다.
한국어 전용 목소리를 고르면 위 제약을 경고로 알려줍니다.

### 📅 일정 · 정산 — 망고

**지금 쓰시는 방식을 그대로 돕는 기능입니다.** 「일정」 카테고리 아래 비공개 채널을 만들고,
거기에 필요한 정보를 모아두고, 끝나면 정산하는 흐름입니다.

#### 준비 — 일정 카테고리 지정 (한 번만)

**아무것도 안 하셔도 됩니다.** `/일정새로` 를 처음 쓰면 **카테고리를 고르는 드롭다운**이 뜹니다.
한 번 고르면 그다음부터는 안 물어봅니다.

봇에게 **`채널 관리`** 권한은 있어야 합니다. 없으면 만들기 전에 알려줍니다.

> 💡 `/채널설정` 에서도 「일정 카테고리」 를 바꿀 수 있습니다. 다만 그쪽 채널 목록에는
> 텍스트·음성 채널이 **섞여 나옵니다** — 디스코드가 종류에 따라 목록을 바꿔주지 못해서입니다.
> `/일정새로` 쪽 드롭다운은 **카테고리만** 보여줍니다.

#### 1. 일정 채널 만들기

```
/일정새로
```

창이 뜨면 채웁니다.

| 칸 | 넣는 것 |
|---|---|
| **일정 이름** | `오사카 여행` |
| **날짜 · 시간** | `10/3 18:30` · `2026-10-03` · `10월 3일 오후 6시` · `내일 19시` |
| **일정표** | 한 줄에 한 곳씩. 여러 곳을 넣을 수 있습니다 (아래 참고) |
| **참여자** | 고른 사람만 채널을 볼 수 있습니다 |
| **참여 역할** | 역할로 한꺼번에 넣을 수 있습니다 |

`[251003-오사카-여행]` 비공개 채널이 생기고 일정 판이 바로 올라옵니다.

#### 2. 일정 판

```
📅 홍대 나들이
🕘 9월 1일 (화) 종일 · 3일 후

📍 일정표
오후 12:00  점심
     📍 홍대 스시로 — 🧭 길찾기 · 카카오맵 · 네이버
오후 2:00   카페
     📍 어니언 홍대 — 🧭 길찾기 · 카카오맵 · 네이버   ← 출발지가 스시로로 채워짐
오후 4:00   방탈출
⬥          저녁 2차                      ← 시간을 안 정했으면 맨 뒤

✅ 할 일  1/2
   ✅ 방탈출 예약   @지예
   ⬜ 카페 자리 확인

[✅ 방탈출 예약] [⬜ 카페 자리 확인]      ← 눌러서 체크
[✏️ 고치기] [➕ 할 일] [📝 메모] [🔔 알림] [🗑️ 삭제]
```

**일정표는 이렇게 적습니다.** 한 줄에 한 곳씩, `시간 이름 | 장소`.

```
12:00 점심 | 홍대 스시로
오후 2시 카페 | 어니언 홍대
16:00 방탈출
저녁 2차
```

- **`|` 뒤가 지도에 검색됩니다.** "점심" 을 지도에 검색해봐야 소용없으니 이름과 장소를 나눕니다.
- 시간이 있는 것은 **시간순으로 정렬**되고, 없는 것은 적은 순서대로 맨 뒤에 붙습니다.
- 시간도 장소도 **없어도 됩니다.** `저녁 2차` 처럼만 적어도 됩니다.
- 시간은 **`12:00`** 이나 **`오후 2시`** 처럼 `:` 나 `시` 를 붙여주세요.
  (그래야 `저녁 2차` 의 `2` 를 시간으로 오해하지 않습니다)

**🧭 길찾기** 는 출발지·도착지가 **미리 채워진 채로** 열립니다.
첫 곳은 **현재 위치에서**, 두 번째부터는 **바로 앞 장소에서** 출발합니다.

> 💡 길찾기만 **구글 지도**로 엽니다. 카카오맵·네이버지도는 주소로 출발지·도착지를
> 채워 넣을 수가 없습니다 — 좌표가 필요합니다 (직접 열어서 확인했습니다).
> 장소를 보는 것은 한국 정보가 정확한 **카카오맵·네이버** 링크를 그대로 씁니다.

- 할 일은 **누른 사람이 담당자**로 표시됩니다. 따로 지정할 필요가 없습니다.
- 이미 만든 채널에서 `/일정` 을 치면 판이 뜹니다. 제목은 **채널 이름에서 자동으로** 채워집니다.
- `🔔 알림` 으로 **1시간 전 / 3시간 전 / 당일 아침 / 하루 전 저녁** 중에 고르면 그때 알려줍니다.
  **봇을 재시작해도 예약은 살아 있습니다.**
- 할 일이나 메모를 **지우려면** `✏️ 고치기` 를 누르세요. 목록이 채워져 나오니 줄을 지우면 됩니다.
- `🗑️ 삭제` 는 두 가지로 갈라집니다. 만든 사람과 **채널 관리** 권한이 있는 사람만 누를 수 있습니다.

  | | 하는 일 |
  |---|---|
  | **일정만 지우기** | 판과 등록한 내용만 지웁니다. **채널·사진·대화는 그대로** |
  | **채널까지 지우기** | 채널을 지웁니다. **사진과 대화가 전부 사라지고 되돌릴 수 없습니다** |

#### 3. 중요한 메시지를 참고자료로 모으기

숙소 예약 확인서나 캡처처럼 **나중에 다시 봐야 하는 메시지**는 위로 밀려 사라집니다.

> 그 메시지 **우클릭 → 앱 → 📌 일정에 등록**

판의 `📎 참고자료` 에 링크로 모입니다. 사진을 다시 올리지 않고 **원본을 가리킵니다.**

> 💡 채팅과 사진을 봇이 **자동으로 긁지 않습니다.** 필요한 것만 직접 등록하는 방식입니다.

#### 4. 정산

```
/정산
```

창에서 **내용 · 금액 · 나눌 사람**을 채웁니다. 금액은 **숫자 하나면 균등분할**,
여러 줄로 적으면 순서대로 개별 금액입니다. `12만` `120,000` 도 읽습니다.

```
💰 오사카 여행 숙소
결제: @지예 · 총 120,000원 · 3명
   @민수  40,000원  ⬜ 송금 전
   @수현  40,000원  ✅ 보냈어요
   @지예  40,000원  (결제자)
받을 돈  40,000원 / 80,000원
[✅ 보냈어요]
```

- 결제자는 **명령어를 쓴 사람**입니다. 따로 고르지 않습니다.
- `✅ 보냈어요` 는 **본인 줄만** 바뀝니다. 전원이 보내면 초록색으로 바뀝니다.
- 실제 송금은 각자 하시면 됩니다. 버튼은 **보냈다는 표시**일 뿐입니다.

### 🎬 영화 고르기 — 망고

"오늘 뭐 볼지" 를 정해줍니다. **장르와 OTT** 를 고르면 실제로 볼 수 있는 작품 중에서 골라줍니다.

```
/영화
```

이것만 치면 고르기 판이 뜹니다 (나만 보입니다).

| | |
|---|---|
| **장르** | 액션·코미디·로맨스 등 12가지. **안 골라도 됩니다** |
| **OTT** | 여러 개 고를 수 있습니다. "넷플릭스 아니면 TVING 중 아무거나" |
| 🎲 **한 편 뽑기** | 바로 한 편을 뽑아 포스터·평점·줄거리를 보여줍니다 |
| 🗳️ **투표 만들기** | 5편을 뽑아 **투표**로 띄웁니다. 다 같이 버튼으로 고르면 됩니다 |

뽑은 게 마음에 안 들면 **🎲 다시 뽑기**, 조건을 바꾸려면 **🔀 조건 바꾸기** 를 누르면 됩니다.
영화와 드라마를 **섞어서** 뽑습니다.

### ⚙️ 구독 중인 OTT만 보이게 하기

고르기 판의 `⚙️ 쓰는 OTT 설정` 을 누르면 됩니다. 서버마다 따로 저장됩니다.
**아무것도 안 고르면 전체**를 씁니다.

> ⚠️ **쿠팡플레이는 오리지널 드라마만 나옵니다.** 영화 정보를 가져오는 TMDB 에
> 쿠팡플레이 **영화 자료가 아예 없습니다.** 오리지널 드라마 22편 정도는 나옵니다.
> 넷플릭스·TVING·wavve·왓챠·디즈니+ 는 영화·드라마 모두 잘 됩니다.

> 💡 TMDB 키가 없으면 `/영화` 가 발급 방법만 알려주고 아무 일도 하지 않습니다.
> 봇의 다른 기능은 그대로 동작합니다.

### 🗳️ 투표 — 망고

**질문과 선택지 양쪽에 사진을 붙일 수 있는** 투표입니다.

```
/투표
```

이것만 치면 **만들기 창**이 뜹니다. 명령어 칸에 이것저것 적을 필요가 없습니다.

| 창의 칸 | 넣는 것 |
|---|---|
| **질문** | 점심 뭐 먹지? |
| **선택지** | 한 줄에 하나씩. 최대 10개 |
| **질문 사진** (선택) | 질문 아래에 **크게** 붙습니다. 한 장 |
| **선택지 사진** (선택) | **선택지 순서대로** 붙습니다. 최대 5장 |
| **자동 마감 (분)** (선택) | 비우면 안 닫힙니다. `30` 을 넣으면 30분 뒤 자동으로 닫힙니다 |

사진은 안 넣어도 됩니다. 넣을 것만 넣으면 됩니다.

> 💡 빨리 만들고 싶으면 `/투표 질문:점심 선택:피자, 치킨` 처럼 한 줄로도 됩니다.
> (이 방법은 사진을 못 붙입니다)

고르는 건 **버튼**입니다. 외울 게 없습니다.

- 같은 걸 **다시 누르면 취소**, 다른 걸 누르면 그쪽으로 **옮겨집니다.**
- **누가 뭘 골랐는지는 안 보입니다.** 표 수와 비율만 보이고, 본인에게만 "○○에 투표했습니다" 가 뜹니다.
- 다 되면 **🔒 마감** 버튼을 누르면 됩니다. 만든 사람과 메시지 관리 권한이 있는 사람만 누를 수 있습니다.
- **자동 마감**을 넣었으면 투표에 `3분 후 자동으로 마감됩니다` 처럼 남은 시간이 보이고,
  시간이 되면 알아서 닫힙니다. 그전에 손으로 마감해도 됩니다.
  **봇을 재시작해도 예약은 살아 있습니다.** 꺼져 있는 동안 시간이 지났으면 켜질 때 바로 닫힙니다.
- 봇을 재시작해도 **투표는 그대로 살아 있습니다.**

> 💡 디스코드에도 기본 투표 기능이 있지만 **선택지에 이미지를 못 넣습니다.**
> 사진이 필요 없다면 디스코드 기본 투표를 쓰셔도 됩니다.

### 🎥 방송 기록 — 망고

여러 명이 각자 유튜브 라이브를 켜고 게임할 때, **재미있던 순간을 버튼 한 번으로 찍어두는** 기능입니다.
방송이 끝나면 유튜브 설명란에 그대로 붙일 타임라인이 나옵니다.

#### 준비 (한 번만)

1. 방송용 채팅방을 하나 만듭니다. (예: `#방송기록`)
2. 그 채널에서 일반 사용자의 **"메시지 보내기"를 끕니다.** 제어판이 채팅에 밀리지 않습니다.
   > ⚠️ **"애플리케이션 명령어 사용"은 켜두세요.** 그걸 끄면 `/방송` 을 못 씁니다.
   > 메시지 보내기를 꺼도 **버튼은 눌립니다** — 버튼은 메시지 전송이 아닙니다.
3. `/채널설정` 에서 **방송 채널**을 그 채널로 지정합니다.
4. `/채널설정` 에서 기존 **녹화 포럼**과 **스샷 포럼**도 지정합니다.

각 게임 포스트 안에서 한 번씩 `/게임 검색:<게임>` 을 실행하면 그 포스트가 게임에 연결됩니다.
봇이 새 포스트를 자동으로 만들지는 않습니다. 연결 전까지 방송 기록은 보류되고,
나중에 녹화 포스트를 연결하면 보류 중인 기록도 자동으로 올라갑니다.

연결할 때 봇은 **그 포스트 제목을 게임의 한글 별칭으로 기억합니다.** 예를 들어
`스타듀밸리 스샷방`에서 `Stardew Valley`를 한 번 골랐다면, 그다음부터 `/게임`과 `/방송`에서
`스타듀` 또는 `스타듀밸리`로 검색해도 같은 Steam 게임이 나옵니다. 띄어쓰기와 이모지는 무시합니다.

줄임말은 스레드 관리자가 `/게임 검색:<목록에서 게임 선택> 별칭:스듀`로 추가할 수 있습니다.
별칭은 서버별로 저장되고 봇을 재시작해도 유지됩니다. 이미 연결해둔 포스트는 그 안에서
`/게임`으로 같은 게임을 한 번 더 연결하면 기존 제목도 학습됩니다. 제미나이는 사용하지 않습니다.

평소에는 아무 채널에서 `/게임 검색:<게임>` 을 실행하면 연결된 **스샷 포스트와 녹화 포스트로
바로 가는 링크**를 볼 수 있습니다. 처음 연결할 Steam 게임은 영문으로 검색하고,
한 번 연결한 뒤에는 포스트 제목의 한글명으로 검색할 수 있습니다. 목록에 없는 게임은 이름을 그대로 입력하면 됩니다.

#### 쓰는 법

**이번 방송 주소**를 붙여넣습니다. 유튜브 스튜디오의 공유 링크입니다.

```
/방송 링크:https://www.youtube.com/live/xxxxxxxxxxx 게임명:발헤임
```

`youtube.com/live/…`, `youtu.be/…`, `…/watch?v=…` 다 됩니다.

> ⚠️ **일부공개(링크 있는 사람만)로 켜시면 방송마다 이렇게 넣어야 합니다.**
> 아래 "고정 주소" 방법은 **공개 방송에서만** 됩니다 — 일부공개는 채널 목록에 안 떠서
> 유튜브가 찾아주지 않습니다. (실제로 확인했습니다)

<details>
<summary><b>공개로 방송하신다면 — 한 번만 등록해두고 버튼으로 (펼치기)</b></summary>

```
/방송 링크:https://www.youtube.com/@내계정/live 게임명:발헤임
```

`@내계정/live` 는 **방송마다 안 바뀌는 주소**입니다. 봇이 저장해두고,
그다음부터 같은 게임을 한다면 제어판의 **`🎬 지난 게임으로 등록`** 버튼 하나로 지금 하는 방송이 등록됩니다.
게임이 바뀌었으면 `/방송 게임명:<게임>`에서 검색해 새 게임을 골라 등록하세요.

`@계정` 만 적어도 알아봅니다. `/방송` 을 인자 없이 치면 **저장된 주소가 보입니다.**

이번 방송 주소(`watch?v=…` 등)는 **저장하지 않습니다** — 그 방송 하나만 가리켜서,
저장하면 다음에 지난 방송이 등록됩니다.

</details>

- **라이브를 켠 다음에** 등록하세요. 켜기 전에 등록하면 시간이 어긋납니다.
- **참여자 각자가 한 번씩** 등록하고, 각자 `게임명`도 고릅니다. 같은 세션에서도 서로 다른 게임을 할 수 있습니다.
- `게임명`은 처음에는 영문 Steam 검색을 쓰고, 연결·저장한 게임은 한글 별칭으로 검색할 수 있습니다. Steam에 없는 게임은 직접 입력합니다.
- 전에 등록한 적이 있는 사람은 같은 게임일 때 **`🎬 지난 게임으로 등록`** 으로 도중에 끼어들 수 있습니다.
- 등록하면 `✅ 등록했습니다 · 방송 시작 21:04 · 지금 8분 진행 중` 이 나옵니다.
  **이 "8분"이 맞는지 꼭 확인하세요.** 다르면 제어판의 `⏱️ 시간 어긋남` 으로 맞추면 됩니다.

그다음은 전부 **버튼**입니다.

| 버튼 | 하는 일 |
|---|---|
| **🎬 지난 게임으로 등록** | 저장해둔 고정 주소와 마지막 게임으로 등록 (**공개 방송만**. 게임이 바뀌었으면 `/방송`에서 다시 고릅니다) |
| **✂️ 지금!** | 그 순간을 찍습니다. **입력 없이 즉시 끝납니다** — 게임 중이니까요 |
| 👥 다 같이 하던 순간 | 찍은 직후에 뜹니다. 누르면 **방송 켠 사람 전원**의 타임라인에 들어갑니다 |
| ↩️ 내 마지막 취소 | **내가 찍은 것 중** 마지막을 지웁니다 (남이 찍은 것은 안 지워집니다) |
| ⏱️ 시간 어긋남 | "실제로 켠 지 1시간 20분" 처럼 적으면 자기 방송 시간만 맞춰집니다 |
| ⏹️ 방송 종료 | 사람마다 타임라인 요약판이 나옵니다 |
| ✏️ 설명 채우기 | 요약판에서. 한 번에 5개씩 채웁니다 |
| ▶️ 이어서 기록 | 종료를 잘못 눌렀을 때. **기록은 지워지지 않습니다** |

> ✂️ **마킹은 내 방송에만 들어갑니다.** 동시에 방송을 켜도 각자 다른 게임을 할 수 있으니까요.
> 다 같이 하던 순간이면 찍은 직후에 뜨는 **`👥 다 같이 하던 순간`** 을 누르면 전원에게 들어갑니다.

`/방송` 을 **인자 없이** 실행하면 지금 상태·내 고정 주소·지난 방송 목록이 보이고,
**지난 방송의 요약판을 다시 올릴 수** 있습니다. (채팅에 밀려 올라갔을 때)

방송을 종료하면 연결된 **녹화 포스트**에 방송자·시작 날짜·라이브 링크·타임라인이 자동으로
올라갑니다. 그 게임의 녹화 포스트가 아직 연결되지 않았다면 기록을 버리지 않고 보류합니다.
포스트 안에서 `/게임 검색:<게임>` 으로 연결하는 순간 보류 기록도 올라갑니다.

설명은 방송 채널 요약판에서 수정하면 **녹화방의 기존 타임라인도 함께 갱신**됩니다.
글이 길어지면 필요한 페이지만 추가하고 짧아지면 남는 페이지를 지웁니다. 공유 마킹은 해당
방송자들의 게시물에도 반영됩니다. 이전의 날짜·링크 전용 게시물도 설명 수정/재종료 시 타임라인이 붙습니다.
동기화 실패 안내가 나오면 권한을 확인한 뒤 설명 채우기를 다시 제출하세요. 수정한 설명은 저장돼 있습니다.

단순 성공 확인 메시지는 같은 사용자·채널에서 최신 것만 남깁니다. 오류, 결과 링크,
임베드와 버튼이 있는 화면은 보존합니다. 마지막 확인도 약 14분 뒤 정리합니다.
봇 재시작 전이나 이 기능 적용 전에 보낸 나만 보이는 메시지는 소급 정리할 수 없습니다.

#### 🎥 구간을 영상으로 잘라내기

요약판의 **`🎥 클립 만들 순간 고르기`** 에서 마킹을 하나 고르면 창이 뜹니다.

- 시작·끝 시간이 **마킹 앞 15초 ~ 뒤 25초** 로 채워져 있습니다. 고쳐도 됩니다.
- **`01:20:30` 처럼 시:분:초로** 적어주세요. 숫자만 적으면 분으로 봅니다.
- 한 개는 **180초까지**입니다. (720p 가 초당 0.19MB 라, 길면 서버 용량이 금방 찹니다)
- 30초~2분쯤 걸립니다. 끝나면 **웹페이지 주소**를 나만 보이게 알려줍니다.
- 그 페이지에서 바로 재생해보고 **⬇️ 받기** 로 내려받습니다. 만든 클립에는 목록에 ✅ 가 붙습니다.

> 🎧 **화면 없이 음성만 녹화된 방송이면 소리만 잘라냅니다.** 그때는
> `🎧 소리만 잘라냈습니다` 라고 알려주고, 웹페이지에서도 소리 재생기로 보입니다.
> 영상보다 훨씬 빠르고 작습니다 (15초 = 약 240KB).
> **영상이 있으면 항상 영상으로** 만듭니다 — 소리만 나오는 일은 화면이 없을 때뿐입니다.

> 🔧 **`ffmpeg exited with code -11` 이 나오면** 클립을 자르는 ffmpeg 이 죽은 것입니다
> (음수는 신호 번호. -11 = 세그폴트). 봇이 서버에 깔린 ffmpeg 으로 알아서 넘어가 보지만,
> 그래도 안 되면 서버에서 이렇게 하세요.
>
> ```bash
> sudo apt install -y ffmpeg
> ```
>
> 그다음 `.env` 에 `FFMPEG_PATH=/usr/bin/ffmpeg` 을 넣고 재시작하면 됩니다.
> **`-9` 였다면 메모리 부족**이니 `free -h` 로 swap 을 확인하고, 화질을 낮춰보세요
> (`STREAM_CLIP_MAX_HEIGHT=480`).

> ⏳ **방송이 끝난 직후에는 안 됩니다.** 유튜브가 다시보기를 만드는 데 시간이 걸립니다.
> "아직 유튜브가 다시보기를 만들고 있습니다" 가 나오면 몇 분 뒤에 다시 눌러주세요.
> 요약판 버튼은 계속 남아 있습니다.

> ⚠️ **다시보기를 남기지 않으면** 클립을 뽑을 수 없습니다. 라이브 아카이브를 켜두세요.
> 유튜브는 **일부공개(링크 있는 사람만)** 로 켜세요. 완전 비공개는 봇이 볼 수 없습니다.

> 🗑️ 클립은 **세션당 20개 · 전체 200개 · 합쳐서 5GB** 까지 쌓입니다.
> 넘으면 오래된 것부터 지우고 **방송 채널에 알려줍니다.** 최근 3일 안에 만든 것은 지우지 않습니다.
> 사진 용량과는 **따로 계산**합니다 (`STREAM_CLIP_MAX_GB`) — 클립이 늘어도 사진이 지워지지 않습니다.
> 클립 페이지의 🗑️ 는 갤러리와 같이 **`WEB_TOKEN`** 이 필요합니다. 보기·받기는 주소만 알면 됩니다.

#### ☁️ 클립을 구글 드라이브에 올리기 (선택 · **안 해도 됩니다**)

> ⚠️ **이 기능은 실제로 확인하지 못했습니다.** 다른 기능은 전부 돌려보고 만들었지만,
> 이건 구글 계정 자격증명이 필요해서 확인할 수 없었습니다. 처음 쓸 때 오류가 나면
> **구글이 한 말이 그대로 보입니다** — 그게 원인입니다.
>
> 설정을 안 하면 조용히 꺼집니다. 클립은 웹페이지에서 그대로 보고 받을 수 있습니다.
> **로컬 파일은 지우지 않습니다** — 사본만 올립니다.

설정하시려면 순서는 이렇습니다. (구글 계정에서 한 번만)

1. [Google Cloud Console](https://console.cloud.google.com) → 프로젝트 만들기
2. **API 및 서비스 → 라이브러리 → "Google Drive API" 검색 → 사용**
3. **API 및 서비스 → OAuth 동의 화면** → 외부 → 앱 이름·이메일만 채우고 저장
   → **대상(테스트 사용자)에 내 구글 계정을 추가**
4. **API 및 서비스 → 사용자 인증 정보 → OAuth 클라이언트 ID 만들기**
   → 애플리케이션 유형 **웹 애플리케이션**
   → 승인된 리디렉션 URI 에 `https://developers.google.com/oauthplayground` 추가
   → **클라이언트 ID·보안 비밀**을 받습니다
5. [OAuth Playground](https://developers.google.com/oauthplayground) 에서
   오른쪽 위 ⚙️ → **Use your own OAuth credentials** 체크 → 위 ID·비밀 입력
   → 왼쪽 칸에 `https://www.googleapis.com/auth/drive.file` 입력 → Authorize
   → **Exchange authorization code for tokens** → **Refresh token** 을 복사
6. `.env` 에 넣고 재시작

```
GDRIVE_CLIENT_ID=...apps.googleusercontent.com
GDRIVE_CLIENT_SECRET=...
GDRIVE_REFRESH_TOKEN=1//...
GDRIVE_FOLDER_ID=        # 특정 폴더에 넣고 싶으면. 폴더 주소의 마지막 부분
```

> 💡 3번의 **테스트 사용자**에 내 계정을 안 넣으면 5번에서 막힙니다.
> 그리고 앱을 "테스트" 상태로 두면 **리프레시 토큰이 7일마다 만료됩니다.**
> 만료되면 `invalid_grant` 가 나오고, 5번을 다시 하면 됩니다.
> 계속 쓰시려면 OAuth 동의 화면을 **프로덕션으로 게시**하세요 (심사 없이 됩니다 — 내 데이터만 쓰므로).

### ⏰ 타이머 (읽어주기 기능의 곁가지)

시간이 되면 **음성채널에서 소리로** 알려주고, 채팅으로도 멘션해줍니다.

| 명령어 | 설명 |
|---|---|
| `/타이머 15분` | 15분 뒤 알림 |
| `/타이머 1시간 30분` | `90m`, `1h30m` 처럼 써도 됩니다 |
| `/타이머 라면` | 등록해둔 단어로 바로 |
| `/알람등록 라면 3분` | 단어 등록 |
| `/알람등록 라면 삭제` | 단어 삭제 |
| `/타이머목록` | 진행 중인 타이머 + ❌ 취소 버튼 |

**시간 칸을 클릭하면 자주 쓰는 시간과 등록한 단어가 목록으로 나옵니다.** 외울 필요 없습니다.

- 음성채널에 들어가 있으면 **거기서** 소리로 알려줍니다. 안 들어가 있으면 채팅으로만 알립니다.
- 알람은 알림이 울려야 의미가 있으므로 **멘션으로 알립니다.** (음악 제어판과 반대)
- **봇을 재시작해도 타이머는 살아남습니다.** 꺼져 있는 동안 시간이 지났으면
  다시 켤 때 "몇 분 늦게 알렸다"고 밝히며 알려줍니다.
- 최대 24시간까지 걸 수 있습니다.

### 🖼️ 이미지 정리

**아무 설정도 필요 없습니다.** 봇이 볼 수 있는 **모든 채널**의 사진을 자동으로 저장하고,
폴더 이름은 **채널 이름**을 그대로 씁니다. ✅ 가 붙으면 저장된 겁니다.

즉 `#여행사진` 채널에 올리면 `여행사진` 폴더에 쌓입니다.

> 봇이 볼 수 있는 채널만 저장됩니다. 특정 채널을 제외하고 싶으면
> 디스코드 권한에서 그 채널의 봇 "채널 보기"를 끄면 됩니다. (아래 "잠가두기" 참고)

**어느 폴더에 들어가나?** — 위에서부터 먼저 맞는 규칙을 씁니다.

1. **스레드**에 올리면 → **스레드 이름**
2. `/폴더 여행사진` 을 실행해둔 채널이면 → `여행사진`
3. 그 외 → **채널 이름** (기본값)

`/채널설정` 에서 "이미지 채널"을 지정하면 **그 채널들만** 저장하도록 좁힐 수 있습니다.
다시 전체로 되돌리려면 `/채널해제` 하세요.

> 서버를 여러 개 쓸 때: 다른 서버에 같은 이름의 채널이 있으면 **같은 폴더에 섞입니다.**
> `/폴더 <다른이름>` 으로 한쪽을 바꿔주면 분리됩니다.

> 참고: 디스코드에서 **채널 이름을 바꾸면 그때부터 새 폴더**에 쌓입니다.
> 이전 사진은 예전 이름의 폴더에 그대로 남아 있습니다 (사라지지 않습니다).
> 합치고 싶으면 갤러리에서 사진을 골라 **옮기기** 하면 됩니다.

> 💡 **사진이 올라온 채널에는 "🖼️ 갤러리 열기" 버튼이 채팅방 맨 아래에 항상 떠 있습니다.**
> 매번 `/갤러리` 를 칠 필요가 없습니다. 새 사진이 올라오면 버튼이 아래로 따라 내려오고,
> 알림은 울리지 않습니다.

**여러 장 한 번에 받기** — 이게 핵심 기능입니다.

1. **보려는 채널에서** `/갤러리` 를 실행하면 **그 채널 폴더**의 주소가 바로 나옵니다
   — 친구들은 링크만 열면 됩니다. **암호 없음**
2. 사진을 클릭해서 여러 장 고릅니다
   - **Shift + 클릭** 으로 범위를 한꺼번에 고를 수 있습니다
   - **전체 선택** 버튼도 있습니다
4. **⬇️ 선택한 사진 받기** 를 누르면 **한 장씩 전부** 저장됩니다 (ZIP으로 묶지 않습니다)
   > 크롬이 처음 한 번 "여러 파일을 다운로드하시겠습니까?" 를 묻습니다. **허용**을 누르세요.

갤러리에서 사진을 다른 폴더로 **옮기거나 삭제**할 수도 있습니다.
이 두 가지는 되돌릴 수 없으므로 **관리 암호(`WEB_TOKEN`)를 한 번 물어봅니다.**
보기·내려받기는 암호가 없으니 친구들은 신경 쓸 필요 없습니다.

**누가 무엇을 볼 수 있나**

| 주소 | 누가 | 내용 |
|---|---|---|
| `/f/<폴더>` | **누구나** (링크만 있으면) | 그 폴더의 사진 보기·내려받기 |
| `/` (루트) | 누구나 | 안내만. **폴더 이름이 보이지 않습니다** |
| `/folders` | **나만** (`WEB_TOKEN` 필요) | 전체 폴더 목록 |
| 삭제·이동 | **나만** (`WEB_TOKEN` 필요) | — |

폴더 안에는 **뒤로가기 버튼이 없습니다.** 친구가 다른 채널 폴더로 넘어갈 수 없게 일부러 뺐습니다.
전체 목록은 서버 주소 뒤에 `/folders` 를 붙여 들어가면 됩니다 (암호를 물어봅니다).

기타 명령어: `/폴더` (인자 없이 = 현재 폴더 확인, `이름:` = 바꾸기, `해제:true` = 되돌리기) · `/폴더목록`

### 🧹 사진 용량 자동 정리

사진은 쌓이기만 하므로 언젠가 디스크가 찹니다. **오래된 것부터 자동으로 지워** 용량을 유지합니다.

`/정리` 를 치면 지금 상태를 보여줍니다.

```
📊 사진 1,240장 · 3.2 GB / 15 GB (21%) · 폴더 8개
💽 디스크 여유 28 GB / 47 GB
✅ 아직 정리할 필요가 없습니다.
```

지울 게 있으면 **무엇을 지울지 먼저 보여주고 확인 버튼을 누르게 합니다.** 바로 지우지 않습니다.

**기준을 "디스크의 80%"로 잡지 않은 이유**

디스크에는 OS·로그도 같이 있습니다. 로그가 폭주해서 디스크가 80%를 넘겼을 때 사진을 지우면
**원인은 그대로 둔 채 사진만 잃습니다.** 그래서 **사진이 쓸 수 있는 예산**을 정하는 방식입니다.

| `.env` 항목 | 기본값 | 뜻 |
|---|---|---|
| `IMAGE_MAX_GB` | **15** | 사진이 쓸 수 있는 최대 용량 |
| `IMAGE_CLEANUP_TARGET_PERCENT` | 80 | 정리할 때 예산의 80%까지 내려감 |
| `IMAGE_MIN_KEEP_DAYS` | 7 | **최근 7일 사진은 안 지움** |
| `IMAGE_MIN_FREE_GB` | 2 | 디스크 여유가 이보다 적으면 예산과 무관하게 정리 |
| `IMAGE_AUTO_CLEANUP` | true | 자동 정리 사용 여부 |

**왜 15GB인가** — Oracle 무료 47GB 부팅 볼륨에서 OS·스왑·node_modules 가 약 10GB를 씁니다.
남는 37GB 중 15GB를 사진에 주면 **사진 약 7,500장**(장당 2MB 기준)이고, 나머지는 여유로 남습니다.
친구 몇 명이 쓰는 규모라면 몇 년치입니다. 사진을 더 오래 보관하고 싶으면 이 값만 올리면 됩니다.

**안전장치**

- **최근 7일 사진은 지우지 않습니다.** 버그나 갑작스러운 대량 업로드로 방금 올린 사진이
  날아가는 것을 막습니다.
- 예산의 80%까지 한 번에 내려갑니다. 경계선에서 매번 지우는 것을 막습니다.
- **지운 내용을 디스코드에 알립니다.** 조용히 사라지지 않습니다.
- 디스크 여유가 2GB 밑으로 떨어지면 **7일 보호를 해제하고** 정리합니다.
  서버가 멈추는 것보다는 낫기 때문이고, 이때는 그 사실을 명시해서 알려줍니다.

> ⚠️ 지운 사진은 **되돌릴 수 없습니다.** 남기고 싶은 사진은 갤러리에서 미리 받아두세요.
> 자동 정리를 끄려면 `.env` 에 `IMAGE_AUTO_CLEANUP=false` 를 넣고 재시작하세요.

---

## 갤러리를 친구들도 열 수 있게 하기

기본값은 "이 컴퓨터 안에서만" 입니다. 친구들이 링크를 열 수 있게 하려면 두 곳을 바꿔야 합니다.

**① `.env` 수정**

```ini
WEB_BIND=0.0.0.0
WEB_PUBLIC_URL=http://<서버IP>:3000
WEB_TOKEN=길고_아무규칙없는_문자열
```

`WEB_PUBLIC_URL` 을 안 바꾸면 `/갤러리` 가 `localhost` 주소를 알려줘서 친구들이 열 수 없습니다.
(봇 시작 로그가 이 실수를 잡아서 경고해줍니다)

**② 서버 방화벽 두 곳 열기** — Oracle Cloud는 두 겹입니다.

콘솔에서 VCN → Security List 에 수신 규칙 추가 (`0.0.0.0/0` / TCP / `3000`), 그리고 서버에서:

```bash
sudo iptables -I INPUT 6 -m state --state NEW -p tcp --dport 3000 -j ACCEPT && sudo netfilter-persistent save
```

자세한 건 [docs/ORACLE-CLOUD.md](docs/ORACLE-CLOUD.md) 8단계에 있습니다.

> ⚠️ **공개하면 감수해야 하는 것**
> 통신이 HTTPS가 아니라 **평문**이고, 열린 포트는 자동 스캐너에 곧 발견됩니다.
> 사진 **보기·내려받기는 링크만 알면 누구나** 가능해집니다 (그게 요청하신 동작입니다).
> 다만 **삭제·이동은 `WEB_TOKEN` 으로 막혀 있으니** 그 값은 반드시 길게 설정하세요.

---

## 친구들에게 알려줄 내용 (그대로 복사해서 보내세요)

> **🎵 음악**
> 채팅방에 유튜브 링크를 붙여넣으면 음성채널에서 재생됩니다.
> 여러 개를 한꺼번에 붙여넣어도 순서대로 들어갑니다.
> 조작은 `/대기열` 을 치면 나오는 **버튼**으로 하세요. (이전/다음/일시정지/반복/정지, 순서 바꾸기, 빼기)
>
> **🗣️ 읽어주기**
> 정해진 채팅방에 글을 쓰면 음성채널에서 읽어줍니다.
> 읽히고 싶지 않으면 맨 앞에 `//` 를 붙이세요.
>
> **⏰ 타이머**
> `/타이머 15분` 이라고 치면 15분 뒤에 음성채널에서 소리로 알려줘요.
> 시간 칸을 클릭하면 자주 쓰는 시간이 목록으로 나옵니다.
>
> **🖼️ 사진**
> 아무 채널에나 사진을 올리면 자동으로 채널별 폴더에 정리됩니다. ✅ 가 붙으면 저장된 거예요.
> 여러 장을 한 번에 받고 싶으면 `/갤러리` → 나온 링크 열기 → 사진 여러 장 클릭 → **⬇️ 선택한 사진 받기**.
> (크롬이 "여러 파일 다운로드 허용?" 을 한 번 물으면 **허용**을 누르세요)
>
> 뭐가 있는지 모르겠으면 `/도움말` 을 쳐보세요.

---

## 두 봇을 서버에서 같이 돌리기

리눅스 서버라면 **systemd 서비스가 두 개**가 됩니다.
기존 서비스 파일을 복사해서 `ExecStart` 만 바꾸면 됩니다.

```bash
sudo cp /etc/systemd/system/sarangbang-bot.service /etc/systemd/system/music-sarangbang-bot.service
```

복사한 파일에서 이 두 줄만 고칩니다.

```
Description=노래하는 망고 (음악)
ExecStart=/usr/bin/npm run start:music
```

```bash
sudo systemctl daemon-reload && sudo systemctl enable --now music-sarangbang-bot
```

이후로는 따로 재시작합니다. **음악을 고쳐도 읽어주기는 안 끊깁니다.**

```bash
sudo systemctl restart music-sarangbang-bot
```

### 알아두실 것

| | |
|---|---|
| 명령어 목록 | `/도움말` `/기능` `/채널설정` `/나가기` 가 **두 번** 보입니다. 봇 아이콘으로 구분하세요 |
| 설정 | 봇마다 따로입니다. 망고의 `/채널설정` 에는 음악 항목이 아예 안 나옵니다 |
| 저장 위치 | 망고는 `data/`, 노래하는 망고는 `data/music/` |
| 갤러리 웹서버 | **망고만** 띄웁니다 (둘 다 띄우면 포트가 겹쳐 죽습니다) |
| 음성채널 | 각자 들어갑니다. 그래서 음악과 읽어주기가 **겹쳐서** 납니다 |

### 음악을 안 쓰시려면

노래하는 망고를 안 띄우면 됩니다. 망고 혼자 읽어주기·타이머·이미지를 그대로 합니다.
서버에서 빼려면 봇을 추방하고 서비스를 끄세요. 데이터는 `data/music/` 에 그대로 남습니다.

---

## 여러 서버에서 쓰기

봇 하나를 서버 2~3개에서 같이 쓸 수 있습니다. 서버마다 설정이 따로 저장되고,
음악 대기열도 서버별로 독립적입니다 (A서버에서 음악 듣는 중에 B서버에서 따로 틀 수 있습니다).

**해야 할 것은 하나뿐입니다** — `.env` 의 `GUILD_ID` 에 서버 ID를 쉼표로 나열하세요.

```ini
GUILD_ID=123456789012345678,987654321098765432
```

그다음 `npm run deploy` 를 실행하면 각 서버에 명령어가 등록됩니다.
어느 서버에 성공/실패했는지 하나씩 알려줍니다.

> ⚠️ **서버를 추가할 때마다 `GUILD_ID` 에 추가하고 `npm run deploy` 를 다시 실행해야 합니다.**
> 안 하면 그 서버에서는 슬래시 명령어가 보이지 않습니다.
> (유튜브 링크 자동 감지나 읽어주기 같은 메시지 기반 기능은 그래도 동작합니다)

> 📁 **이미지 폴더는 서버끼리 공유합니다.** 두 서버에 똑같이 `#사진` 채널이 있으면
> 같은 폴더에 함께 쌓입니다(파일이 사라지진 않습니다). 나누고 싶으면 한쪽 채널에서
> `/폴더 B서버사진` 처럼 이름을 다르게 지정하면 영구히 분리됩니다.

---

## 나만 쓰게 잠가두기 (권장)

개인용 봇이면 두 가지를 잠가두는 게 좋습니다. **초대를 끝낸 뒤에** 하세요.

### 1) 남이 내 봇을 데려가지 못하게 — Public Bot 끄기

기본값은 켜져 있어서, **APPLICATION ID만 알면 아무나 자기 서버에 초대할 수 있습니다.**
끄면 나(앱 소유자)만 초대할 수 있습니다.

> 🔴 **순서를 반드시 지키세요.** `Public Bot` 은 **Install Link 가 `None` 일 때만** 끌 수 있습니다.
> 그런데 Install Link 를 `None` 으로 두면 초대 링크 자체가 사라집니다.
> 즉 **잠근 뒤에는 새 서버에 초대할 수 없습니다.**

1. **쓰려는 서버 전부에 먼저 초대합니다** (2단계) — 2~3개면 세 곳 다 초대해두세요
2. 초대한 서버들의 ID를 `.env` 의 `GUILD_ID` 에 쉼표로 나열하고 `npm run deploy` 실행
3. 개발자 포털 → **Installation** → **Install Link** 를 **None** → Save
4. 개발자 포털 → **Bot** → **Public Bot** 끄기 → Save

**이미 들어가 있는 서버에서는 봇이 그대로 정상 동작합니다.** 새 초대만 막힙니다.
나중에 서버를 더 추가하려면 3번을 되돌려(Discord Provided Link) 초대한 뒤 다시 잠그면 됩니다.

> `Requires OAuth2 Code Grant` 는 **켜지 마세요.** 켜면 일반 초대가 아예 안 됩니다.

### 2) 특정 채널에서만 동작하게 — 두 겹으로 막기

**(가) 디스코드 권한으로 막기** — 봇이 다른 채널을 아예 못 보게 합니다.

1. 서버 설정 → **역할** → 봇 이름의 역할에서 **채널 보기(View Channels)** 를 **끔**
2. 쓰려는 채널 2~3개로 가서 → 채널 설정 → **권한** → 봇 역할을 추가하고
   그 채널에서만 **채널 보기 / 메시지 보내기 / 메시지 기록 보기 / 반응 추가** 허용
3. 음악·읽어주기를 쓸 **음성채널**에도 봇 역할에 **연결 / 말하기** 허용

이러면 다른 채널의 대화는 봇에게 아예 전달되지 않습니다. 가장 확실한 방법입니다.
**비공개 채널에서도 잘 동작합니다** — 봇 역할만 그 채널에 추가해주면 됩니다.

**(나) 봇 설정으로 막기** — `/채널설정` 으로 기능별 채널을 못박습니다.

특히 **음악 채팅방을 꼭 지정하세요.** 지정하지 않으면 봇이 **볼 수 있는 모든 채널**에서
유튜브 링크를 감지해 재생을 시작합니다. 친구들과 쓰는 서버에서는 성가실 수 있습니다.

```
/채널설정 종류:음악 채팅방  채널:#원하는채널
```

`/채널확인` 으로 지금 무엇이 어디로 지정됐는지 확인할 수 있습니다.

> 💡 슬래시 명령어 자체를 특정 채널에서만 보이게 하려면
> 서버 설정 → **연동(Integrations)** → 봇 선택 → 명령어별로 채널·역할을 제한할 수 있습니다.

---

## 문제가 생겼을 때

| 증상 | 해결 |
|---|---|
| **음악이 갑자기 안 나옴** | `npm run update-ytdlp` 실행 후 재시작. 원인의 90%입니다 |
| `Sign in to confirm you're not a bot` | 아래 "유튜브가 막힐 때" 참고 |
| **읽어주기가 무반응 (에러도 없음)** | 1단계 6번 **MESSAGE CONTENT INTENT** 를 안 켰습니다 |
| **`로그인 실패: Used disallowed intents`** | 토큰 문제가 **아닙니다.** 그 앱의 **MESSAGE CONTENT INTENT** 가 꺼져 있습니다. 봇마다 따로 켜야 합니다 (노래하는 망고를 새로 만들면 특히 빠뜨리기 쉽습니다) |
| **슬래시 명령어가 안 보임** | `npm run deploy` 실행. 그래도 안 되면 2단계 초대 링크로 재초대 |
| `이 애플리케이션에 지원되지 않는 설치 유형` | 2-1단계 **Guild Install** 을 안 켰습니다 |
| **Public Bot 을 끌 수 없음 (회색)** | Installation → Install Link 를 먼저 `None` 으로 바꿔야 합니다 |
| **특정 채널에서만 봇이 반응 안 함** | 그 채널 권한에 봇 역할의 "채널 보기"가 있는지 확인 |
| **엉뚱한 채널에서 유튜브 링크에 반응함** | 음악 채팅방을 지정하지 않았습니다. `/채널설정` 으로 지정하세요 |
| **새로 추가한 서버에 명령어가 안 보임** | `GUILD_ID` 에 그 서버 ID를 추가하고 `npm run deploy` |
| **음성채널을 읽어주기 채팅방으로 못 고름** | 고칠 수 있습니다 (이제 지원). `npm run deploy` 후 다시 시도 |
| `로그인 실패` | `.env` 의 `DISCORD_TOKEN` 이 틀렸습니다 |
| **봇이 음성채널에 못 들어감** | 그 음성채널에서 봇에게 "연결"·"말하기" 권한이 있는지 확인 |
| **사진이 저장 안 됨** | `/채널확인` 으로 이미지 채널이 맞는지, 봇이 그 채널을 볼 수 있는지 확인 |
| **갤러리가 안 열림** | 봇이 실행 중인지, `WEB_PUBLIC_URL` 의 주소·포트가 맞는지 확인 |
| **친구는 갤러리를 못 열음** | `WEB_BIND=0.0.0.0` 인지, 방화벽 **두 곳**을 다 열었는지 확인 |
| **갤러리 링크가 localhost 로 나옴** | `.env` 의 `WEB_PUBLIC_URL` 을 서버 IP로 바꾸고 재시작 |
| **삭제·이동에서 암호를 물어봄** | 정상입니다. `.env` 의 `WEB_TOKEN` 값을 넣으세요 |
| **제어판 버튼이 반응 없음** | 명령어를 새로 등록해야 합니다. `npm run deploy` 후 봇 재시작 |
| **다른 봇과 겹쳐서 같이 반응함** | `/기능` 에서 겹치는 기능을 끄세요 |
| **"기능이 꺼져 있습니다" 가 나옴** | `/기능` 에서 그 기능을 켜세요 |
| **링크 메시지가 안 지워짐** | 봇 역할에 **메시지 관리** 권한을 켜주세요 |
| **제어판이 위로 밀려남** | 위와 같은 원인입니다 (링크가 안 지워져서) |
| **`.env` 를 고쳤는데 안 바뀜** | `/채널설정` 으로 지정한 값이 우선합니다. `/채널확인` 으로 출처를 보고 `/채널해제` 하세요 |
| **일본어가 소리 없이 넘어감** | 한국어 전용 목소리입니다. `/목소리` 로 **현수(다국어)** 를 고르세요 |
| **명령어가 아직 영어(`/play`)로 보임** | `npm run deploy` 를 다시 실행하세요. 등록해야 새 이름이 반영됩니다 |

### 유튜브가 막힐 때

**클라우드 서버(Oracle, AWS, GCP 등)에서 돌리면 거의 반드시 겪습니다.** 집 PC에서는 거의 안 생깁니다.
유튜브가 데이터센터 IP를 봇으로 판단해 막기 때문입니다.

증상이 여러 얼굴로 나타납니다 — 아래는 **전부 같은 원인**입니다.

- `Sign in to confirm you're not a bot`
- `The page needs to be reloaded` (몇 번 재시도해도 계속될 때)
- `playability status: LOGIN_REQUIRED`

**확인 방법** — 서버에서 실행하면 원인이 바로 보입니다.

```bash
cd ~/sarangbang-bot && ./bin/yt-dlp --simulate -v "https://www.youtube.com/watch?v=wp43OdtAAkM" 2>&1 | grep -E "LOGIN_REQUIRED|Sign in|ERROR"
```

`LOGIN_REQUIRED` 가 보이면 IP 차단이 확정입니다.

> ✅ **쿠키와 JS 런타임이 둘 다 있어야 합니다.** 봇은 JS 런타임을 자동으로 붙이므로
> `.env.music` 의 `YTDLP_JS_RUNTIME` 을 건드리지 않았다면 **쿠키만 넣으면 됩니다.**
> 쿠키만 넣고 JS 런타임이 없으면 `n challenge solving failed` 가 납니다.

**해결: 쿠키 넣기**

브라우저에서 유튜브 쿠키를 파일로 뽑아 **`.env.music`** 의 `YTDLP_COOKIES_FILE` 에 경로를 넣습니다.
(음악은 노래하는 망고가 돌립니다. `.env` 에 적어도 동작하지만 나중에 찾기 어렵습니다)
(브라우저 확장 프로그램 "Get cookies.txt LOCALLY" 계열로 `cookies.txt` 를 저장하면 됩니다)

> 🔴 **본인 주계정을 쓰지 마세요.** 데이터센터 IP에서 쿠키를 쓰다가
> 유튜브가 계정을 제한하는 사례가 있습니다. **따로 만든 계정**을 쓰세요.
> 이 파일은 그 계정의 로그인 정보 그 자체입니다 — 공유·커밋 금지 (`.gitignore` 에 이미 있습니다).

> 쿠키는 시간이 지나면 만료됩니다. 다시 막히면 새로 뽑아서 교체하세요.

**대안** — 계정을 걸고 싶지 않다면

| 방법 | 장단점 |
|---|---|
| **PO Token 제공자 설치** (`bgutil-ytdlp-pot-provider` 플러그인) | 계정 위험 없음. 설정이 더 복잡하고 별도 서비스를 띄워야 함 |
| **음악만 집 PC에서 돌리기** | 가장 확실. 가정용 IP는 차단되지 않음. 대신 PC를 켜둬야 함 |
| **주거용 프록시** (`YTDLP_PROXY`) | 동작하지만 유료이고 느림 |

`.env.music` 에 이렇게 적습니다. (집 PC 기준 경로 예시)

```
YTDLP_COOKIES_FILE=C:\sarangbang-bot\cookies.txt
```

서버라면 절대경로로: `/home/ubuntu/sarangbang-bot/cookies.txt`

---

## 다른 PC나 서버에서 이어서 작업하려면

- 설계와 구현 내용은 **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** 에 전부 정리되어 있습니다.
- 코딩 에이전트(Claude Code 등)를 쓴다면 **[CLAUDE.md](CLAUDE.md)** 를 먼저 읽게 하세요.
- **무료 서버에 24시간 올리려면 → [docs/ORACLE-CLOUD.md](docs/ORACLE-CLOUD.md)**
  (Oracle Cloud Always Free 배포 전체 과정. 인스턴스 생성부터 자동 실행 등록까지)

⚠️ 옮길 때 주의: `.env`, `bin/`, `data/`, `node_modules/` 는 저장소에 포함되지 않습니다.
새 PC에서는 `npm install` 과 `npm run update-ytdlp` 를 다시 해야 하고,
`.env` 는 직접 옮기거나 새로 만들어야 합니다.
